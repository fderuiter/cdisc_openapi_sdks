#' CDISC Library API
#'
#' REST API Documentation for CDISC Library in production server
#'
#' The version of the OpenAPI document: 1.1.0
#' Contact: cdisc_library_support@cdisc.org
#' Generated by: https://openapi-generator.tech
#'
#' @docType class
#' @title SENDImplementationGuideSENDIG operations
#' @description SENDImplementationGuideSENDIGApi
#' @format An \code{R6Class} generator object
#' @field api_client Handles the client-server communication.
#'
#' @examples
#' \dontrun{
#' ####################  MdrRootSendigDatasetsDatasetVariablesVarGet  ####################
#'
#' library(openapi)
#' var_dataset <- "VS" # character | SENDIG Dataset Identifier
#' var_var <- "VSTEST" # character | SENDIG Variable Identifier
#'
#' api_instance <- SENDImplementationGuideSENDIGApi$new()
#'
#' # Configure HTTP basic authorization: basicAuth
#' api_instance$api_client$username <- Sys.getenv("USERNAME")
#' api_instance$api_client$password <- Sys.getenv("PASSWORD")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$MdrRootSendigDatasetsDatasetVariablesVarGet(var_dataset, var_vardata_file = "result.txt")
#' result <- api_instance$MdrRootSendigDatasetsDatasetVariablesVarGet(var_dataset, var_var)
#' dput(result)
#'
#'
#' ####################  MdrSendigVersionClassesClassDatasetsGet  ####################
#'
#' library(openapi)
#' var_version <- "3-1" # character | CDISC Library Product Version
#' var_class <- "Findings" # character | SENDIG Class Identifier
#'
#' api_instance <- SENDImplementationGuideSENDIGApi$new()
#'
#' # Configure HTTP basic authorization: basicAuth
#' api_instance$api_client$username <- Sys.getenv("USERNAME")
#' api_instance$api_client$password <- Sys.getenv("PASSWORD")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$MdrSendigVersionClassesClassDatasetsGet(var_version, var_classdata_file = "result.txt")
#' result <- api_instance$MdrSendigVersionClassesClassDatasetsGet(var_version, var_class)
#' dput(result)
#'
#'
#' ####################  MdrSendigVersionClassesClassGet  ####################
#'
#' library(openapi)
#' var_version <- "3-1" # character | CDISC Library Product Version
#' var_class <- "Findings" # character | SENDIG Class Identifier
#'
#' api_instance <- SENDImplementationGuideSENDIGApi$new()
#'
#' # Configure HTTP basic authorization: basicAuth
#' api_instance$api_client$username <- Sys.getenv("USERNAME")
#' api_instance$api_client$password <- Sys.getenv("PASSWORD")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$MdrSendigVersionClassesClassGet(var_version, var_classdata_file = "result.txt")
#' result <- api_instance$MdrSendigVersionClassesClassGet(var_version, var_class)
#' dput(result)
#'
#'
#' ####################  MdrSendigVersionClassesGet  ####################
#'
#' library(openapi)
#' var_version <- "3-1" # character | CDISC Library Product Version
#'
#' api_instance <- SENDImplementationGuideSENDIGApi$new()
#'
#' # Configure HTTP basic authorization: basicAuth
#' api_instance$api_client$username <- Sys.getenv("USERNAME")
#' api_instance$api_client$password <- Sys.getenv("PASSWORD")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$MdrSendigVersionClassesGet(var_versiondata_file = "result.txt")
#' result <- api_instance$MdrSendigVersionClassesGet(var_version)
#' dput(result)
#'
#'
#' ####################  MdrSendigVersionDatasetsDatasetGet  ####################
#'
#' library(openapi)
#' var_version <- "3-1" # character | CDISC Library Product Version
#' var_dataset <- "VS" # character | SENDIG Class Identifier
#'
#' api_instance <- SENDImplementationGuideSENDIGApi$new()
#'
#' # Configure HTTP basic authorization: basicAuth
#' api_instance$api_client$username <- Sys.getenv("USERNAME")
#' api_instance$api_client$password <- Sys.getenv("PASSWORD")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$MdrSendigVersionDatasetsDatasetGet(var_version, var_datasetdata_file = "result.txt")
#' result <- api_instance$MdrSendigVersionDatasetsDatasetGet(var_version, var_dataset)
#' dput(result)
#'
#'
#' ####################  MdrSendigVersionDatasetsDatasetVariablesGet  ####################
#'
#' library(openapi)
#' var_version <- "3-1" # character | CDISC Library Product Version
#' var_dataset <- "VS" # character | SENDIG Class Identifier
#'
#' api_instance <- SENDImplementationGuideSENDIGApi$new()
#'
#' # Configure HTTP basic authorization: basicAuth
#' api_instance$api_client$username <- Sys.getenv("USERNAME")
#' api_instance$api_client$password <- Sys.getenv("PASSWORD")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$MdrSendigVersionDatasetsDatasetVariablesGet(var_version, var_datasetdata_file = "result.txt")
#' result <- api_instance$MdrSendigVersionDatasetsDatasetVariablesGet(var_version, var_dataset)
#' dput(result)
#'
#'
#' ####################  MdrSendigVersionDatasetsDatasetVariablesVarGet  ####################
#'
#' library(openapi)
#' var_version <- "3-1" # character | CDISC Library Product Version
#' var_dataset <- "VS" # character | SENDIG Class Identifier
#' var_var <- "VSTEST" # character | SENDIG Variable Identifier
#'
#' api_instance <- SENDImplementationGuideSENDIGApi$new()
#'
#' # Configure HTTP basic authorization: basicAuth
#' api_instance$api_client$username <- Sys.getenv("USERNAME")
#' api_instance$api_client$password <- Sys.getenv("PASSWORD")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$MdrSendigVersionDatasetsDatasetVariablesVarGet(var_version, var_dataset, var_vardata_file = "result.txt")
#' result <- api_instance$MdrSendigVersionDatasetsDatasetVariablesVarGet(var_version, var_dataset, var_var)
#' dput(result)
#'
#'
#' ####################  MdrSendigVersionDatasetsGet  ####################
#'
#' library(openapi)
#' var_version <- "3-1" # character | CDISC Library Product Version
#'
#' api_instance <- SENDImplementationGuideSENDIGApi$new()
#'
#' # Configure HTTP basic authorization: basicAuth
#' api_instance$api_client$username <- Sys.getenv("USERNAME")
#' api_instance$api_client$password <- Sys.getenv("PASSWORD")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$MdrSendigVersionDatasetsGet(var_versiondata_file = "result.txt")
#' result <- api_instance$MdrSendigVersionDatasetsGet(var_version)
#' dput(result)
#'
#'
#' ####################  MdrSendigVersionGet  ####################
#'
#' library(openapi)
#' var_version <- "3-1" # character | CDISC Library Product Version
#'
#' api_instance <- SENDImplementationGuideSENDIGApi$new()
#'
#' # Configure HTTP basic authorization: basicAuth
#' api_instance$api_client$username <- Sys.getenv("USERNAME")
#' api_instance$api_client$password <- Sys.getenv("PASSWORD")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$MdrSendigVersionGet(var_versiondata_file = "result.txt")
#' result <- api_instance$MdrSendigVersionGet(var_version)
#' dput(result)
#'
#'
#' }
#' @importFrom R6 R6Class
#' @importFrom base64enc base64encode
#' @export
SENDImplementationGuideSENDIGApi <- R6::R6Class(
  "SENDImplementationGuideSENDIGApi",
  public = list(
    api_client = NULL,

    #' @description
    #' Initialize a new SENDImplementationGuideSENDIGApi.
    #'
    #' @param api_client An instance of API client.
    initialize = function(api_client) {
      if (!missing(api_client)) {
        self$api_client <- api_client
      } else {
        self$api_client <- ApiClient$new()
      }
    },

    #' @description
    #' 
    #'
    #' @param dataset SENDIG Dataset Identifier
    #' @param var SENDIG Variable Identifier
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return RootSendigDatasetVariable
    MdrRootSendigDatasetsDatasetVariablesVarGet = function(dataset, var, data_file = NULL, ...) {
      local_var_response <- self$MdrRootSendigDatasetsDatasetVariablesVarGetWithHttpInfo(dataset, var, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' 
    #'
    #' @param dataset SENDIG Dataset Identifier
    #' @param var SENDIG Variable Identifier
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return API response (RootSendigDatasetVariable) with additional information such as HTTP status code, headers
    MdrRootSendigDatasetsDatasetVariablesVarGetWithHttpInfo = function(dataset, var, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`dataset`)) {
        stop("Missing required parameter `dataset`.")
      }

      if (missing(`var`)) {
        stop("Missing required parameter `var`.")
      }

      if (!missing(`dataset`) && is.null(`dataset`)) {
        stop("Invalid value for `dataset` when calling SENDImplementationGuideSENDIGApi$MdrRootSendigDatasetsDatasetVariablesVarGet, `dataset` is not nullable")
      }

      if (!missing(`var`) && is.null(`var`)) {
        stop("Invalid value for `var` when calling SENDImplementationGuideSENDIGApi$MdrRootSendigDatasetsDatasetVariablesVarGet, `var` is not nullable")
      }

      local_var_url_path <- "/mdr/root/sendig/datasets/{dataset}/variables/{var}"
      if (!missing(`dataset`)) {
        local_var_url_path <- gsub("\\{dataset\\}", URLencode(as.character(`dataset`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`var`)) {
        local_var_url_path <- gsub("\\{var\\}", URLencode(as.character(`var`), reserved = TRUE), local_var_url_path)
      }

      # HTTP basic auth
      if (!is.null(self$api_client$username) || !is.null(self$api_client$password)) {
        header_params["Authorization"] <- paste("Basic", base64enc::base64encode(charToRaw(paste(self$api_client$username, self$api_client$password, sep = ":"))))
      }

      # The Accept request HTTP header
      local_var_accepts <- list("application/json", "application/xml")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "RootSendigDatasetVariable"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' 
    #'
    #' @param version CDISC Library Product Version
    #' @param class SENDIG Class Identifier
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return SendigClassDatasets
    MdrSendigVersionClassesClassDatasetsGet = function(version, class, data_file = NULL, ...) {
      local_var_response <- self$MdrSendigVersionClassesClassDatasetsGetWithHttpInfo(version, class, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' 
    #'
    #' @param version CDISC Library Product Version
    #' @param class SENDIG Class Identifier
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return API response (SendigClassDatasets) with additional information such as HTTP status code, headers
    MdrSendigVersionClassesClassDatasetsGetWithHttpInfo = function(version, class, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`version`)) {
        stop("Missing required parameter `version`.")
      }

      if (missing(`class`)) {
        stop("Missing required parameter `class`.")
      }

      if (!missing(`version`) && is.null(`version`)) {
        stop("Invalid value for `version` when calling SENDImplementationGuideSENDIGApi$MdrSendigVersionClassesClassDatasetsGet, `version` is not nullable")
      }

      if (!missing(`class`) && is.null(`class`)) {
        stop("Invalid value for `class` when calling SENDImplementationGuideSENDIGApi$MdrSendigVersionClassesClassDatasetsGet, `class` is not nullable")
      }

      local_var_url_path <- "/mdr/sendig/{version}/classes/{class}/datasets"
      if (!missing(`version`)) {
        local_var_url_path <- gsub("\\{version\\}", URLencode(as.character(`version`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`class`)) {
        local_var_url_path <- gsub("\\{class\\}", URLencode(as.character(`class`), reserved = TRUE), local_var_url_path)
      }

      # HTTP basic auth
      if (!is.null(self$api_client$username) || !is.null(self$api_client$password)) {
        header_params["Authorization"] <- paste("Basic", base64enc::base64encode(charToRaw(paste(self$api_client$username, self$api_client$password, sep = ":"))))
      }

      # The Accept request HTTP header
      local_var_accepts <- list("application/json", "application/xml")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "SendigClassDatasets"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' 
    #'
    #' @param version CDISC Library Product Version
    #' @param class SENDIG Class Identifier
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return SendigClass
    MdrSendigVersionClassesClassGet = function(version, class, data_file = NULL, ...) {
      local_var_response <- self$MdrSendigVersionClassesClassGetWithHttpInfo(version, class, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' 
    #'
    #' @param version CDISC Library Product Version
    #' @param class SENDIG Class Identifier
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return API response (SendigClass) with additional information such as HTTP status code, headers
    MdrSendigVersionClassesClassGetWithHttpInfo = function(version, class, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`version`)) {
        stop("Missing required parameter `version`.")
      }

      if (missing(`class`)) {
        stop("Missing required parameter `class`.")
      }

      if (!missing(`version`) && is.null(`version`)) {
        stop("Invalid value for `version` when calling SENDImplementationGuideSENDIGApi$MdrSendigVersionClassesClassGet, `version` is not nullable")
      }

      if (!missing(`class`) && is.null(`class`)) {
        stop("Invalid value for `class` when calling SENDImplementationGuideSENDIGApi$MdrSendigVersionClassesClassGet, `class` is not nullable")
      }

      local_var_url_path <- "/mdr/sendig/{version}/classes/{class}"
      if (!missing(`version`)) {
        local_var_url_path <- gsub("\\{version\\}", URLencode(as.character(`version`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`class`)) {
        local_var_url_path <- gsub("\\{class\\}", URLencode(as.character(`class`), reserved = TRUE), local_var_url_path)
      }

      # HTTP basic auth
      if (!is.null(self$api_client$username) || !is.null(self$api_client$password)) {
        header_params["Authorization"] <- paste("Basic", base64enc::base64encode(charToRaw(paste(self$api_client$username, self$api_client$password, sep = ":"))))
      }

      # The Accept request HTTP header
      local_var_accepts <- list("application/json", "application/xml")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "SendigClass"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' 
    #'
    #' @param version CDISC Library Product Version
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return SendigClasses
    MdrSendigVersionClassesGet = function(version, data_file = NULL, ...) {
      local_var_response <- self$MdrSendigVersionClassesGetWithHttpInfo(version, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' 
    #'
    #' @param version CDISC Library Product Version
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return API response (SendigClasses) with additional information such as HTTP status code, headers
    MdrSendigVersionClassesGetWithHttpInfo = function(version, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`version`)) {
        stop("Missing required parameter `version`.")
      }

      if (!missing(`version`) && is.null(`version`)) {
        stop("Invalid value for `version` when calling SENDImplementationGuideSENDIGApi$MdrSendigVersionClassesGet, `version` is not nullable")
      }

      local_var_url_path <- "/mdr/sendig/{version}/classes"
      if (!missing(`version`)) {
        local_var_url_path <- gsub("\\{version\\}", URLencode(as.character(`version`), reserved = TRUE), local_var_url_path)
      }

      # HTTP basic auth
      if (!is.null(self$api_client$username) || !is.null(self$api_client$password)) {
        header_params["Authorization"] <- paste("Basic", base64enc::base64encode(charToRaw(paste(self$api_client$username, self$api_client$password, sep = ":"))))
      }

      # The Accept request HTTP header
      local_var_accepts <- list("application/json", "application/xml")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "SendigClasses"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' 
    #'
    #' @param version CDISC Library Product Version
    #' @param dataset SENDIG Class Identifier
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return SendigDataset
    MdrSendigVersionDatasetsDatasetGet = function(version, dataset, data_file = NULL, ...) {
      local_var_response <- self$MdrSendigVersionDatasetsDatasetGetWithHttpInfo(version, dataset, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' 
    #'
    #' @param version CDISC Library Product Version
    #' @param dataset SENDIG Class Identifier
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return API response (SendigDataset) with additional information such as HTTP status code, headers
    MdrSendigVersionDatasetsDatasetGetWithHttpInfo = function(version, dataset, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`version`)) {
        stop("Missing required parameter `version`.")
      }

      if (missing(`dataset`)) {
        stop("Missing required parameter `dataset`.")
      }

      if (!missing(`version`) && is.null(`version`)) {
        stop("Invalid value for `version` when calling SENDImplementationGuideSENDIGApi$MdrSendigVersionDatasetsDatasetGet, `version` is not nullable")
      }

      if (!missing(`dataset`) && is.null(`dataset`)) {
        stop("Invalid value for `dataset` when calling SENDImplementationGuideSENDIGApi$MdrSendigVersionDatasetsDatasetGet, `dataset` is not nullable")
      }

      local_var_url_path <- "/mdr/sendig/{version}/datasets/{dataset}"
      if (!missing(`version`)) {
        local_var_url_path <- gsub("\\{version\\}", URLencode(as.character(`version`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`dataset`)) {
        local_var_url_path <- gsub("\\{dataset\\}", URLencode(as.character(`dataset`), reserved = TRUE), local_var_url_path)
      }

      # HTTP basic auth
      if (!is.null(self$api_client$username) || !is.null(self$api_client$password)) {
        header_params["Authorization"] <- paste("Basic", base64enc::base64encode(charToRaw(paste(self$api_client$username, self$api_client$password, sep = ":"))))
      }

      # The Accept request HTTP header
      local_var_accepts <- list("application/json", "application/xml")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "SendigDataset"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' 
    #'
    #' @param version CDISC Library Product Version
    #' @param dataset SENDIG Class Identifier
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return SendigDatasetVariables
    MdrSendigVersionDatasetsDatasetVariablesGet = function(version, dataset, data_file = NULL, ...) {
      local_var_response <- self$MdrSendigVersionDatasetsDatasetVariablesGetWithHttpInfo(version, dataset, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' 
    #'
    #' @param version CDISC Library Product Version
    #' @param dataset SENDIG Class Identifier
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return API response (SendigDatasetVariables) with additional information such as HTTP status code, headers
    MdrSendigVersionDatasetsDatasetVariablesGetWithHttpInfo = function(version, dataset, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`version`)) {
        stop("Missing required parameter `version`.")
      }

      if (missing(`dataset`)) {
        stop("Missing required parameter `dataset`.")
      }

      if (!missing(`version`) && is.null(`version`)) {
        stop("Invalid value for `version` when calling SENDImplementationGuideSENDIGApi$MdrSendigVersionDatasetsDatasetVariablesGet, `version` is not nullable")
      }

      if (!missing(`dataset`) && is.null(`dataset`)) {
        stop("Invalid value for `dataset` when calling SENDImplementationGuideSENDIGApi$MdrSendigVersionDatasetsDatasetVariablesGet, `dataset` is not nullable")
      }

      local_var_url_path <- "/mdr/sendig/{version}/datasets/{dataset}/variables"
      if (!missing(`version`)) {
        local_var_url_path <- gsub("\\{version\\}", URLencode(as.character(`version`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`dataset`)) {
        local_var_url_path <- gsub("\\{dataset\\}", URLencode(as.character(`dataset`), reserved = TRUE), local_var_url_path)
      }

      # HTTP basic auth
      if (!is.null(self$api_client$username) || !is.null(self$api_client$password)) {
        header_params["Authorization"] <- paste("Basic", base64enc::base64encode(charToRaw(paste(self$api_client$username, self$api_client$password, sep = ":"))))
      }

      # The Accept request HTTP header
      local_var_accepts <- list("application/json", "application/xml")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "SendigDatasetVariables"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' 
    #'
    #' @param version CDISC Library Product Version
    #' @param dataset SENDIG Class Identifier
    #' @param var SENDIG Variable Identifier
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return SendigDatasetVariable
    MdrSendigVersionDatasetsDatasetVariablesVarGet = function(version, dataset, var, data_file = NULL, ...) {
      local_var_response <- self$MdrSendigVersionDatasetsDatasetVariablesVarGetWithHttpInfo(version, dataset, var, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' 
    #'
    #' @param version CDISC Library Product Version
    #' @param dataset SENDIG Class Identifier
    #' @param var SENDIG Variable Identifier
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return API response (SendigDatasetVariable) with additional information such as HTTP status code, headers
    MdrSendigVersionDatasetsDatasetVariablesVarGetWithHttpInfo = function(version, dataset, var, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`version`)) {
        stop("Missing required parameter `version`.")
      }

      if (missing(`dataset`)) {
        stop("Missing required parameter `dataset`.")
      }

      if (missing(`var`)) {
        stop("Missing required parameter `var`.")
      }

      if (!missing(`version`) && is.null(`version`)) {
        stop("Invalid value for `version` when calling SENDImplementationGuideSENDIGApi$MdrSendigVersionDatasetsDatasetVariablesVarGet, `version` is not nullable")
      }

      if (!missing(`dataset`) && is.null(`dataset`)) {
        stop("Invalid value for `dataset` when calling SENDImplementationGuideSENDIGApi$MdrSendigVersionDatasetsDatasetVariablesVarGet, `dataset` is not nullable")
      }

      if (!missing(`var`) && is.null(`var`)) {
        stop("Invalid value for `var` when calling SENDImplementationGuideSENDIGApi$MdrSendigVersionDatasetsDatasetVariablesVarGet, `var` is not nullable")
      }

      local_var_url_path <- "/mdr/sendig/{version}/datasets/{dataset}/variables/{var}"
      if (!missing(`version`)) {
        local_var_url_path <- gsub("\\{version\\}", URLencode(as.character(`version`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`dataset`)) {
        local_var_url_path <- gsub("\\{dataset\\}", URLencode(as.character(`dataset`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`var`)) {
        local_var_url_path <- gsub("\\{var\\}", URLencode(as.character(`var`), reserved = TRUE), local_var_url_path)
      }

      # HTTP basic auth
      if (!is.null(self$api_client$username) || !is.null(self$api_client$password)) {
        header_params["Authorization"] <- paste("Basic", base64enc::base64encode(charToRaw(paste(self$api_client$username, self$api_client$password, sep = ":"))))
      }

      # The Accept request HTTP header
      local_var_accepts <- list("application/json", "application/xml")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "SendigDatasetVariable"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' 
    #'
    #' @param version CDISC Library Product Version
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return SendigDatasets
    MdrSendigVersionDatasetsGet = function(version, data_file = NULL, ...) {
      local_var_response <- self$MdrSendigVersionDatasetsGetWithHttpInfo(version, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' 
    #'
    #' @param version CDISC Library Product Version
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return API response (SendigDatasets) with additional information such as HTTP status code, headers
    MdrSendigVersionDatasetsGetWithHttpInfo = function(version, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`version`)) {
        stop("Missing required parameter `version`.")
      }

      if (!missing(`version`) && is.null(`version`)) {
        stop("Invalid value for `version` when calling SENDImplementationGuideSENDIGApi$MdrSendigVersionDatasetsGet, `version` is not nullable")
      }

      local_var_url_path <- "/mdr/sendig/{version}/datasets"
      if (!missing(`version`)) {
        local_var_url_path <- gsub("\\{version\\}", URLencode(as.character(`version`), reserved = TRUE), local_var_url_path)
      }

      # HTTP basic auth
      if (!is.null(self$api_client$username) || !is.null(self$api_client$password)) {
        header_params["Authorization"] <- paste("Basic", base64enc::base64encode(charToRaw(paste(self$api_client$username, self$api_client$password, sep = ":"))))
      }

      # The Accept request HTTP header
      local_var_accepts <- list("application/json", "application/xml", "text/csv")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "SendigDatasets"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' 
    #'
    #' @param version CDISC Library Product Version
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return SendigProduct
    MdrSendigVersionGet = function(version, data_file = NULL, ...) {
      local_var_response <- self$MdrSendigVersionGetWithHttpInfo(version, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' 
    #'
    #' @param version CDISC Library Product Version
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return API response (SendigProduct) with additional information such as HTTP status code, headers
    MdrSendigVersionGetWithHttpInfo = function(version, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`version`)) {
        stop("Missing required parameter `version`.")
      }

      if (!missing(`version`) && is.null(`version`)) {
        stop("Invalid value for `version` when calling SENDImplementationGuideSENDIGApi$MdrSendigVersionGet, `version` is not nullable")
      }

      local_var_url_path <- "/mdr/sendig/{version}"
      if (!missing(`version`)) {
        local_var_url_path <- gsub("\\{version\\}", URLencode(as.character(`version`), reserved = TRUE), local_var_url_path)
      }

      # HTTP basic auth
      if (!is.null(self$api_client$username) || !is.null(self$api_client$password)) {
        header_params["Authorization"] <- paste("Basic", base64enc::base64encode(charToRaw(paste(self$api_client$username, self$api_client$password, sep = ":"))))
      }

      # The Accept request HTTP header
      local_var_accepts <- list("application/json", "application/xml", "text/csv", "application/vnd.ms-excel", "application/vnd.openxmlformats-officedocument.spreadsheetml.sheet")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "SendigProduct"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    }
  )
)
