#' CDISC Library API
#'
#' REST API Documentation for CDISC Library in production server
#'
#' The version of the OpenAPI document: 1.1.0
#' Contact: cdisc_library_support@cdisc.org
#' Generated by: https://openapi-generator.tech
#'
#' @docType class
#' @title ClinicalDataAcquisitionStandardsHarmonizationCDASH operations
#' @description ClinicalDataAcquisitionStandardsHarmonizationCDASHApi
#' @format An \code{R6Class} generator object
#' @field api_client Handles the client-server communication.
#'
#' @examples
#' \dontrun{
#' ####################  MdrCdashVersionClassesClassDomainsGet  ####################
#'
#' library(openapi)
#' var_version <- "1-1" # character | CDISC Library Product Version
#' var_class <- "SpecialPurpose" # character | CDASH Class Identifier
#'
#' api_instance <- ClinicalDataAcquisitionStandardsHarmonizationCDASHApi$new()
#'
#' # Configure HTTP basic authorization: basicAuth
#' api_instance$api_client$username <- Sys.getenv("USERNAME")
#' api_instance$api_client$password <- Sys.getenv("PASSWORD")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$MdrCdashVersionClassesClassDomainsGet(var_version, var_classdata_file = "result.txt")
#' result <- api_instance$MdrCdashVersionClassesClassDomainsGet(var_version, var_class)
#' dput(result)
#'
#'
#' ####################  MdrCdashVersionClassesClassFieldsFieldGet  ####################
#'
#' library(openapi)
#' var_version <- "1-1" # character | CDISC Library Product Version
#' var_class <- "Timing" # character | CDASH Class Identifier
#' var_field <- "--DAT" # character | CDASH Field Identifier
#'
#' api_instance <- ClinicalDataAcquisitionStandardsHarmonizationCDASHApi$new()
#'
#' # Configure HTTP basic authorization: basicAuth
#' api_instance$api_client$username <- Sys.getenv("USERNAME")
#' api_instance$api_client$password <- Sys.getenv("PASSWORD")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$MdrCdashVersionClassesClassFieldsFieldGet(var_version, var_class, var_fielddata_file = "result.txt")
#' result <- api_instance$MdrCdashVersionClassesClassFieldsFieldGet(var_version, var_class, var_field)
#' dput(result)
#'
#'
#' ####################  MdrCdashVersionClassesClassGet  ####################
#'
#' library(openapi)
#' var_version <- "1-1" # character | CDISC Library Product Version
#' var_class <- "Timing" # character | CDASH Class Identifier
#'
#' api_instance <- ClinicalDataAcquisitionStandardsHarmonizationCDASHApi$new()
#'
#' # Configure HTTP basic authorization: basicAuth
#' api_instance$api_client$username <- Sys.getenv("USERNAME")
#' api_instance$api_client$password <- Sys.getenv("PASSWORD")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$MdrCdashVersionClassesClassGet(var_version, var_classdata_file = "result.txt")
#' result <- api_instance$MdrCdashVersionClassesClassGet(var_version, var_class)
#' dput(result)
#'
#'
#' ####################  MdrCdashVersionClassesGet  ####################
#'
#' library(openapi)
#' var_version <- "1-1" # character | CDISC Library Product Version
#'
#' api_instance <- ClinicalDataAcquisitionStandardsHarmonizationCDASHApi$new()
#'
#' # Configure HTTP basic authorization: basicAuth
#' api_instance$api_client$username <- Sys.getenv("USERNAME")
#' api_instance$api_client$password <- Sys.getenv("PASSWORD")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$MdrCdashVersionClassesGet(var_versiondata_file = "result.txt")
#' result <- api_instance$MdrCdashVersionClassesGet(var_version)
#' dput(result)
#'
#'
#' ####################  MdrCdashVersionDomainsDomainFieldsFieldGet  ####################
#'
#' library(openapi)
#' var_version <- "1-1" # character | CDISC Library Product Version
#' var_domain <- "DM" # character | CDASH Domain Identifier
#' var_field <- "AGE" # character | CDASH Field Identifier
#'
#' api_instance <- ClinicalDataAcquisitionStandardsHarmonizationCDASHApi$new()
#'
#' # Configure HTTP basic authorization: basicAuth
#' api_instance$api_client$username <- Sys.getenv("USERNAME")
#' api_instance$api_client$password <- Sys.getenv("PASSWORD")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$MdrCdashVersionDomainsDomainFieldsFieldGet(var_version, var_domain, var_fielddata_file = "result.txt")
#' result <- api_instance$MdrCdashVersionDomainsDomainFieldsFieldGet(var_version, var_domain, var_field)
#' dput(result)
#'
#'
#' ####################  MdrCdashVersionDomainsDomainFieldsGet  ####################
#'
#' library(openapi)
#' var_version <- "1-1" # character | CDISC Library Product Version
#' var_domain <- "DM" # character | CDASH Domain Identifier
#'
#' api_instance <- ClinicalDataAcquisitionStandardsHarmonizationCDASHApi$new()
#'
#' # Configure HTTP basic authorization: basicAuth
#' api_instance$api_client$username <- Sys.getenv("USERNAME")
#' api_instance$api_client$password <- Sys.getenv("PASSWORD")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$MdrCdashVersionDomainsDomainFieldsGet(var_version, var_domaindata_file = "result.txt")
#' result <- api_instance$MdrCdashVersionDomainsDomainFieldsGet(var_version, var_domain)
#' dput(result)
#'
#'
#' ####################  MdrCdashVersionDomainsDomainGet  ####################
#'
#' library(openapi)
#' var_version <- "1-1" # character | CDISC Library Product Version
#' var_domain <- "DM" # character | CDASH Domain Identifier
#'
#' api_instance <- ClinicalDataAcquisitionStandardsHarmonizationCDASHApi$new()
#'
#' # Configure HTTP basic authorization: basicAuth
#' api_instance$api_client$username <- Sys.getenv("USERNAME")
#' api_instance$api_client$password <- Sys.getenv("PASSWORD")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$MdrCdashVersionDomainsDomainGet(var_version, var_domaindata_file = "result.txt")
#' result <- api_instance$MdrCdashVersionDomainsDomainGet(var_version, var_domain)
#' dput(result)
#'
#'
#' ####################  MdrCdashVersionDomainsGet  ####################
#'
#' library(openapi)
#' var_version <- "1-1" # character | CDISC Library Product Version
#'
#' api_instance <- ClinicalDataAcquisitionStandardsHarmonizationCDASHApi$new()
#'
#' # Configure HTTP basic authorization: basicAuth
#' api_instance$api_client$username <- Sys.getenv("USERNAME")
#' api_instance$api_client$password <- Sys.getenv("PASSWORD")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$MdrCdashVersionDomainsGet(var_versiondata_file = "result.txt")
#' result <- api_instance$MdrCdashVersionDomainsGet(var_version)
#' dput(result)
#'
#'
#' ####################  MdrCdashVersionGet  ####################
#'
#' library(openapi)
#' var_version <- "1-1" # character | CDISC Library Product Version
#'
#' api_instance <- ClinicalDataAcquisitionStandardsHarmonizationCDASHApi$new()
#'
#' # Configure HTTP basic authorization: basicAuth
#' api_instance$api_client$username <- Sys.getenv("USERNAME")
#' api_instance$api_client$password <- Sys.getenv("PASSWORD")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$MdrCdashVersionGet(var_versiondata_file = "result.txt")
#' result <- api_instance$MdrCdashVersionGet(var_version)
#' dput(result)
#'
#'
#' ####################  MdrRootCdashClassesClassFieldsFieldGet  ####################
#'
#' library(openapi)
#' var_class <- "Timing" # character | CDASH Class Identifier
#' var_field <- "--DAT" # character | CDASH Field Identifier
#'
#' api_instance <- ClinicalDataAcquisitionStandardsHarmonizationCDASHApi$new()
#'
#' # Configure HTTP basic authorization: basicAuth
#' api_instance$api_client$username <- Sys.getenv("USERNAME")
#' api_instance$api_client$password <- Sys.getenv("PASSWORD")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$MdrRootCdashClassesClassFieldsFieldGet(var_class, var_fielddata_file = "result.txt")
#' result <- api_instance$MdrRootCdashClassesClassFieldsFieldGet(var_class, var_field)
#' dput(result)
#'
#'
#' ####################  MdrRootCdashDomainsDomainFieldsFieldGet  ####################
#'
#' library(openapi)
#' var_domain <- "DM" # character | CDASH Domain Identifier
#' var_field <- "AGE" # character | CDASH Field Identifier
#'
#' api_instance <- ClinicalDataAcquisitionStandardsHarmonizationCDASHApi$new()
#'
#' # Configure HTTP basic authorization: basicAuth
#' api_instance$api_client$username <- Sys.getenv("USERNAME")
#' api_instance$api_client$password <- Sys.getenv("PASSWORD")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$MdrRootCdashDomainsDomainFieldsFieldGet(var_domain, var_fielddata_file = "result.txt")
#' result <- api_instance$MdrRootCdashDomainsDomainFieldsFieldGet(var_domain, var_field)
#' dput(result)
#'
#'
#' }
#' @importFrom R6 R6Class
#' @importFrom base64enc base64encode
#' @export
ClinicalDataAcquisitionStandardsHarmonizationCDASHApi <- R6::R6Class(
  "ClinicalDataAcquisitionStandardsHarmonizationCDASHApi",
  public = list(
    api_client = NULL,

    #' @description
    #' Initialize a new ClinicalDataAcquisitionStandardsHarmonizationCDASHApi.
    #'
    #' @param api_client An instance of API client.
    initialize = function(api_client) {
      if (!missing(api_client)) {
        self$api_client <- api_client
      } else {
        self$api_client <- ApiClient$new()
      }
    },

    #' @description
    #' 
    #'
    #' @param version CDISC Library Product Version
    #' @param class CDASH Class Identifier
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return CdashClassDomains
    MdrCdashVersionClassesClassDomainsGet = function(version, class, data_file = NULL, ...) {
      local_var_response <- self$MdrCdashVersionClassesClassDomainsGetWithHttpInfo(version, class, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' 
    #'
    #' @param version CDISC Library Product Version
    #' @param class CDASH Class Identifier
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return API response (CdashClassDomains) with additional information such as HTTP status code, headers
    MdrCdashVersionClassesClassDomainsGetWithHttpInfo = function(version, class, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`version`)) {
        stop("Missing required parameter `version`.")
      }

      if (missing(`class`)) {
        stop("Missing required parameter `class`.")
      }

      if (!missing(`version`) && is.null(`version`)) {
        stop("Invalid value for `version` when calling ClinicalDataAcquisitionStandardsHarmonizationCDASHApi$MdrCdashVersionClassesClassDomainsGet, `version` is not nullable")
      }

      if (!missing(`class`) && is.null(`class`)) {
        stop("Invalid value for `class` when calling ClinicalDataAcquisitionStandardsHarmonizationCDASHApi$MdrCdashVersionClassesClassDomainsGet, `class` is not nullable")
      }

      local_var_url_path <- "/mdr/cdash/{version}/classes/{class}/domains"
      if (!missing(`version`)) {
        local_var_url_path <- gsub("\\{version\\}", URLencode(as.character(`version`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`class`)) {
        local_var_url_path <- gsub("\\{class\\}", URLencode(as.character(`class`), reserved = TRUE), local_var_url_path)
      }

      # HTTP basic auth
      if (!is.null(self$api_client$username) || !is.null(self$api_client$password)) {
        header_params["Authorization"] <- paste("Basic", base64enc::base64encode(charToRaw(paste(self$api_client$username, self$api_client$password, sep = ":"))))
      }

      # The Accept request HTTP header
      local_var_accepts <- list("application/json", "application/xml")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "CdashClassDomains"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' 
    #'
    #' @param version CDISC Library Product Version
    #' @param class CDASH Class Identifier
    #' @param field CDASH Field Identifier
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return CdashClassField
    MdrCdashVersionClassesClassFieldsFieldGet = function(version, class, field, data_file = NULL, ...) {
      local_var_response <- self$MdrCdashVersionClassesClassFieldsFieldGetWithHttpInfo(version, class, field, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' 
    #'
    #' @param version CDISC Library Product Version
    #' @param class CDASH Class Identifier
    #' @param field CDASH Field Identifier
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return API response (CdashClassField) with additional information such as HTTP status code, headers
    MdrCdashVersionClassesClassFieldsFieldGetWithHttpInfo = function(version, class, field, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`version`)) {
        stop("Missing required parameter `version`.")
      }

      if (missing(`class`)) {
        stop("Missing required parameter `class`.")
      }

      if (missing(`field`)) {
        stop("Missing required parameter `field`.")
      }

      if (!missing(`version`) && is.null(`version`)) {
        stop("Invalid value for `version` when calling ClinicalDataAcquisitionStandardsHarmonizationCDASHApi$MdrCdashVersionClassesClassFieldsFieldGet, `version` is not nullable")
      }

      if (!missing(`class`) && is.null(`class`)) {
        stop("Invalid value for `class` when calling ClinicalDataAcquisitionStandardsHarmonizationCDASHApi$MdrCdashVersionClassesClassFieldsFieldGet, `class` is not nullable")
      }

      if (!missing(`field`) && is.null(`field`)) {
        stop("Invalid value for `field` when calling ClinicalDataAcquisitionStandardsHarmonizationCDASHApi$MdrCdashVersionClassesClassFieldsFieldGet, `field` is not nullable")
      }

      local_var_url_path <- "/mdr/cdash/{version}/classes/{class}/fields/{field}"
      if (!missing(`version`)) {
        local_var_url_path <- gsub("\\{version\\}", URLencode(as.character(`version`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`class`)) {
        local_var_url_path <- gsub("\\{class\\}", URLencode(as.character(`class`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`field`)) {
        local_var_url_path <- gsub("\\{field\\}", URLencode(as.character(`field`), reserved = TRUE), local_var_url_path)
      }

      # HTTP basic auth
      if (!is.null(self$api_client$username) || !is.null(self$api_client$password)) {
        header_params["Authorization"] <- paste("Basic", base64enc::base64encode(charToRaw(paste(self$api_client$username, self$api_client$password, sep = ":"))))
      }

      # The Accept request HTTP header
      local_var_accepts <- list("application/json", "application/xml")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "CdashClassField"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' 
    #'
    #' @param version CDISC Library Product Version
    #' @param class CDASH Class Identifier
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return CdashClass
    MdrCdashVersionClassesClassGet = function(version, class, data_file = NULL, ...) {
      local_var_response <- self$MdrCdashVersionClassesClassGetWithHttpInfo(version, class, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' 
    #'
    #' @param version CDISC Library Product Version
    #' @param class CDASH Class Identifier
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return API response (CdashClass) with additional information such as HTTP status code, headers
    MdrCdashVersionClassesClassGetWithHttpInfo = function(version, class, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`version`)) {
        stop("Missing required parameter `version`.")
      }

      if (missing(`class`)) {
        stop("Missing required parameter `class`.")
      }

      if (!missing(`version`) && is.null(`version`)) {
        stop("Invalid value for `version` when calling ClinicalDataAcquisitionStandardsHarmonizationCDASHApi$MdrCdashVersionClassesClassGet, `version` is not nullable")
      }

      if (!missing(`class`) && is.null(`class`)) {
        stop("Invalid value for `class` when calling ClinicalDataAcquisitionStandardsHarmonizationCDASHApi$MdrCdashVersionClassesClassGet, `class` is not nullable")
      }

      local_var_url_path <- "/mdr/cdash/{version}/classes/{class}"
      if (!missing(`version`)) {
        local_var_url_path <- gsub("\\{version\\}", URLencode(as.character(`version`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`class`)) {
        local_var_url_path <- gsub("\\{class\\}", URLencode(as.character(`class`), reserved = TRUE), local_var_url_path)
      }

      # HTTP basic auth
      if (!is.null(self$api_client$username) || !is.null(self$api_client$password)) {
        header_params["Authorization"] <- paste("Basic", base64enc::base64encode(charToRaw(paste(self$api_client$username, self$api_client$password, sep = ":"))))
      }

      # The Accept request HTTP header
      local_var_accepts <- list("application/json", "application/xml")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "CdashClass"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' 
    #'
    #' @param version CDISC Library Product Version
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return CdashProductClasses
    MdrCdashVersionClassesGet = function(version, data_file = NULL, ...) {
      local_var_response <- self$MdrCdashVersionClassesGetWithHttpInfo(version, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' 
    #'
    #' @param version CDISC Library Product Version
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return API response (CdashProductClasses) with additional information such as HTTP status code, headers
    MdrCdashVersionClassesGetWithHttpInfo = function(version, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`version`)) {
        stop("Missing required parameter `version`.")
      }

      if (!missing(`version`) && is.null(`version`)) {
        stop("Invalid value for `version` when calling ClinicalDataAcquisitionStandardsHarmonizationCDASHApi$MdrCdashVersionClassesGet, `version` is not nullable")
      }

      local_var_url_path <- "/mdr/cdash/{version}/classes"
      if (!missing(`version`)) {
        local_var_url_path <- gsub("\\{version\\}", URLencode(as.character(`version`), reserved = TRUE), local_var_url_path)
      }

      # HTTP basic auth
      if (!is.null(self$api_client$username) || !is.null(self$api_client$password)) {
        header_params["Authorization"] <- paste("Basic", base64enc::base64encode(charToRaw(paste(self$api_client$username, self$api_client$password, sep = ":"))))
      }

      # The Accept request HTTP header
      local_var_accepts <- list("application/json", "application/xml")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "CdashProductClasses"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' 
    #'
    #' @param version CDISC Library Product Version
    #' @param domain CDASH Domain Identifier
    #' @param field CDASH Field Identifier
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return CdashDomainField
    MdrCdashVersionDomainsDomainFieldsFieldGet = function(version, domain, field, data_file = NULL, ...) {
      local_var_response <- self$MdrCdashVersionDomainsDomainFieldsFieldGetWithHttpInfo(version, domain, field, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' 
    #'
    #' @param version CDISC Library Product Version
    #' @param domain CDASH Domain Identifier
    #' @param field CDASH Field Identifier
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return API response (CdashDomainField) with additional information such as HTTP status code, headers
    MdrCdashVersionDomainsDomainFieldsFieldGetWithHttpInfo = function(version, domain, field, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`version`)) {
        stop("Missing required parameter `version`.")
      }

      if (missing(`domain`)) {
        stop("Missing required parameter `domain`.")
      }

      if (missing(`field`)) {
        stop("Missing required parameter `field`.")
      }

      if (!missing(`version`) && is.null(`version`)) {
        stop("Invalid value for `version` when calling ClinicalDataAcquisitionStandardsHarmonizationCDASHApi$MdrCdashVersionDomainsDomainFieldsFieldGet, `version` is not nullable")
      }

      if (!missing(`domain`) && is.null(`domain`)) {
        stop("Invalid value for `domain` when calling ClinicalDataAcquisitionStandardsHarmonizationCDASHApi$MdrCdashVersionDomainsDomainFieldsFieldGet, `domain` is not nullable")
      }

      if (!missing(`field`) && is.null(`field`)) {
        stop("Invalid value for `field` when calling ClinicalDataAcquisitionStandardsHarmonizationCDASHApi$MdrCdashVersionDomainsDomainFieldsFieldGet, `field` is not nullable")
      }

      local_var_url_path <- "/mdr/cdash/{version}/domains/{domain}/fields/{field}"
      if (!missing(`version`)) {
        local_var_url_path <- gsub("\\{version\\}", URLencode(as.character(`version`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`domain`)) {
        local_var_url_path <- gsub("\\{domain\\}", URLencode(as.character(`domain`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`field`)) {
        local_var_url_path <- gsub("\\{field\\}", URLencode(as.character(`field`), reserved = TRUE), local_var_url_path)
      }

      # HTTP basic auth
      if (!is.null(self$api_client$username) || !is.null(self$api_client$password)) {
        header_params["Authorization"] <- paste("Basic", base64enc::base64encode(charToRaw(paste(self$api_client$username, self$api_client$password, sep = ":"))))
      }

      # The Accept request HTTP header
      local_var_accepts <- list("application/json", "application/xml")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "CdashDomainField"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' 
    #'
    #' @param version CDISC Library Product Version
    #' @param domain CDASH Domain Identifier
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return CdashDomainFields
    MdrCdashVersionDomainsDomainFieldsGet = function(version, domain, data_file = NULL, ...) {
      local_var_response <- self$MdrCdashVersionDomainsDomainFieldsGetWithHttpInfo(version, domain, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' 
    #'
    #' @param version CDISC Library Product Version
    #' @param domain CDASH Domain Identifier
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return API response (CdashDomainFields) with additional information such as HTTP status code, headers
    MdrCdashVersionDomainsDomainFieldsGetWithHttpInfo = function(version, domain, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`version`)) {
        stop("Missing required parameter `version`.")
      }

      if (missing(`domain`)) {
        stop("Missing required parameter `domain`.")
      }

      if (!missing(`version`) && is.null(`version`)) {
        stop("Invalid value for `version` when calling ClinicalDataAcquisitionStandardsHarmonizationCDASHApi$MdrCdashVersionDomainsDomainFieldsGet, `version` is not nullable")
      }

      if (!missing(`domain`) && is.null(`domain`)) {
        stop("Invalid value for `domain` when calling ClinicalDataAcquisitionStandardsHarmonizationCDASHApi$MdrCdashVersionDomainsDomainFieldsGet, `domain` is not nullable")
      }

      local_var_url_path <- "/mdr/cdash/{version}/domains/{domain}/fields"
      if (!missing(`version`)) {
        local_var_url_path <- gsub("\\{version\\}", URLencode(as.character(`version`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`domain`)) {
        local_var_url_path <- gsub("\\{domain\\}", URLencode(as.character(`domain`), reserved = TRUE), local_var_url_path)
      }

      # HTTP basic auth
      if (!is.null(self$api_client$username) || !is.null(self$api_client$password)) {
        header_params["Authorization"] <- paste("Basic", base64enc::base64encode(charToRaw(paste(self$api_client$username, self$api_client$password, sep = ":"))))
      }

      # The Accept request HTTP header
      local_var_accepts <- list("application/json", "application/xml")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "CdashDomainFields"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' 
    #'
    #' @param version CDISC Library Product Version
    #' @param domain CDASH Domain Identifier
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return CdashDomain
    MdrCdashVersionDomainsDomainGet = function(version, domain, data_file = NULL, ...) {
      local_var_response <- self$MdrCdashVersionDomainsDomainGetWithHttpInfo(version, domain, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' 
    #'
    #' @param version CDISC Library Product Version
    #' @param domain CDASH Domain Identifier
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return API response (CdashDomain) with additional information such as HTTP status code, headers
    MdrCdashVersionDomainsDomainGetWithHttpInfo = function(version, domain, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`version`)) {
        stop("Missing required parameter `version`.")
      }

      if (missing(`domain`)) {
        stop("Missing required parameter `domain`.")
      }

      if (!missing(`version`) && is.null(`version`)) {
        stop("Invalid value for `version` when calling ClinicalDataAcquisitionStandardsHarmonizationCDASHApi$MdrCdashVersionDomainsDomainGet, `version` is not nullable")
      }

      if (!missing(`domain`) && is.null(`domain`)) {
        stop("Invalid value for `domain` when calling ClinicalDataAcquisitionStandardsHarmonizationCDASHApi$MdrCdashVersionDomainsDomainGet, `domain` is not nullable")
      }

      local_var_url_path <- "/mdr/cdash/{version}/domains/{domain}"
      if (!missing(`version`)) {
        local_var_url_path <- gsub("\\{version\\}", URLencode(as.character(`version`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`domain`)) {
        local_var_url_path <- gsub("\\{domain\\}", URLencode(as.character(`domain`), reserved = TRUE), local_var_url_path)
      }

      # HTTP basic auth
      if (!is.null(self$api_client$username) || !is.null(self$api_client$password)) {
        header_params["Authorization"] <- paste("Basic", base64enc::base64encode(charToRaw(paste(self$api_client$username, self$api_client$password, sep = ":"))))
      }

      # The Accept request HTTP header
      local_var_accepts <- list("application/json", "application/xml")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "CdashDomain"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' 
    #'
    #' @param version CDISC Library Product Version
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return CdashProductDomains
    MdrCdashVersionDomainsGet = function(version, data_file = NULL, ...) {
      local_var_response <- self$MdrCdashVersionDomainsGetWithHttpInfo(version, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' 
    #'
    #' @param version CDISC Library Product Version
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return API response (CdashProductDomains) with additional information such as HTTP status code, headers
    MdrCdashVersionDomainsGetWithHttpInfo = function(version, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`version`)) {
        stop("Missing required parameter `version`.")
      }

      if (!missing(`version`) && is.null(`version`)) {
        stop("Invalid value for `version` when calling ClinicalDataAcquisitionStandardsHarmonizationCDASHApi$MdrCdashVersionDomainsGet, `version` is not nullable")
      }

      local_var_url_path <- "/mdr/cdash/{version}/domains"
      if (!missing(`version`)) {
        local_var_url_path <- gsub("\\{version\\}", URLencode(as.character(`version`), reserved = TRUE), local_var_url_path)
      }

      # HTTP basic auth
      if (!is.null(self$api_client$username) || !is.null(self$api_client$password)) {
        header_params["Authorization"] <- paste("Basic", base64enc::base64encode(charToRaw(paste(self$api_client$username, self$api_client$password, sep = ":"))))
      }

      # The Accept request HTTP header
      local_var_accepts <- list("application/json", "application/xml")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "CdashProductDomains"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' 
    #'
    #' @param version CDISC Library Product Version
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return CdashProduct
    MdrCdashVersionGet = function(version, data_file = NULL, ...) {
      local_var_response <- self$MdrCdashVersionGetWithHttpInfo(version, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' 
    #'
    #' @param version CDISC Library Product Version
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return API response (CdashProduct) with additional information such as HTTP status code, headers
    MdrCdashVersionGetWithHttpInfo = function(version, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`version`)) {
        stop("Missing required parameter `version`.")
      }

      if (!missing(`version`) && is.null(`version`)) {
        stop("Invalid value for `version` when calling ClinicalDataAcquisitionStandardsHarmonizationCDASHApi$MdrCdashVersionGet, `version` is not nullable")
      }

      local_var_url_path <- "/mdr/cdash/{version}"
      if (!missing(`version`)) {
        local_var_url_path <- gsub("\\{version\\}", URLencode(as.character(`version`), reserved = TRUE), local_var_url_path)
      }

      # HTTP basic auth
      if (!is.null(self$api_client$username) || !is.null(self$api_client$password)) {
        header_params["Authorization"] <- paste("Basic", base64enc::base64encode(charToRaw(paste(self$api_client$username, self$api_client$password, sep = ":"))))
      }

      # The Accept request HTTP header
      local_var_accepts <- list("application/json", "application/xml", "text/csv", "application/vnd.ms-excel", "application/vnd.openxmlformats-officedocument.spreadsheetml.sheet")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "CdashProduct"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' 
    #'
    #' @param class CDASH Class Identifier
    #' @param field CDASH Field Identifier
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return RootCdashClassField
    MdrRootCdashClassesClassFieldsFieldGet = function(class, field, data_file = NULL, ...) {
      local_var_response <- self$MdrRootCdashClassesClassFieldsFieldGetWithHttpInfo(class, field, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' 
    #'
    #' @param class CDASH Class Identifier
    #' @param field CDASH Field Identifier
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return API response (RootCdashClassField) with additional information such as HTTP status code, headers
    MdrRootCdashClassesClassFieldsFieldGetWithHttpInfo = function(class, field, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`class`)) {
        stop("Missing required parameter `class`.")
      }

      if (missing(`field`)) {
        stop("Missing required parameter `field`.")
      }

      if (!missing(`class`) && is.null(`class`)) {
        stop("Invalid value for `class` when calling ClinicalDataAcquisitionStandardsHarmonizationCDASHApi$MdrRootCdashClassesClassFieldsFieldGet, `class` is not nullable")
      }

      if (!missing(`field`) && is.null(`field`)) {
        stop("Invalid value for `field` when calling ClinicalDataAcquisitionStandardsHarmonizationCDASHApi$MdrRootCdashClassesClassFieldsFieldGet, `field` is not nullable")
      }

      local_var_url_path <- "/mdr/root/cdash/classes/{class}/fields/{field}"
      if (!missing(`class`)) {
        local_var_url_path <- gsub("\\{class\\}", URLencode(as.character(`class`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`field`)) {
        local_var_url_path <- gsub("\\{field\\}", URLencode(as.character(`field`), reserved = TRUE), local_var_url_path)
      }

      # HTTP basic auth
      if (!is.null(self$api_client$username) || !is.null(self$api_client$password)) {
        header_params["Authorization"] <- paste("Basic", base64enc::base64encode(charToRaw(paste(self$api_client$username, self$api_client$password, sep = ":"))))
      }

      # The Accept request HTTP header
      local_var_accepts <- list("application/json", "application/xml")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "RootCdashClassField"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' 
    #'
    #' @param domain CDASH Domain Identifier
    #' @param field CDASH Field Identifier
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return RootCdashDomainField
    MdrRootCdashDomainsDomainFieldsFieldGet = function(domain, field, data_file = NULL, ...) {
      local_var_response <- self$MdrRootCdashDomainsDomainFieldsFieldGetWithHttpInfo(domain, field, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' 
    #'
    #' @param domain CDASH Domain Identifier
    #' @param field CDASH Field Identifier
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return API response (RootCdashDomainField) with additional information such as HTTP status code, headers
    MdrRootCdashDomainsDomainFieldsFieldGetWithHttpInfo = function(domain, field, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`domain`)) {
        stop("Missing required parameter `domain`.")
      }

      if (missing(`field`)) {
        stop("Missing required parameter `field`.")
      }

      if (!missing(`domain`) && is.null(`domain`)) {
        stop("Invalid value for `domain` when calling ClinicalDataAcquisitionStandardsHarmonizationCDASHApi$MdrRootCdashDomainsDomainFieldsFieldGet, `domain` is not nullable")
      }

      if (!missing(`field`) && is.null(`field`)) {
        stop("Invalid value for `field` when calling ClinicalDataAcquisitionStandardsHarmonizationCDASHApi$MdrRootCdashDomainsDomainFieldsFieldGet, `field` is not nullable")
      }

      local_var_url_path <- "/mdr/root/cdash/domains/{domain}/fields/{field}"
      if (!missing(`domain`)) {
        local_var_url_path <- gsub("\\{domain\\}", URLencode(as.character(`domain`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`field`)) {
        local_var_url_path <- gsub("\\{field\\}", URLencode(as.character(`field`), reserved = TRUE), local_var_url_path)
      }

      # HTTP basic auth
      if (!is.null(self$api_client$username) || !is.null(self$api_client$password)) {
        header_params["Authorization"] <- paste("Basic", base64enc::base64encode(charToRaw(paste(self$api_client$username, self$api_client$password, sep = ":"))))
      }

      # The Accept request HTTP header
      local_var_accepts <- list("application/json", "application/xml")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "RootCdashDomainField"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    }
  )
)
