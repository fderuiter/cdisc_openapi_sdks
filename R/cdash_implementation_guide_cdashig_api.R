#' CDISC Library API
#'
#' REST API Documentation for CDISC Library in production server
#'
#' The version of the OpenAPI document: 1.1.0
#' Contact: cdisc_library_support@cdisc.org
#' Generated by: https://openapi-generator.tech
#'
#' @docType class
#' @title CDASHImplementationGuideCDASHIG operations
#' @description CDASHImplementationGuideCDASHIGApi
#' @format An \code{R6Class} generator object
#' @field api_client Handles the client-server communication.
#'
#' @examples
#' \dontrun{
#' ####################  MdrCdashigVersionClassesClassDomainsGet  ####################
#'
#' library(openapi)
#' var_version <- "2-1" # character | CDISC Library Product Version
#' var_class <- "Findings" # character | CDASHIG Class Identifier
#'
#' api_instance <- CDASHImplementationGuideCDASHIGApi$new()
#'
#' # Configure HTTP basic authorization: basicAuth
#' api_instance$api_client$username <- Sys.getenv("USERNAME")
#' api_instance$api_client$password <- Sys.getenv("PASSWORD")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$MdrCdashigVersionClassesClassDomainsGet(var_version, var_classdata_file = "result.txt")
#' result <- api_instance$MdrCdashigVersionClassesClassDomainsGet(var_version, var_class)
#' dput(result)
#'
#'
#' ####################  MdrCdashigVersionClassesClassGet  ####################
#'
#' library(openapi)
#' var_version <- "2-1" # character | CDISC Library Product Version
#' var_class <- "Findings" # character | CDASHIG Class Identifier
#'
#' api_instance <- CDASHImplementationGuideCDASHIGApi$new()
#'
#' # Configure HTTP basic authorization: basicAuth
#' api_instance$api_client$username <- Sys.getenv("USERNAME")
#' api_instance$api_client$password <- Sys.getenv("PASSWORD")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$MdrCdashigVersionClassesClassGet(var_version, var_classdata_file = "result.txt")
#' result <- api_instance$MdrCdashigVersionClassesClassGet(var_version, var_class)
#' dput(result)
#'
#'
#' ####################  MdrCdashigVersionClassesClassScenariosGet  ####################
#'
#' library(openapi)
#' var_version <- "2-1" # character | CDISC Library Product Version
#' var_class <- "Findings" # character | CDASHIG Class Identifier
#'
#' api_instance <- CDASHImplementationGuideCDASHIGApi$new()
#'
#' # Configure HTTP basic authorization: basicAuth
#' api_instance$api_client$username <- Sys.getenv("USERNAME")
#' api_instance$api_client$password <- Sys.getenv("PASSWORD")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$MdrCdashigVersionClassesClassScenariosGet(var_version, var_classdata_file = "result.txt")
#' result <- api_instance$MdrCdashigVersionClassesClassScenariosGet(var_version, var_class)
#' dput(result)
#'
#'
#' ####################  MdrCdashigVersionClassesGet  ####################
#'
#' library(openapi)
#' var_version <- "2-1" # character | CDISC Library Product Version
#'
#' api_instance <- CDASHImplementationGuideCDASHIGApi$new()
#'
#' # Configure HTTP basic authorization: basicAuth
#' api_instance$api_client$username <- Sys.getenv("USERNAME")
#' api_instance$api_client$password <- Sys.getenv("PASSWORD")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$MdrCdashigVersionClassesGet(var_versiondata_file = "result.txt")
#' result <- api_instance$MdrCdashigVersionClassesGet(var_version)
#' dput(result)
#'
#'
#' ####################  MdrCdashigVersionDomainsDomainFieldsFieldGet  ####################
#'
#' library(openapi)
#' var_version <- "2-1" # character | CDISC Library Product Version
#' var_domain <- "VS" # character | CDASHIG Domain Identifier
#' var_field <- "VSDAT" # character | CDASHIG Field Identifier
#'
#' api_instance <- CDASHImplementationGuideCDASHIGApi$new()
#'
#' # Configure HTTP basic authorization: basicAuth
#' api_instance$api_client$username <- Sys.getenv("USERNAME")
#' api_instance$api_client$password <- Sys.getenv("PASSWORD")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$MdrCdashigVersionDomainsDomainFieldsFieldGet(var_version, var_domain, var_fielddata_file = "result.txt")
#' result <- api_instance$MdrCdashigVersionDomainsDomainFieldsFieldGet(var_version, var_domain, var_field)
#' dput(result)
#'
#'
#' ####################  MdrCdashigVersionDomainsDomainFieldsGet  ####################
#'
#' library(openapi)
#' var_version <- "2-1" # character | CDISC Library Product Version
#' var_domain <- "VS" # character | CDASHIG Domain Identifier
#'
#' api_instance <- CDASHImplementationGuideCDASHIGApi$new()
#'
#' # Configure HTTP basic authorization: basicAuth
#' api_instance$api_client$username <- Sys.getenv("USERNAME")
#' api_instance$api_client$password <- Sys.getenv("PASSWORD")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$MdrCdashigVersionDomainsDomainFieldsGet(var_version, var_domaindata_file = "result.txt")
#' result <- api_instance$MdrCdashigVersionDomainsDomainFieldsGet(var_version, var_domain)
#' dput(result)
#'
#'
#' ####################  MdrCdashigVersionDomainsDomainGet  ####################
#'
#' library(openapi)
#' var_version <- "2-1" # character | CDISC Library Product Version
#' var_domain <- "VS" # character | CDASHIG Domain Identifier
#'
#' api_instance <- CDASHImplementationGuideCDASHIGApi$new()
#'
#' # Configure HTTP basic authorization: basicAuth
#' api_instance$api_client$username <- Sys.getenv("USERNAME")
#' api_instance$api_client$password <- Sys.getenv("PASSWORD")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$MdrCdashigVersionDomainsDomainGet(var_version, var_domaindata_file = "result.txt")
#' result <- api_instance$MdrCdashigVersionDomainsDomainGet(var_version, var_domain)
#' dput(result)
#'
#'
#' ####################  MdrCdashigVersionDomainsGet  ####################
#'
#' library(openapi)
#' var_version <- "2-1" # character | CDISC Library Product Version
#'
#' api_instance <- CDASHImplementationGuideCDASHIGApi$new()
#'
#' # Configure HTTP basic authorization: basicAuth
#' api_instance$api_client$username <- Sys.getenv("USERNAME")
#' api_instance$api_client$password <- Sys.getenv("PASSWORD")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$MdrCdashigVersionDomainsGet(var_versiondata_file = "result.txt")
#' result <- api_instance$MdrCdashigVersionDomainsGet(var_version)
#' dput(result)
#'
#'
#' ####################  MdrCdashigVersionGet  ####################
#'
#' library(openapi)
#' var_version <- "2-1" # character | CDISC Library Product Version
#'
#' api_instance <- CDASHImplementationGuideCDASHIGApi$new()
#'
#' # Configure HTTP basic authorization: basicAuth
#' api_instance$api_client$username <- Sys.getenv("USERNAME")
#' api_instance$api_client$password <- Sys.getenv("PASSWORD")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$MdrCdashigVersionGet(var_versiondata_file = "result.txt")
#' result <- api_instance$MdrCdashigVersionGet(var_version)
#' dput(result)
#'
#'
#' ####################  MdrCdashigVersionScenariosDomainScenarioFieldsFieldGet  ####################
#'
#' library(openapi)
#' var_version <- "2-1" # character | CDISC Library Product Version
#' var_domain <- "VS" # character | CDASHIG Domain Identifier
#' var_scenario <- "Generic" # character | CDASHIG Scenario Identifier
#' var_field <- "VSDAT" # character | CDASHIG Field Identifier
#'
#' api_instance <- CDASHImplementationGuideCDASHIGApi$new()
#'
#' # Configure HTTP basic authorization: basicAuth
#' api_instance$api_client$username <- Sys.getenv("USERNAME")
#' api_instance$api_client$password <- Sys.getenv("PASSWORD")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$MdrCdashigVersionScenariosDomainScenarioFieldsFieldGet(var_version, var_domain, var_scenario, var_fielddata_file = "result.txt")
#' result <- api_instance$MdrCdashigVersionScenariosDomainScenarioFieldsFieldGet(var_version, var_domain, var_scenario, var_field)
#' dput(result)
#'
#'
#' ####################  MdrCdashigVersionScenariosDomainScenarioFieldsGet  ####################
#'
#' library(openapi)
#' var_version <- "2-1" # character | CDISC Library Product Version
#' var_domain <- "VS" # character | CDASHIG Domain Identifier
#' var_scenario <- "HorizontalGeneric" # character | CDASHIG Scenario Identifier
#'
#' api_instance <- CDASHImplementationGuideCDASHIGApi$new()
#'
#' # Configure HTTP basic authorization: basicAuth
#' api_instance$api_client$username <- Sys.getenv("USERNAME")
#' api_instance$api_client$password <- Sys.getenv("PASSWORD")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$MdrCdashigVersionScenariosDomainScenarioFieldsGet(var_version, var_domain, var_scenariodata_file = "result.txt")
#' result <- api_instance$MdrCdashigVersionScenariosDomainScenarioFieldsGet(var_version, var_domain, var_scenario)
#' dput(result)
#'
#'
#' ####################  MdrCdashigVersionScenariosDomainScenarioGet  ####################
#'
#' library(openapi)
#' var_version <- "2-1" # character | CDISC Library Product Version
#' var_domain <- "VS" # character | CDASHIG Domain Identifier
#' var_scenario <- "HorizontalGeneric" # character | CDASHIG Scenario Identifier
#'
#' api_instance <- CDASHImplementationGuideCDASHIGApi$new()
#'
#' # Configure HTTP basic authorization: basicAuth
#' api_instance$api_client$username <- Sys.getenv("USERNAME")
#' api_instance$api_client$password <- Sys.getenv("PASSWORD")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$MdrCdashigVersionScenariosDomainScenarioGet(var_version, var_domain, var_scenariodata_file = "result.txt")
#' result <- api_instance$MdrCdashigVersionScenariosDomainScenarioGet(var_version, var_domain, var_scenario)
#' dput(result)
#'
#'
#' ####################  MdrCdashigVersionScenariosGet  ####################
#'
#' library(openapi)
#' var_version <- "2-1" # character | CDISC Library Product Version
#'
#' api_instance <- CDASHImplementationGuideCDASHIGApi$new()
#'
#' # Configure HTTP basic authorization: basicAuth
#' api_instance$api_client$username <- Sys.getenv("USERNAME")
#' api_instance$api_client$password <- Sys.getenv("PASSWORD")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$MdrCdashigVersionScenariosGet(var_versiondata_file = "result.txt")
#' result <- api_instance$MdrCdashigVersionScenariosGet(var_version)
#' dput(result)
#'
#'
#' ####################  MdrRootCdashigDomainsDomainFieldsFieldGet  ####################
#'
#' library(openapi)
#' var_domain <- "VS" # character | CDASHIG Domain Identifier
#' var_field <- "VSDAT" # character | CDASHIG Field Identifier
#'
#' api_instance <- CDASHImplementationGuideCDASHIGApi$new()
#'
#' # Configure HTTP basic authorization: basicAuth
#' api_instance$api_client$username <- Sys.getenv("USERNAME")
#' api_instance$api_client$password <- Sys.getenv("PASSWORD")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$MdrRootCdashigDomainsDomainFieldsFieldGet(var_domain, var_fielddata_file = "result.txt")
#' result <- api_instance$MdrRootCdashigDomainsDomainFieldsFieldGet(var_domain, var_field)
#' dput(result)
#'
#'
#' ####################  MdrRootCdashigScenariosDomainScenarioFieldsFieldGet  ####################
#'
#' library(openapi)
#' var_domain <- "VS" # character | CDASHIG Domain Identifier
#' var_scenario <- "Generic" # character | CDASHIG Scenario Identifier
#' var_field <- "VSDAT" # character | CDASHIG Field Identifier
#'
#' api_instance <- CDASHImplementationGuideCDASHIGApi$new()
#'
#' # Configure HTTP basic authorization: basicAuth
#' api_instance$api_client$username <- Sys.getenv("USERNAME")
#' api_instance$api_client$password <- Sys.getenv("PASSWORD")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$MdrRootCdashigScenariosDomainScenarioFieldsFieldGet(var_domain, var_scenario, var_fielddata_file = "result.txt")
#' result <- api_instance$MdrRootCdashigScenariosDomainScenarioFieldsFieldGet(var_domain, var_scenario, var_field)
#' dput(result)
#'
#'
#' }
#' @importFrom R6 R6Class
#' @importFrom base64enc base64encode
#' @export
CDASHImplementationGuideCDASHIGApi <- R6::R6Class(
  "CDASHImplementationGuideCDASHIGApi",
  public = list(
    api_client = NULL,

    #' @description
    #' Initialize a new CDASHImplementationGuideCDASHIGApi.
    #'
    #' @param api_client An instance of API client.
    initialize = function(api_client) {
      if (!missing(api_client)) {
        self$api_client <- api_client
      } else {
        self$api_client <- ApiClient$new()
      }
    },

    #' @description
    #' 
    #'
    #' @param version CDISC Library Product Version
    #' @param class CDASHIG Class Identifier
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return CdashigClassDomains
    MdrCdashigVersionClassesClassDomainsGet = function(version, class, data_file = NULL, ...) {
      local_var_response <- self$MdrCdashigVersionClassesClassDomainsGetWithHttpInfo(version, class, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' 
    #'
    #' @param version CDISC Library Product Version
    #' @param class CDASHIG Class Identifier
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return API response (CdashigClassDomains) with additional information such as HTTP status code, headers
    MdrCdashigVersionClassesClassDomainsGetWithHttpInfo = function(version, class, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`version`)) {
        stop("Missing required parameter `version`.")
      }

      if (missing(`class`)) {
        stop("Missing required parameter `class`.")
      }

      if (!missing(`version`) && is.null(`version`)) {
        stop("Invalid value for `version` when calling CDASHImplementationGuideCDASHIGApi$MdrCdashigVersionClassesClassDomainsGet, `version` is not nullable")
      }

      if (!missing(`class`) && is.null(`class`)) {
        stop("Invalid value for `class` when calling CDASHImplementationGuideCDASHIGApi$MdrCdashigVersionClassesClassDomainsGet, `class` is not nullable")
      }

      local_var_url_path <- "/mdr/cdashig/{version}/classes/{class}/domains"
      if (!missing(`version`)) {
        local_var_url_path <- gsub("\\{version\\}", URLencode(as.character(`version`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`class`)) {
        local_var_url_path <- gsub("\\{class\\}", URLencode(as.character(`class`), reserved = TRUE), local_var_url_path)
      }

      # HTTP basic auth
      if (!is.null(self$api_client$username) || !is.null(self$api_client$password)) {
        header_params["Authorization"] <- paste("Basic", base64enc::base64encode(charToRaw(paste(self$api_client$username, self$api_client$password, sep = ":"))))
      }

      # The Accept request HTTP header
      local_var_accepts <- list("application/json", "application/xml")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "CdashigClassDomains"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' 
    #'
    #' @param version CDISC Library Product Version
    #' @param class CDASHIG Class Identifier
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return CdashigClass
    MdrCdashigVersionClassesClassGet = function(version, class, data_file = NULL, ...) {
      local_var_response <- self$MdrCdashigVersionClassesClassGetWithHttpInfo(version, class, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' 
    #'
    #' @param version CDISC Library Product Version
    #' @param class CDASHIG Class Identifier
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return API response (CdashigClass) with additional information such as HTTP status code, headers
    MdrCdashigVersionClassesClassGetWithHttpInfo = function(version, class, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`version`)) {
        stop("Missing required parameter `version`.")
      }

      if (missing(`class`)) {
        stop("Missing required parameter `class`.")
      }

      if (!missing(`version`) && is.null(`version`)) {
        stop("Invalid value for `version` when calling CDASHImplementationGuideCDASHIGApi$MdrCdashigVersionClassesClassGet, `version` is not nullable")
      }

      if (!missing(`class`) && is.null(`class`)) {
        stop("Invalid value for `class` when calling CDASHImplementationGuideCDASHIGApi$MdrCdashigVersionClassesClassGet, `class` is not nullable")
      }

      local_var_url_path <- "/mdr/cdashig/{version}/classes/{class}"
      if (!missing(`version`)) {
        local_var_url_path <- gsub("\\{version\\}", URLencode(as.character(`version`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`class`)) {
        local_var_url_path <- gsub("\\{class\\}", URLencode(as.character(`class`), reserved = TRUE), local_var_url_path)
      }

      # HTTP basic auth
      if (!is.null(self$api_client$username) || !is.null(self$api_client$password)) {
        header_params["Authorization"] <- paste("Basic", base64enc::base64encode(charToRaw(paste(self$api_client$username, self$api_client$password, sep = ":"))))
      }

      # The Accept request HTTP header
      local_var_accepts <- list("application/json", "application/xml")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "CdashigClass"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' 
    #'
    #' @param version CDISC Library Product Version
    #' @param class CDASHIG Class Identifier
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return CdashigClassScenarios
    MdrCdashigVersionClassesClassScenariosGet = function(version, class, data_file = NULL, ...) {
      local_var_response <- self$MdrCdashigVersionClassesClassScenariosGetWithHttpInfo(version, class, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' 
    #'
    #' @param version CDISC Library Product Version
    #' @param class CDASHIG Class Identifier
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return API response (CdashigClassScenarios) with additional information such as HTTP status code, headers
    MdrCdashigVersionClassesClassScenariosGetWithHttpInfo = function(version, class, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`version`)) {
        stop("Missing required parameter `version`.")
      }

      if (missing(`class`)) {
        stop("Missing required parameter `class`.")
      }

      if (!missing(`version`) && is.null(`version`)) {
        stop("Invalid value for `version` when calling CDASHImplementationGuideCDASHIGApi$MdrCdashigVersionClassesClassScenariosGet, `version` is not nullable")
      }

      if (!missing(`class`) && is.null(`class`)) {
        stop("Invalid value for `class` when calling CDASHImplementationGuideCDASHIGApi$MdrCdashigVersionClassesClassScenariosGet, `class` is not nullable")
      }

      local_var_url_path <- "/mdr/cdashig/{version}/classes/{class}/scenarios"
      if (!missing(`version`)) {
        local_var_url_path <- gsub("\\{version\\}", URLencode(as.character(`version`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`class`)) {
        local_var_url_path <- gsub("\\{class\\}", URLencode(as.character(`class`), reserved = TRUE), local_var_url_path)
      }

      # HTTP basic auth
      if (!is.null(self$api_client$username) || !is.null(self$api_client$password)) {
        header_params["Authorization"] <- paste("Basic", base64enc::base64encode(charToRaw(paste(self$api_client$username, self$api_client$password, sep = ":"))))
      }

      # The Accept request HTTP header
      local_var_accepts <- list("application/json", "application/xml")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "CdashigClassScenarios"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' 
    #'
    #' @param version CDISC Library Product Version
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return CdashigProductClasses
    MdrCdashigVersionClassesGet = function(version, data_file = NULL, ...) {
      local_var_response <- self$MdrCdashigVersionClassesGetWithHttpInfo(version, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' 
    #'
    #' @param version CDISC Library Product Version
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return API response (CdashigProductClasses) with additional information such as HTTP status code, headers
    MdrCdashigVersionClassesGetWithHttpInfo = function(version, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`version`)) {
        stop("Missing required parameter `version`.")
      }

      if (!missing(`version`) && is.null(`version`)) {
        stop("Invalid value for `version` when calling CDASHImplementationGuideCDASHIGApi$MdrCdashigVersionClassesGet, `version` is not nullable")
      }

      local_var_url_path <- "/mdr/cdashig/{version}/classes"
      if (!missing(`version`)) {
        local_var_url_path <- gsub("\\{version\\}", URLencode(as.character(`version`), reserved = TRUE), local_var_url_path)
      }

      # HTTP basic auth
      if (!is.null(self$api_client$username) || !is.null(self$api_client$password)) {
        header_params["Authorization"] <- paste("Basic", base64enc::base64encode(charToRaw(paste(self$api_client$username, self$api_client$password, sep = ":"))))
      }

      # The Accept request HTTP header
      local_var_accepts <- list("application/json", "application/xml")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "CdashigProductClasses"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' 
    #'
    #' @param version CDISC Library Product Version
    #' @param domain CDASHIG Domain Identifier
    #' @param field CDASHIG Field Identifier
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return CdashigDomainField
    MdrCdashigVersionDomainsDomainFieldsFieldGet = function(version, domain, field, data_file = NULL, ...) {
      local_var_response <- self$MdrCdashigVersionDomainsDomainFieldsFieldGetWithHttpInfo(version, domain, field, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' 
    #'
    #' @param version CDISC Library Product Version
    #' @param domain CDASHIG Domain Identifier
    #' @param field CDASHIG Field Identifier
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return API response (CdashigDomainField) with additional information such as HTTP status code, headers
    MdrCdashigVersionDomainsDomainFieldsFieldGetWithHttpInfo = function(version, domain, field, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`version`)) {
        stop("Missing required parameter `version`.")
      }

      if (missing(`domain`)) {
        stop("Missing required parameter `domain`.")
      }

      if (missing(`field`)) {
        stop("Missing required parameter `field`.")
      }

      if (!missing(`version`) && is.null(`version`)) {
        stop("Invalid value for `version` when calling CDASHImplementationGuideCDASHIGApi$MdrCdashigVersionDomainsDomainFieldsFieldGet, `version` is not nullable")
      }

      if (!missing(`domain`) && is.null(`domain`)) {
        stop("Invalid value for `domain` when calling CDASHImplementationGuideCDASHIGApi$MdrCdashigVersionDomainsDomainFieldsFieldGet, `domain` is not nullable")
      }

      if (!missing(`field`) && is.null(`field`)) {
        stop("Invalid value for `field` when calling CDASHImplementationGuideCDASHIGApi$MdrCdashigVersionDomainsDomainFieldsFieldGet, `field` is not nullable")
      }

      local_var_url_path <- "/mdr/cdashig/{version}/domains/{domain}/fields/{field}"
      if (!missing(`version`)) {
        local_var_url_path <- gsub("\\{version\\}", URLencode(as.character(`version`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`domain`)) {
        local_var_url_path <- gsub("\\{domain\\}", URLencode(as.character(`domain`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`field`)) {
        local_var_url_path <- gsub("\\{field\\}", URLencode(as.character(`field`), reserved = TRUE), local_var_url_path)
      }

      # HTTP basic auth
      if (!is.null(self$api_client$username) || !is.null(self$api_client$password)) {
        header_params["Authorization"] <- paste("Basic", base64enc::base64encode(charToRaw(paste(self$api_client$username, self$api_client$password, sep = ":"))))
      }

      # The Accept request HTTP header
      local_var_accepts <- list("application/json", "application/xml")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "CdashigDomainField"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' 
    #'
    #' @param version CDISC Library Product Version
    #' @param domain CDASHIG Domain Identifier
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return CdashigDomainFields
    MdrCdashigVersionDomainsDomainFieldsGet = function(version, domain, data_file = NULL, ...) {
      local_var_response <- self$MdrCdashigVersionDomainsDomainFieldsGetWithHttpInfo(version, domain, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' 
    #'
    #' @param version CDISC Library Product Version
    #' @param domain CDASHIG Domain Identifier
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return API response (CdashigDomainFields) with additional information such as HTTP status code, headers
    MdrCdashigVersionDomainsDomainFieldsGetWithHttpInfo = function(version, domain, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`version`)) {
        stop("Missing required parameter `version`.")
      }

      if (missing(`domain`)) {
        stop("Missing required parameter `domain`.")
      }

      if (!missing(`version`) && is.null(`version`)) {
        stop("Invalid value for `version` when calling CDASHImplementationGuideCDASHIGApi$MdrCdashigVersionDomainsDomainFieldsGet, `version` is not nullable")
      }

      if (!missing(`domain`) && is.null(`domain`)) {
        stop("Invalid value for `domain` when calling CDASHImplementationGuideCDASHIGApi$MdrCdashigVersionDomainsDomainFieldsGet, `domain` is not nullable")
      }

      local_var_url_path <- "/mdr/cdashig/{version}/domains/{domain}/fields"
      if (!missing(`version`)) {
        local_var_url_path <- gsub("\\{version\\}", URLencode(as.character(`version`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`domain`)) {
        local_var_url_path <- gsub("\\{domain\\}", URLencode(as.character(`domain`), reserved = TRUE), local_var_url_path)
      }

      # HTTP basic auth
      if (!is.null(self$api_client$username) || !is.null(self$api_client$password)) {
        header_params["Authorization"] <- paste("Basic", base64enc::base64encode(charToRaw(paste(self$api_client$username, self$api_client$password, sep = ":"))))
      }

      # The Accept request HTTP header
      local_var_accepts <- list("application/json", "application/xml")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "CdashigDomainFields"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' 
    #'
    #' @param version CDISC Library Product Version
    #' @param domain CDASHIG Domain Identifier
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return CdashigDomain
    MdrCdashigVersionDomainsDomainGet = function(version, domain, data_file = NULL, ...) {
      local_var_response <- self$MdrCdashigVersionDomainsDomainGetWithHttpInfo(version, domain, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' 
    #'
    #' @param version CDISC Library Product Version
    #' @param domain CDASHIG Domain Identifier
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return API response (CdashigDomain) with additional information such as HTTP status code, headers
    MdrCdashigVersionDomainsDomainGetWithHttpInfo = function(version, domain, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`version`)) {
        stop("Missing required parameter `version`.")
      }

      if (missing(`domain`)) {
        stop("Missing required parameter `domain`.")
      }

      if (!missing(`version`) && is.null(`version`)) {
        stop("Invalid value for `version` when calling CDASHImplementationGuideCDASHIGApi$MdrCdashigVersionDomainsDomainGet, `version` is not nullable")
      }

      if (!missing(`domain`) && is.null(`domain`)) {
        stop("Invalid value for `domain` when calling CDASHImplementationGuideCDASHIGApi$MdrCdashigVersionDomainsDomainGet, `domain` is not nullable")
      }

      local_var_url_path <- "/mdr/cdashig/{version}/domains/{domain}"
      if (!missing(`version`)) {
        local_var_url_path <- gsub("\\{version\\}", URLencode(as.character(`version`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`domain`)) {
        local_var_url_path <- gsub("\\{domain\\}", URLencode(as.character(`domain`), reserved = TRUE), local_var_url_path)
      }

      # HTTP basic auth
      if (!is.null(self$api_client$username) || !is.null(self$api_client$password)) {
        header_params["Authorization"] <- paste("Basic", base64enc::base64encode(charToRaw(paste(self$api_client$username, self$api_client$password, sep = ":"))))
      }

      # The Accept request HTTP header
      local_var_accepts <- list("application/json", "application/xml")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "CdashigDomain"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' 
    #'
    #' @param version CDISC Library Product Version
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return CdashigProductDomains
    MdrCdashigVersionDomainsGet = function(version, data_file = NULL, ...) {
      local_var_response <- self$MdrCdashigVersionDomainsGetWithHttpInfo(version, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' 
    #'
    #' @param version CDISC Library Product Version
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return API response (CdashigProductDomains) with additional information such as HTTP status code, headers
    MdrCdashigVersionDomainsGetWithHttpInfo = function(version, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`version`)) {
        stop("Missing required parameter `version`.")
      }

      if (!missing(`version`) && is.null(`version`)) {
        stop("Invalid value for `version` when calling CDASHImplementationGuideCDASHIGApi$MdrCdashigVersionDomainsGet, `version` is not nullable")
      }

      local_var_url_path <- "/mdr/cdashig/{version}/domains"
      if (!missing(`version`)) {
        local_var_url_path <- gsub("\\{version\\}", URLencode(as.character(`version`), reserved = TRUE), local_var_url_path)
      }

      # HTTP basic auth
      if (!is.null(self$api_client$username) || !is.null(self$api_client$password)) {
        header_params["Authorization"] <- paste("Basic", base64enc::base64encode(charToRaw(paste(self$api_client$username, self$api_client$password, sep = ":"))))
      }

      # The Accept request HTTP header
      local_var_accepts <- list("application/json", "application/xml")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "CdashigProductDomains"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' 
    #'
    #' @param version CDISC Library Product Version
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return CdashigProduct
    MdrCdashigVersionGet = function(version, data_file = NULL, ...) {
      local_var_response <- self$MdrCdashigVersionGetWithHttpInfo(version, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' 
    #'
    #' @param version CDISC Library Product Version
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return API response (CdashigProduct) with additional information such as HTTP status code, headers
    MdrCdashigVersionGetWithHttpInfo = function(version, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`version`)) {
        stop("Missing required parameter `version`.")
      }

      if (!missing(`version`) && is.null(`version`)) {
        stop("Invalid value for `version` when calling CDASHImplementationGuideCDASHIGApi$MdrCdashigVersionGet, `version` is not nullable")
      }

      local_var_url_path <- "/mdr/cdashig/{version}"
      if (!missing(`version`)) {
        local_var_url_path <- gsub("\\{version\\}", URLencode(as.character(`version`), reserved = TRUE), local_var_url_path)
      }

      # HTTP basic auth
      if (!is.null(self$api_client$username) || !is.null(self$api_client$password)) {
        header_params["Authorization"] <- paste("Basic", base64enc::base64encode(charToRaw(paste(self$api_client$username, self$api_client$password, sep = ":"))))
      }

      # The Accept request HTTP header
      local_var_accepts <- list("application/json", "application/xml", "text/csv", "application/vnd.ms-excel", "application/vnd.openxmlformats-officedocument.spreadsheetml.sheet")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "CdashigProduct"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' 
    #'
    #' @param version CDISC Library Product Version
    #' @param domain CDASHIG Domain Identifier
    #' @param scenario CDASHIG Scenario Identifier
    #' @param field CDASHIG Field Identifier
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return CdashigScenarioField
    MdrCdashigVersionScenariosDomainScenarioFieldsFieldGet = function(version, domain, scenario, field, data_file = NULL, ...) {
      local_var_response <- self$MdrCdashigVersionScenariosDomainScenarioFieldsFieldGetWithHttpInfo(version, domain, scenario, field, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' 
    #'
    #' @param version CDISC Library Product Version
    #' @param domain CDASHIG Domain Identifier
    #' @param scenario CDASHIG Scenario Identifier
    #' @param field CDASHIG Field Identifier
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return API response (CdashigScenarioField) with additional information such as HTTP status code, headers
    MdrCdashigVersionScenariosDomainScenarioFieldsFieldGetWithHttpInfo = function(version, domain, scenario, field, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`version`)) {
        stop("Missing required parameter `version`.")
      }

      if (missing(`domain`)) {
        stop("Missing required parameter `domain`.")
      }

      if (missing(`scenario`)) {
        stop("Missing required parameter `scenario`.")
      }

      if (missing(`field`)) {
        stop("Missing required parameter `field`.")
      }

      if (!missing(`version`) && is.null(`version`)) {
        stop("Invalid value for `version` when calling CDASHImplementationGuideCDASHIGApi$MdrCdashigVersionScenariosDomainScenarioFieldsFieldGet, `version` is not nullable")
      }

      if (!missing(`domain`) && is.null(`domain`)) {
        stop("Invalid value for `domain` when calling CDASHImplementationGuideCDASHIGApi$MdrCdashigVersionScenariosDomainScenarioFieldsFieldGet, `domain` is not nullable")
      }

      if (!missing(`scenario`) && is.null(`scenario`)) {
        stop("Invalid value for `scenario` when calling CDASHImplementationGuideCDASHIGApi$MdrCdashigVersionScenariosDomainScenarioFieldsFieldGet, `scenario` is not nullable")
      }

      if (!missing(`field`) && is.null(`field`)) {
        stop("Invalid value for `field` when calling CDASHImplementationGuideCDASHIGApi$MdrCdashigVersionScenariosDomainScenarioFieldsFieldGet, `field` is not nullable")
      }

      local_var_url_path <- "/mdr/cdashig/{version}/scenarios/{domain}.{scenario}/fields/{field}"
      if (!missing(`version`)) {
        local_var_url_path <- gsub("\\{version\\}", URLencode(as.character(`version`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`domain`)) {
        local_var_url_path <- gsub("\\{domain\\}", URLencode(as.character(`domain`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`scenario`)) {
        local_var_url_path <- gsub("\\{scenario\\}", URLencode(as.character(`scenario`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`field`)) {
        local_var_url_path <- gsub("\\{field\\}", URLencode(as.character(`field`), reserved = TRUE), local_var_url_path)
      }

      # HTTP basic auth
      if (!is.null(self$api_client$username) || !is.null(self$api_client$password)) {
        header_params["Authorization"] <- paste("Basic", base64enc::base64encode(charToRaw(paste(self$api_client$username, self$api_client$password, sep = ":"))))
      }

      # The Accept request HTTP header
      local_var_accepts <- list("application/json", "application/xml")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "CdashigScenarioField"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' 
    #'
    #' @param version CDISC Library Product Version
    #' @param domain CDASHIG Domain Identifier
    #' @param scenario CDASHIG Scenario Identifier
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return CdashigScenarioFields
    MdrCdashigVersionScenariosDomainScenarioFieldsGet = function(version, domain, scenario, data_file = NULL, ...) {
      local_var_response <- self$MdrCdashigVersionScenariosDomainScenarioFieldsGetWithHttpInfo(version, domain, scenario, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' 
    #'
    #' @param version CDISC Library Product Version
    #' @param domain CDASHIG Domain Identifier
    #' @param scenario CDASHIG Scenario Identifier
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return API response (CdashigScenarioFields) with additional information such as HTTP status code, headers
    MdrCdashigVersionScenariosDomainScenarioFieldsGetWithHttpInfo = function(version, domain, scenario, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`version`)) {
        stop("Missing required parameter `version`.")
      }

      if (missing(`domain`)) {
        stop("Missing required parameter `domain`.")
      }

      if (missing(`scenario`)) {
        stop("Missing required parameter `scenario`.")
      }

      if (!missing(`version`) && is.null(`version`)) {
        stop("Invalid value for `version` when calling CDASHImplementationGuideCDASHIGApi$MdrCdashigVersionScenariosDomainScenarioFieldsGet, `version` is not nullable")
      }

      if (!missing(`domain`) && is.null(`domain`)) {
        stop("Invalid value for `domain` when calling CDASHImplementationGuideCDASHIGApi$MdrCdashigVersionScenariosDomainScenarioFieldsGet, `domain` is not nullable")
      }

      if (!missing(`scenario`) && is.null(`scenario`)) {
        stop("Invalid value for `scenario` when calling CDASHImplementationGuideCDASHIGApi$MdrCdashigVersionScenariosDomainScenarioFieldsGet, `scenario` is not nullable")
      }

      local_var_url_path <- "/mdr/cdashig/{version}/scenarios/{domain}.{scenario}/fields"
      if (!missing(`version`)) {
        local_var_url_path <- gsub("\\{version\\}", URLencode(as.character(`version`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`domain`)) {
        local_var_url_path <- gsub("\\{domain\\}", URLencode(as.character(`domain`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`scenario`)) {
        local_var_url_path <- gsub("\\{scenario\\}", URLencode(as.character(`scenario`), reserved = TRUE), local_var_url_path)
      }

      # HTTP basic auth
      if (!is.null(self$api_client$username) || !is.null(self$api_client$password)) {
        header_params["Authorization"] <- paste("Basic", base64enc::base64encode(charToRaw(paste(self$api_client$username, self$api_client$password, sep = ":"))))
      }

      # The Accept request HTTP header
      local_var_accepts <- list("application/json", "application/xml")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "CdashigScenarioFields"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' 
    #'
    #' @param version CDISC Library Product Version
    #' @param domain CDASHIG Domain Identifier
    #' @param scenario CDASHIG Scenario Identifier
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return CdashigScenario
    MdrCdashigVersionScenariosDomainScenarioGet = function(version, domain, scenario, data_file = NULL, ...) {
      local_var_response <- self$MdrCdashigVersionScenariosDomainScenarioGetWithHttpInfo(version, domain, scenario, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' 
    #'
    #' @param version CDISC Library Product Version
    #' @param domain CDASHIG Domain Identifier
    #' @param scenario CDASHIG Scenario Identifier
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return API response (CdashigScenario) with additional information such as HTTP status code, headers
    MdrCdashigVersionScenariosDomainScenarioGetWithHttpInfo = function(version, domain, scenario, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`version`)) {
        stop("Missing required parameter `version`.")
      }

      if (missing(`domain`)) {
        stop("Missing required parameter `domain`.")
      }

      if (missing(`scenario`)) {
        stop("Missing required parameter `scenario`.")
      }

      if (!missing(`version`) && is.null(`version`)) {
        stop("Invalid value for `version` when calling CDASHImplementationGuideCDASHIGApi$MdrCdashigVersionScenariosDomainScenarioGet, `version` is not nullable")
      }

      if (!missing(`domain`) && is.null(`domain`)) {
        stop("Invalid value for `domain` when calling CDASHImplementationGuideCDASHIGApi$MdrCdashigVersionScenariosDomainScenarioGet, `domain` is not nullable")
      }

      if (!missing(`scenario`) && is.null(`scenario`)) {
        stop("Invalid value for `scenario` when calling CDASHImplementationGuideCDASHIGApi$MdrCdashigVersionScenariosDomainScenarioGet, `scenario` is not nullable")
      }

      local_var_url_path <- "/mdr/cdashig/{version}/scenarios/{domain}.{scenario}"
      if (!missing(`version`)) {
        local_var_url_path <- gsub("\\{version\\}", URLencode(as.character(`version`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`domain`)) {
        local_var_url_path <- gsub("\\{domain\\}", URLencode(as.character(`domain`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`scenario`)) {
        local_var_url_path <- gsub("\\{scenario\\}", URLencode(as.character(`scenario`), reserved = TRUE), local_var_url_path)
      }

      # HTTP basic auth
      if (!is.null(self$api_client$username) || !is.null(self$api_client$password)) {
        header_params["Authorization"] <- paste("Basic", base64enc::base64encode(charToRaw(paste(self$api_client$username, self$api_client$password, sep = ":"))))
      }

      # The Accept request HTTP header
      local_var_accepts <- list("application/json", "application/xml")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "CdashigScenario"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' 
    #'
    #' @param version CDISC Library Product Version
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return CdashigProductScenarios
    MdrCdashigVersionScenariosGet = function(version, data_file = NULL, ...) {
      local_var_response <- self$MdrCdashigVersionScenariosGetWithHttpInfo(version, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' 
    #'
    #' @param version CDISC Library Product Version
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return API response (CdashigProductScenarios) with additional information such as HTTP status code, headers
    MdrCdashigVersionScenariosGetWithHttpInfo = function(version, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`version`)) {
        stop("Missing required parameter `version`.")
      }

      if (!missing(`version`) && is.null(`version`)) {
        stop("Invalid value for `version` when calling CDASHImplementationGuideCDASHIGApi$MdrCdashigVersionScenariosGet, `version` is not nullable")
      }

      local_var_url_path <- "/mdr/cdashig/{version}/scenarios"
      if (!missing(`version`)) {
        local_var_url_path <- gsub("\\{version\\}", URLencode(as.character(`version`), reserved = TRUE), local_var_url_path)
      }

      # HTTP basic auth
      if (!is.null(self$api_client$username) || !is.null(self$api_client$password)) {
        header_params["Authorization"] <- paste("Basic", base64enc::base64encode(charToRaw(paste(self$api_client$username, self$api_client$password, sep = ":"))))
      }

      # The Accept request HTTP header
      local_var_accepts <- list("application/json", "application/xml")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "CdashigProductScenarios"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' 
    #'
    #' @param domain CDASHIG Domain Identifier
    #' @param field CDASHIG Field Identifier
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return RootCdashigDomainField
    MdrRootCdashigDomainsDomainFieldsFieldGet = function(domain, field, data_file = NULL, ...) {
      local_var_response <- self$MdrRootCdashigDomainsDomainFieldsFieldGetWithHttpInfo(domain, field, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' 
    #'
    #' @param domain CDASHIG Domain Identifier
    #' @param field CDASHIG Field Identifier
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return API response (RootCdashigDomainField) with additional information such as HTTP status code, headers
    MdrRootCdashigDomainsDomainFieldsFieldGetWithHttpInfo = function(domain, field, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`domain`)) {
        stop("Missing required parameter `domain`.")
      }

      if (missing(`field`)) {
        stop("Missing required parameter `field`.")
      }

      if (!missing(`domain`) && is.null(`domain`)) {
        stop("Invalid value for `domain` when calling CDASHImplementationGuideCDASHIGApi$MdrRootCdashigDomainsDomainFieldsFieldGet, `domain` is not nullable")
      }

      if (!missing(`field`) && is.null(`field`)) {
        stop("Invalid value for `field` when calling CDASHImplementationGuideCDASHIGApi$MdrRootCdashigDomainsDomainFieldsFieldGet, `field` is not nullable")
      }

      local_var_url_path <- "/mdr/root/cdashig/domains/{domain}/fields/{field}"
      if (!missing(`domain`)) {
        local_var_url_path <- gsub("\\{domain\\}", URLencode(as.character(`domain`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`field`)) {
        local_var_url_path <- gsub("\\{field\\}", URLencode(as.character(`field`), reserved = TRUE), local_var_url_path)
      }

      # HTTP basic auth
      if (!is.null(self$api_client$username) || !is.null(self$api_client$password)) {
        header_params["Authorization"] <- paste("Basic", base64enc::base64encode(charToRaw(paste(self$api_client$username, self$api_client$password, sep = ":"))))
      }

      # The Accept request HTTP header
      local_var_accepts <- list("application/json", "application/xml")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "RootCdashigDomainField"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' 
    #'
    #' @param domain CDASHIG Domain Identifier
    #' @param scenario CDASHIG Scenario Identifier
    #' @param field CDASHIG Field Identifier
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return RootCdashigScenarioField
    MdrRootCdashigScenariosDomainScenarioFieldsFieldGet = function(domain, scenario, field, data_file = NULL, ...) {
      local_var_response <- self$MdrRootCdashigScenariosDomainScenarioFieldsFieldGetWithHttpInfo(domain, scenario, field, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' 
    #'
    #' @param domain CDASHIG Domain Identifier
    #' @param scenario CDASHIG Scenario Identifier
    #' @param field CDASHIG Field Identifier
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return API response (RootCdashigScenarioField) with additional information such as HTTP status code, headers
    MdrRootCdashigScenariosDomainScenarioFieldsFieldGetWithHttpInfo = function(domain, scenario, field, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`domain`)) {
        stop("Missing required parameter `domain`.")
      }

      if (missing(`scenario`)) {
        stop("Missing required parameter `scenario`.")
      }

      if (missing(`field`)) {
        stop("Missing required parameter `field`.")
      }

      if (!missing(`domain`) && is.null(`domain`)) {
        stop("Invalid value for `domain` when calling CDASHImplementationGuideCDASHIGApi$MdrRootCdashigScenariosDomainScenarioFieldsFieldGet, `domain` is not nullable")
      }

      if (!missing(`scenario`) && is.null(`scenario`)) {
        stop("Invalid value for `scenario` when calling CDASHImplementationGuideCDASHIGApi$MdrRootCdashigScenariosDomainScenarioFieldsFieldGet, `scenario` is not nullable")
      }

      if (!missing(`field`) && is.null(`field`)) {
        stop("Invalid value for `field` when calling CDASHImplementationGuideCDASHIGApi$MdrRootCdashigScenariosDomainScenarioFieldsFieldGet, `field` is not nullable")
      }

      local_var_url_path <- "/mdr/root/cdashig/scenarios/{domain}.{scenario}/fields/{field}"
      if (!missing(`domain`)) {
        local_var_url_path <- gsub("\\{domain\\}", URLencode(as.character(`domain`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`scenario`)) {
        local_var_url_path <- gsub("\\{scenario\\}", URLencode(as.character(`scenario`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`field`)) {
        local_var_url_path <- gsub("\\{field\\}", URLencode(as.character(`field`), reserved = TRUE), local_var_url_path)
      }

      # HTTP basic auth
      if (!is.null(self$api_client$username) || !is.null(self$api_client$password)) {
        header_params["Authorization"] <- paste("Basic", base64enc::base64encode(charToRaw(paste(self$api_client$username, self$api_client$password, sep = ":"))))
      }

      # The Accept request HTTP header
      local_var_accepts <- list("application/json", "application/xml")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "RootCdashigScenarioField"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    }
  )
)
