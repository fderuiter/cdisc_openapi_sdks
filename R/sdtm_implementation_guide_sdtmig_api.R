#' CDISC Library API
#'
#' REST API Documentation for CDISC Library in production server
#'
#' The version of the OpenAPI document: 1.1.0
#' Contact: cdisc_library_support@cdisc.org
#' Generated by: https://openapi-generator.tech
#'
#' @docType class
#' @title SDTMImplementationGuideSDTMIG operations
#' @description SDTMImplementationGuideSDTMIGApi
#' @format An \code{R6Class} generator object
#' @field api_client Handles the client-server communication.
#'
#' @examples
#' \dontrun{
#' ####################  MdrRootSdtmigDatasetsDatasetVariablesVarGet  ####################
#'
#' library(openapi)
#' var_dataset <- "DM" # character | SDTMIG Dataset Identifier
#' var_var <- "SUBJID" # character | SDTMIG Variable Identifier
#'
#' api_instance <- SDTMImplementationGuideSDTMIGApi$new()
#'
#' # Configure HTTP basic authorization: basicAuth
#' api_instance$api_client$username <- Sys.getenv("USERNAME")
#' api_instance$api_client$password <- Sys.getenv("PASSWORD")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$MdrRootSdtmigDatasetsDatasetVariablesVarGet(var_dataset, var_vardata_file = "result.txt")
#' result <- api_instance$MdrRootSdtmigDatasetsDatasetVariablesVarGet(var_dataset, var_var)
#' dput(result)
#'
#'
#' ####################  MdrSdtmigVersionClassesClassDatasetsGet  ####################
#'
#' library(openapi)
#' var_version <- "3-3" # character | CDISC Library Product Version
#' var_class <- "Findings" # character | SDTMIG Class Identifier
#'
#' api_instance <- SDTMImplementationGuideSDTMIGApi$new()
#'
#' # Configure HTTP basic authorization: basicAuth
#' api_instance$api_client$username <- Sys.getenv("USERNAME")
#' api_instance$api_client$password <- Sys.getenv("PASSWORD")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$MdrSdtmigVersionClassesClassDatasetsGet(var_version, var_classdata_file = "result.txt")
#' result <- api_instance$MdrSdtmigVersionClassesClassDatasetsGet(var_version, var_class)
#' dput(result)
#'
#'
#' ####################  MdrSdtmigVersionClassesClassGet  ####################
#'
#' library(openapi)
#' var_version <- "3-3" # character | CDISC Library Product Version
#' var_class <- "Findings" # character | SDTMIG Class Identifier
#'
#' api_instance <- SDTMImplementationGuideSDTMIGApi$new()
#'
#' # Configure HTTP basic authorization: basicAuth
#' api_instance$api_client$username <- Sys.getenv("USERNAME")
#' api_instance$api_client$password <- Sys.getenv("PASSWORD")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$MdrSdtmigVersionClassesClassGet(var_version, var_classdata_file = "result.txt")
#' result <- api_instance$MdrSdtmigVersionClassesClassGet(var_version, var_class)
#' dput(result)
#'
#'
#' ####################  MdrSdtmigVersionClassesGet  ####################
#'
#' library(openapi)
#' var_version <- "3-3" # character | CDISC Library Product Version
#'
#' api_instance <- SDTMImplementationGuideSDTMIGApi$new()
#'
#' # Configure HTTP basic authorization: basicAuth
#' api_instance$api_client$username <- Sys.getenv("USERNAME")
#' api_instance$api_client$password <- Sys.getenv("PASSWORD")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$MdrSdtmigVersionClassesGet(var_versiondata_file = "result.txt")
#' result <- api_instance$MdrSdtmigVersionClassesGet(var_version)
#' dput(result)
#'
#'
#' ####################  MdrSdtmigVersionDatasetsDatasetGet  ####################
#'
#' library(openapi)
#' var_version <- "3-3" # character | CDISC Library Product Version
#' var_dataset <- "LB" # character | SDTMIG Dataset Identifier
#'
#' api_instance <- SDTMImplementationGuideSDTMIGApi$new()
#'
#' # Configure HTTP basic authorization: basicAuth
#' api_instance$api_client$username <- Sys.getenv("USERNAME")
#' api_instance$api_client$password <- Sys.getenv("PASSWORD")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$MdrSdtmigVersionDatasetsDatasetGet(var_version, var_datasetdata_file = "result.txt")
#' result <- api_instance$MdrSdtmigVersionDatasetsDatasetGet(var_version, var_dataset)
#' dput(result)
#'
#'
#' ####################  MdrSdtmigVersionDatasetsDatasetVariablesGet  ####################
#'
#' library(openapi)
#' var_version <- "3-3" # character | CDISC Library Product Version
#' var_dataset <- "LB" # character | SDTMIG Dataset Identifier
#'
#' api_instance <- SDTMImplementationGuideSDTMIGApi$new()
#'
#' # Configure HTTP basic authorization: basicAuth
#' api_instance$api_client$username <- Sys.getenv("USERNAME")
#' api_instance$api_client$password <- Sys.getenv("PASSWORD")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$MdrSdtmigVersionDatasetsDatasetVariablesGet(var_version, var_datasetdata_file = "result.txt")
#' result <- api_instance$MdrSdtmigVersionDatasetsDatasetVariablesGet(var_version, var_dataset)
#' dput(result)
#'
#'
#' ####################  MdrSdtmigVersionDatasetsDatasetVariablesVarGet  ####################
#'
#' library(openapi)
#' var_version <- "3-3" # character | CDISC Library Product Version
#' var_dataset <- "LB" # character | SDTMIG Dataset Identifier
#' var_var <- "LBDTC" # character | SDTMIG Variable Identifier
#'
#' api_instance <- SDTMImplementationGuideSDTMIGApi$new()
#'
#' # Configure HTTP basic authorization: basicAuth
#' api_instance$api_client$username <- Sys.getenv("USERNAME")
#' api_instance$api_client$password <- Sys.getenv("PASSWORD")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$MdrSdtmigVersionDatasetsDatasetVariablesVarGet(var_version, var_dataset, var_vardata_file = "result.txt")
#' result <- api_instance$MdrSdtmigVersionDatasetsDatasetVariablesVarGet(var_version, var_dataset, var_var)
#' dput(result)
#'
#'
#' ####################  MdrSdtmigVersionDatasetsGet  ####################
#'
#' library(openapi)
#' var_version <- "3-3" # character | CDISC Library Product Version
#'
#' api_instance <- SDTMImplementationGuideSDTMIGApi$new()
#'
#' # Configure HTTP basic authorization: basicAuth
#' api_instance$api_client$username <- Sys.getenv("USERNAME")
#' api_instance$api_client$password <- Sys.getenv("PASSWORD")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$MdrSdtmigVersionDatasetsGet(var_versiondata_file = "result.txt")
#' result <- api_instance$MdrSdtmigVersionDatasetsGet(var_version)
#' dput(result)
#'
#'
#' ####################  MdrSdtmigVersionGet  ####################
#'
#' library(openapi)
#' var_version <- "3-3" # character | CDISC Library Product Version
#'
#' api_instance <- SDTMImplementationGuideSDTMIGApi$new()
#'
#' # Configure HTTP basic authorization: basicAuth
#' api_instance$api_client$username <- Sys.getenv("USERNAME")
#' api_instance$api_client$password <- Sys.getenv("PASSWORD")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$MdrSdtmigVersionGet(var_versiondata_file = "result.txt")
#' result <- api_instance$MdrSdtmigVersionGet(var_version)
#' dput(result)
#'
#'
#' }
#' @importFrom R6 R6Class
#' @importFrom base64enc base64encode
#' @export
SDTMImplementationGuideSDTMIGApi <- R6::R6Class(
  "SDTMImplementationGuideSDTMIGApi",
  public = list(
    api_client = NULL,

    #' @description
    #' Initialize a new SDTMImplementationGuideSDTMIGApi.
    #'
    #' @param api_client An instance of API client.
    initialize = function(api_client) {
      if (!missing(api_client)) {
        self$api_client <- api_client
      } else {
        self$api_client <- ApiClient$new()
      }
    },

    #' @description
    #' 
    #'
    #' @param dataset SDTMIG Dataset Identifier
    #' @param var SDTMIG Variable Identifier
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return RootSdtmigDatasetVariable
    MdrRootSdtmigDatasetsDatasetVariablesVarGet = function(dataset, var, data_file = NULL, ...) {
      local_var_response <- self$MdrRootSdtmigDatasetsDatasetVariablesVarGetWithHttpInfo(dataset, var, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' 
    #'
    #' @param dataset SDTMIG Dataset Identifier
    #' @param var SDTMIG Variable Identifier
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return API response (RootSdtmigDatasetVariable) with additional information such as HTTP status code, headers
    MdrRootSdtmigDatasetsDatasetVariablesVarGetWithHttpInfo = function(dataset, var, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`dataset`)) {
        stop("Missing required parameter `dataset`.")
      }

      if (missing(`var`)) {
        stop("Missing required parameter `var`.")
      }

      if (!missing(`dataset`) && is.null(`dataset`)) {
        stop("Invalid value for `dataset` when calling SDTMImplementationGuideSDTMIGApi$MdrRootSdtmigDatasetsDatasetVariablesVarGet, `dataset` is not nullable")
      }

      if (!missing(`var`) && is.null(`var`)) {
        stop("Invalid value for `var` when calling SDTMImplementationGuideSDTMIGApi$MdrRootSdtmigDatasetsDatasetVariablesVarGet, `var` is not nullable")
      }

      local_var_url_path <- "/mdr/root/sdtmig/datasets/{dataset}/variables/{var}"
      if (!missing(`dataset`)) {
        local_var_url_path <- gsub("\\{dataset\\}", URLencode(as.character(`dataset`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`var`)) {
        local_var_url_path <- gsub("\\{var\\}", URLencode(as.character(`var`), reserved = TRUE), local_var_url_path)
      }

      # HTTP basic auth
      if (!is.null(self$api_client$username) || !is.null(self$api_client$password)) {
        header_params["Authorization"] <- paste("Basic", base64enc::base64encode(charToRaw(paste(self$api_client$username, self$api_client$password, sep = ":"))))
      }

      # The Accept request HTTP header
      local_var_accepts <- list("application/json", "application/xml")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "RootSdtmigDatasetVariable"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' 
    #'
    #' @param version CDISC Library Product Version
    #' @param class SDTMIG Class Identifier
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return SdtmigClassDatasets
    MdrSdtmigVersionClassesClassDatasetsGet = function(version, class, data_file = NULL, ...) {
      local_var_response <- self$MdrSdtmigVersionClassesClassDatasetsGetWithHttpInfo(version, class, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' 
    #'
    #' @param version CDISC Library Product Version
    #' @param class SDTMIG Class Identifier
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return API response (SdtmigClassDatasets) with additional information such as HTTP status code, headers
    MdrSdtmigVersionClassesClassDatasetsGetWithHttpInfo = function(version, class, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`version`)) {
        stop("Missing required parameter `version`.")
      }

      if (missing(`class`)) {
        stop("Missing required parameter `class`.")
      }

      if (!missing(`version`) && is.null(`version`)) {
        stop("Invalid value for `version` when calling SDTMImplementationGuideSDTMIGApi$MdrSdtmigVersionClassesClassDatasetsGet, `version` is not nullable")
      }

      if (!missing(`class`) && is.null(`class`)) {
        stop("Invalid value for `class` when calling SDTMImplementationGuideSDTMIGApi$MdrSdtmigVersionClassesClassDatasetsGet, `class` is not nullable")
      }

      local_var_url_path <- "/mdr/sdtmig/{version}/classes/{class}/datasets"
      if (!missing(`version`)) {
        local_var_url_path <- gsub("\\{version\\}", URLencode(as.character(`version`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`class`)) {
        local_var_url_path <- gsub("\\{class\\}", URLencode(as.character(`class`), reserved = TRUE), local_var_url_path)
      }

      # HTTP basic auth
      if (!is.null(self$api_client$username) || !is.null(self$api_client$password)) {
        header_params["Authorization"] <- paste("Basic", base64enc::base64encode(charToRaw(paste(self$api_client$username, self$api_client$password, sep = ":"))))
      }

      # The Accept request HTTP header
      local_var_accepts <- list("application/json", "application/xml")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "SdtmigClassDatasets"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' 
    #'
    #' @param version CDISC Library Product Version
    #' @param class SDTMIG Class Identifier
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return SdtmigClass
    MdrSdtmigVersionClassesClassGet = function(version, class, data_file = NULL, ...) {
      local_var_response <- self$MdrSdtmigVersionClassesClassGetWithHttpInfo(version, class, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' 
    #'
    #' @param version CDISC Library Product Version
    #' @param class SDTMIG Class Identifier
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return API response (SdtmigClass) with additional information such as HTTP status code, headers
    MdrSdtmigVersionClassesClassGetWithHttpInfo = function(version, class, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`version`)) {
        stop("Missing required parameter `version`.")
      }

      if (missing(`class`)) {
        stop("Missing required parameter `class`.")
      }

      if (!missing(`version`) && is.null(`version`)) {
        stop("Invalid value for `version` when calling SDTMImplementationGuideSDTMIGApi$MdrSdtmigVersionClassesClassGet, `version` is not nullable")
      }

      if (!missing(`class`) && is.null(`class`)) {
        stop("Invalid value for `class` when calling SDTMImplementationGuideSDTMIGApi$MdrSdtmigVersionClassesClassGet, `class` is not nullable")
      }

      local_var_url_path <- "/mdr/sdtmig/{version}/classes/{class}"
      if (!missing(`version`)) {
        local_var_url_path <- gsub("\\{version\\}", URLencode(as.character(`version`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`class`)) {
        local_var_url_path <- gsub("\\{class\\}", URLencode(as.character(`class`), reserved = TRUE), local_var_url_path)
      }

      # HTTP basic auth
      if (!is.null(self$api_client$username) || !is.null(self$api_client$password)) {
        header_params["Authorization"] <- paste("Basic", base64enc::base64encode(charToRaw(paste(self$api_client$username, self$api_client$password, sep = ":"))))
      }

      # The Accept request HTTP header
      local_var_accepts <- list("application/json", "application/xml")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "SdtmigClass"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' 
    #'
    #' @param version CDISC Library Product Version
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return SdtmigClasses
    MdrSdtmigVersionClassesGet = function(version, data_file = NULL, ...) {
      local_var_response <- self$MdrSdtmigVersionClassesGetWithHttpInfo(version, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' 
    #'
    #' @param version CDISC Library Product Version
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return API response (SdtmigClasses) with additional information such as HTTP status code, headers
    MdrSdtmigVersionClassesGetWithHttpInfo = function(version, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`version`)) {
        stop("Missing required parameter `version`.")
      }

      if (!missing(`version`) && is.null(`version`)) {
        stop("Invalid value for `version` when calling SDTMImplementationGuideSDTMIGApi$MdrSdtmigVersionClassesGet, `version` is not nullable")
      }

      local_var_url_path <- "/mdr/sdtmig/{version}/classes"
      if (!missing(`version`)) {
        local_var_url_path <- gsub("\\{version\\}", URLencode(as.character(`version`), reserved = TRUE), local_var_url_path)
      }

      # HTTP basic auth
      if (!is.null(self$api_client$username) || !is.null(self$api_client$password)) {
        header_params["Authorization"] <- paste("Basic", base64enc::base64encode(charToRaw(paste(self$api_client$username, self$api_client$password, sep = ":"))))
      }

      # The Accept request HTTP header
      local_var_accepts <- list("application/json", "application/xml")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "SdtmigClasses"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' 
    #'
    #' @param version CDISC Library Product Version
    #' @param dataset SDTMIG Dataset Identifier
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return SdtmigDataset
    MdrSdtmigVersionDatasetsDatasetGet = function(version, dataset, data_file = NULL, ...) {
      local_var_response <- self$MdrSdtmigVersionDatasetsDatasetGetWithHttpInfo(version, dataset, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' 
    #'
    #' @param version CDISC Library Product Version
    #' @param dataset SDTMIG Dataset Identifier
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return API response (SdtmigDataset) with additional information such as HTTP status code, headers
    MdrSdtmigVersionDatasetsDatasetGetWithHttpInfo = function(version, dataset, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`version`)) {
        stop("Missing required parameter `version`.")
      }

      if (missing(`dataset`)) {
        stop("Missing required parameter `dataset`.")
      }

      if (!missing(`version`) && is.null(`version`)) {
        stop("Invalid value for `version` when calling SDTMImplementationGuideSDTMIGApi$MdrSdtmigVersionDatasetsDatasetGet, `version` is not nullable")
      }

      if (!missing(`dataset`) && is.null(`dataset`)) {
        stop("Invalid value for `dataset` when calling SDTMImplementationGuideSDTMIGApi$MdrSdtmigVersionDatasetsDatasetGet, `dataset` is not nullable")
      }

      local_var_url_path <- "/mdr/sdtmig/{version}/datasets/{dataset}"
      if (!missing(`version`)) {
        local_var_url_path <- gsub("\\{version\\}", URLencode(as.character(`version`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`dataset`)) {
        local_var_url_path <- gsub("\\{dataset\\}", URLencode(as.character(`dataset`), reserved = TRUE), local_var_url_path)
      }

      # HTTP basic auth
      if (!is.null(self$api_client$username) || !is.null(self$api_client$password)) {
        header_params["Authorization"] <- paste("Basic", base64enc::base64encode(charToRaw(paste(self$api_client$username, self$api_client$password, sep = ":"))))
      }

      # The Accept request HTTP header
      local_var_accepts <- list("application/json", "application/xml")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "SdtmigDataset"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' 
    #'
    #' @param version CDISC Library Product Version
    #' @param dataset SDTMIG Dataset Identifier
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return SdtmigDatasetVariables
    MdrSdtmigVersionDatasetsDatasetVariablesGet = function(version, dataset, data_file = NULL, ...) {
      local_var_response <- self$MdrSdtmigVersionDatasetsDatasetVariablesGetWithHttpInfo(version, dataset, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' 
    #'
    #' @param version CDISC Library Product Version
    #' @param dataset SDTMIG Dataset Identifier
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return API response (SdtmigDatasetVariables) with additional information such as HTTP status code, headers
    MdrSdtmigVersionDatasetsDatasetVariablesGetWithHttpInfo = function(version, dataset, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`version`)) {
        stop("Missing required parameter `version`.")
      }

      if (missing(`dataset`)) {
        stop("Missing required parameter `dataset`.")
      }

      if (!missing(`version`) && is.null(`version`)) {
        stop("Invalid value for `version` when calling SDTMImplementationGuideSDTMIGApi$MdrSdtmigVersionDatasetsDatasetVariablesGet, `version` is not nullable")
      }

      if (!missing(`dataset`) && is.null(`dataset`)) {
        stop("Invalid value for `dataset` when calling SDTMImplementationGuideSDTMIGApi$MdrSdtmigVersionDatasetsDatasetVariablesGet, `dataset` is not nullable")
      }

      local_var_url_path <- "/mdr/sdtmig/{version}/datasets/{dataset}/variables"
      if (!missing(`version`)) {
        local_var_url_path <- gsub("\\{version\\}", URLencode(as.character(`version`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`dataset`)) {
        local_var_url_path <- gsub("\\{dataset\\}", URLencode(as.character(`dataset`), reserved = TRUE), local_var_url_path)
      }

      # HTTP basic auth
      if (!is.null(self$api_client$username) || !is.null(self$api_client$password)) {
        header_params["Authorization"] <- paste("Basic", base64enc::base64encode(charToRaw(paste(self$api_client$username, self$api_client$password, sep = ":"))))
      }

      # The Accept request HTTP header
      local_var_accepts <- list("application/json", "application/xml")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "SdtmigDatasetVariables"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' 
    #'
    #' @param version CDISC Library Product Version
    #' @param dataset SDTMIG Dataset Identifier
    #' @param var SDTMIG Variable Identifier
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return SdtmigDatasetVariable
    MdrSdtmigVersionDatasetsDatasetVariablesVarGet = function(version, dataset, var, data_file = NULL, ...) {
      local_var_response <- self$MdrSdtmigVersionDatasetsDatasetVariablesVarGetWithHttpInfo(version, dataset, var, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' 
    #'
    #' @param version CDISC Library Product Version
    #' @param dataset SDTMIG Dataset Identifier
    #' @param var SDTMIG Variable Identifier
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return API response (SdtmigDatasetVariable) with additional information such as HTTP status code, headers
    MdrSdtmigVersionDatasetsDatasetVariablesVarGetWithHttpInfo = function(version, dataset, var, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`version`)) {
        stop("Missing required parameter `version`.")
      }

      if (missing(`dataset`)) {
        stop("Missing required parameter `dataset`.")
      }

      if (missing(`var`)) {
        stop("Missing required parameter `var`.")
      }

      if (!missing(`version`) && is.null(`version`)) {
        stop("Invalid value for `version` when calling SDTMImplementationGuideSDTMIGApi$MdrSdtmigVersionDatasetsDatasetVariablesVarGet, `version` is not nullable")
      }

      if (!missing(`dataset`) && is.null(`dataset`)) {
        stop("Invalid value for `dataset` when calling SDTMImplementationGuideSDTMIGApi$MdrSdtmigVersionDatasetsDatasetVariablesVarGet, `dataset` is not nullable")
      }

      if (!missing(`var`) && is.null(`var`)) {
        stop("Invalid value for `var` when calling SDTMImplementationGuideSDTMIGApi$MdrSdtmigVersionDatasetsDatasetVariablesVarGet, `var` is not nullable")
      }

      local_var_url_path <- "/mdr/sdtmig/{version}/datasets/{dataset}/variables/{var}"
      if (!missing(`version`)) {
        local_var_url_path <- gsub("\\{version\\}", URLencode(as.character(`version`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`dataset`)) {
        local_var_url_path <- gsub("\\{dataset\\}", URLencode(as.character(`dataset`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`var`)) {
        local_var_url_path <- gsub("\\{var\\}", URLencode(as.character(`var`), reserved = TRUE), local_var_url_path)
      }

      # HTTP basic auth
      if (!is.null(self$api_client$username) || !is.null(self$api_client$password)) {
        header_params["Authorization"] <- paste("Basic", base64enc::base64encode(charToRaw(paste(self$api_client$username, self$api_client$password, sep = ":"))))
      }

      # The Accept request HTTP header
      local_var_accepts <- list("application/json", "application/xml")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "SdtmigDatasetVariable"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' 
    #'
    #' @param version CDISC Library Product Version
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return SdtmigDatasets
    MdrSdtmigVersionDatasetsGet = function(version, data_file = NULL, ...) {
      local_var_response <- self$MdrSdtmigVersionDatasetsGetWithHttpInfo(version, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' 
    #'
    #' @param version CDISC Library Product Version
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return API response (SdtmigDatasets) with additional information such as HTTP status code, headers
    MdrSdtmigVersionDatasetsGetWithHttpInfo = function(version, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`version`)) {
        stop("Missing required parameter `version`.")
      }

      if (!missing(`version`) && is.null(`version`)) {
        stop("Invalid value for `version` when calling SDTMImplementationGuideSDTMIGApi$MdrSdtmigVersionDatasetsGet, `version` is not nullable")
      }

      local_var_url_path <- "/mdr/sdtmig/{version}/datasets"
      if (!missing(`version`)) {
        local_var_url_path <- gsub("\\{version\\}", URLencode(as.character(`version`), reserved = TRUE), local_var_url_path)
      }

      # HTTP basic auth
      if (!is.null(self$api_client$username) || !is.null(self$api_client$password)) {
        header_params["Authorization"] <- paste("Basic", base64enc::base64encode(charToRaw(paste(self$api_client$username, self$api_client$password, sep = ":"))))
      }

      # The Accept request HTTP header
      local_var_accepts <- list("application/json", "application/xml", "text/csv")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "SdtmigDatasets"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' 
    #'
    #' @param version CDISC Library Product Version
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return SdtmigProduct
    MdrSdtmigVersionGet = function(version, data_file = NULL, ...) {
      local_var_response <- self$MdrSdtmigVersionGetWithHttpInfo(version, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' 
    #'
    #' @param version CDISC Library Product Version
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return API response (SdtmigProduct) with additional information such as HTTP status code, headers
    MdrSdtmigVersionGetWithHttpInfo = function(version, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`version`)) {
        stop("Missing required parameter `version`.")
      }

      if (!missing(`version`) && is.null(`version`)) {
        stop("Invalid value for `version` when calling SDTMImplementationGuideSDTMIGApi$MdrSdtmigVersionGet, `version` is not nullable")
      }

      local_var_url_path <- "/mdr/sdtmig/{version}"
      if (!missing(`version`)) {
        local_var_url_path <- gsub("\\{version\\}", URLencode(as.character(`version`), reserved = TRUE), local_var_url_path)
      }

      # HTTP basic auth
      if (!is.null(self$api_client$username) || !is.null(self$api_client$password)) {
        header_params["Authorization"] <- paste("Basic", base64enc::base64encode(charToRaw(paste(self$api_client$username, self$api_client$password, sep = ":"))))
      }

      # The Accept request HTTP header
      local_var_accepts <- list("application/json", "application/xml", "text/csv", "application/vnd.ms-excel", "application/vnd.openxmlformats-officedocument.spreadsheetml.sheet")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "SdtmigProduct"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    }
  )
)
