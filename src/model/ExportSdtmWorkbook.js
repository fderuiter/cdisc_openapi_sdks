/**
 * CDISC Library API
 * REST API Documentation for CDISC Library in production server
 *
 * The version of the OpenAPI document: 1.1.0
 * Contact: cdisc_library_support@cdisc.org
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import ExportSdtmClassVariablesRow from './ExportSdtmClassVariablesRow';
import ExportSdtmDatasetVariablesRow from './ExportSdtmDatasetVariablesRow';
import ExportSdtmDatasetsRow from './ExportSdtmDatasetsRow';

/**
 * The ExportSdtmWorkbook model module.
 * @module model/ExportSdtmWorkbook
 * @version 1.1.0
 */
class ExportSdtmWorkbook {
    /**
     * Constructs a new <code>ExportSdtmWorkbook</code>.
     * @alias module:model/ExportSdtmWorkbook
     */
    constructor() { 
        
        ExportSdtmWorkbook.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
    }

    /**
     * Constructs a <code>ExportSdtmWorkbook</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/ExportSdtmWorkbook} obj Optional instance to populate.
     * @return {module:model/ExportSdtmWorkbook} The populated <code>ExportSdtmWorkbook</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new ExportSdtmWorkbook();

            if (data.hasOwnProperty('class-variables')) {
                obj['class-variables'] = ApiClient.convertToType(data['class-variables'], [ExportSdtmClassVariablesRow]);
            }
            if (data.hasOwnProperty('dataset-variables')) {
                obj['dataset-variables'] = ApiClient.convertToType(data['dataset-variables'], [ExportSdtmDatasetVariablesRow]);
            }
            if (data.hasOwnProperty('datasets')) {
                obj['datasets'] = ApiClient.convertToType(data['datasets'], [ExportSdtmDatasetsRow]);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>ExportSdtmWorkbook</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>ExportSdtmWorkbook</code>.
     */
    static validateJSON(data) {
        if (data['class-variables']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['class-variables'])) {
                throw new Error("Expected the field `class-variables` to be an array in the JSON data but got " + data['class-variables']);
            }
            // validate the optional field `class-variables` (array)
            for (const item of data['class-variables']) {
                ExportSdtmClassVariablesRow.validateJSON(item);
            };
        }
        if (data['dataset-variables']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['dataset-variables'])) {
                throw new Error("Expected the field `dataset-variables` to be an array in the JSON data but got " + data['dataset-variables']);
            }
            // validate the optional field `dataset-variables` (array)
            for (const item of data['dataset-variables']) {
                ExportSdtmDatasetVariablesRow.validateJSON(item);
            };
        }
        if (data['datasets']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['datasets'])) {
                throw new Error("Expected the field `datasets` to be an array in the JSON data but got " + data['datasets']);
            }
            // validate the optional field `datasets` (array)
            for (const item of data['datasets']) {
                ExportSdtmDatasetsRow.validateJSON(item);
            };
        }

        return true;
    }


}



/**
 * @member {Array.<module:model/ExportSdtmClassVariablesRow>} class-variables
 */
ExportSdtmWorkbook.prototype['class-variables'] = undefined;

/**
 * @member {Array.<module:model/ExportSdtmDatasetVariablesRow>} dataset-variables
 */
ExportSdtmWorkbook.prototype['dataset-variables'] = undefined;

/**
 * @member {Array.<module:model/ExportSdtmDatasetsRow>} datasets
 */
ExportSdtmWorkbook.prototype['datasets'] = undefined;






export default ExportSdtmWorkbook;

