#' CDISC Library API
#'
#' REST API Documentation for CDISC Library in production server
#'
#' The version of the OpenAPI document: 1.1.0
#' Contact: cdisc_library_support@cdisc.org
#' Generated by: https://openapi-generator.tech
#'
#' @docType class
#' @title Searches operations
#' @description SearchesApi
#' @format An \code{R6Class} generator object
#' @field api_client Handles the client-server communication.
#'
#' @examples
#' \dontrun{
#' ####################  MdrSearchGet  ####################
#'
#' library(openapi)
#' var_q <- "q_example" # character | Search Term Expression
#' var_highlight <- "highlight_example" # character | Search Highlight Expression (Optional)
#' var_start <- 0 # numeric | Initial Search Result Index (Optional)
#' var_page_size <- 100 # numeric | Number of Search Results to Return (Optional)
#' var_class <- "class_example" # character | Limit Search by Setting Scope to Class (Optional)
#' var_codelist <- "codelist_example" # character | Limit Search by Setting Scope to Codelist (Optional)
#' var_concept_id <- "concept_id_example" # character | Limit Search by Setting Scope to Concept ID (Optional)
#' var_core <- "core_example" # character | Limit Search by Setting Scope to Core (Optional)
#' var_data_structure <- "data_structure_example" # character | Limit Search by Setting Scope to Data Structure (Optional)
#' var_dataset_structure <- "dataset_structure_example" # character | Limit Search by Setting Scope to Dataset Structure (Optional)
#' var_definition <- "definition_example" # character | Limit Search by Setting Scope to Definition (Optional)
#' var_description <- "description_example" # character | Limit Search by Setting Scope to Description (Optional)
#' var_domain <- "domain_example" # character | Limit Search by Setting Scope to Domain (Optional)
#' var_effective_date <- "effective_date_example" # character | Limit Search by Setting Scope to Effective Date (Optional)
#' var_extensible <- "extensible_example" # character | Limit Search by Setting Scope to Extensible (Optional)
#' var_href <- "href_example" # character | Limit Search by Setting Scope to Href (Optional)
#' var_label <- "label_example" # character | Limit Search by Setting Scope to Label (Optional)
#' var_measure_type <- "measure_type_example" # character | SLimit Search by Setting Scope to Measure Type (Optional)
#' var_name <- "name_example" # character | Limit Search by Setting Scope to Name (Optional)
#' var_preferred_term <- "preferred_term_example" # character | Limit Search by Setting Scope to Preferred Term (Optional)
#' var_product <- "product_example" # character | Limit Search by Setting Scope to Product (Optional)
#' var_product_group <- "product_group_example" # character | Limit Search by Setting Scope to Product Group (Optional)
#' var_registration_status <- "registration_status_example" # character | Limit Search by Setting Scope to Registration Status (Optional)
#' var_role_description <- "role_description_example" # character | Limit Search by Setting Scope to Role Description (Optional)
#' var_sdtm_target <- "sdtm_target_example" # character | Limit Search by Setting Scope to SDTM Target (Optional)
#' var_simple_datatype <- "simple_datatype_example" # character | Limit Search by Setting Scope to Simple Datatype (Optional)
#' var_submission_value <- "submission_value_example" # character | Limit Search by Setting Scope to Submission Value (Optional)
#' var_synonyms <- "synonyms_example" # character | Limit Search by Setting Scope to Synonyms (Optional)
#' var_type <- "type_example" # character | Limit Search by Setting Scope to Type (Optional)
#' var_ui_href <- "ui_href_example" # character | Limit Search by Setting Scope to UI Href (Optional)
#' var_value_domain <- "value_domain_example" # character | Limit Search by Setting Scope to Value Domain (Optional)
#' var_variable_set <- "variable_set_example" # character | Limit Search by Setting Scope to Variable Set (Optional)
#' var_version <- "version_example" # character | Limit Search by Setting Scope to Version (Optional)
#'
#' api_instance <- SearchesApi$new()
#'
#' # Configure HTTP basic authorization: basicAuth
#' api_instance$api_client$username <- Sys.getenv("USERNAME")
#' api_instance$api_client$password <- Sys.getenv("PASSWORD")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$MdrSearchGet(var_q, highlight = var_highlight, start = var_start, page_size = var_page_size, class = var_class, codelist = var_codelist, concept_id = var_concept_id, core = var_core, data_structure = var_data_structure, dataset_structure = var_dataset_structure, definition = var_definition, description = var_description, domain = var_domain, effective_date = var_effective_date, extensible = var_extensible, href = var_href, label = var_label, measure_type = var_measure_type, name = var_name, preferred_term = var_preferred_term, product = var_product, product_group = var_product_group, registration_status = var_registration_status, role_description = var_role_description, sdtm_target = var_sdtm_target, simple_datatype = var_simple_datatype, submission_value = var_submission_value, synonyms = var_synonyms, type = var_type, ui_href = var_ui_href, value_domain = var_value_domain, variable_set = var_variable_set, version = var_versiondata_file = "result.txt")
#' result <- api_instance$MdrSearchGet(var_q, highlight = var_highlight, start = var_start, page_size = var_page_size, class = var_class, codelist = var_codelist, concept_id = var_concept_id, core = var_core, data_structure = var_data_structure, dataset_structure = var_dataset_structure, definition = var_definition, description = var_description, domain = var_domain, effective_date = var_effective_date, extensible = var_extensible, href = var_href, label = var_label, measure_type = var_measure_type, name = var_name, preferred_term = var_preferred_term, product = var_product, product_group = var_product_group, registration_status = var_registration_status, role_description = var_role_description, sdtm_target = var_sdtm_target, simple_datatype = var_simple_datatype, submission_value = var_submission_value, synonyms = var_synonyms, type = var_type, ui_href = var_ui_href, value_domain = var_value_domain, variable_set = var_variable_set, version = var_version)
#' dput(result)
#'
#'
#' ####################  MdrSearchScopesGet  ####################
#'
#' library(openapi)
#'
#' api_instance <- SearchesApi$new()
#'
#' # Configure HTTP basic authorization: basicAuth
#' api_instance$api_client$username <- Sys.getenv("USERNAME")
#' api_instance$api_client$password <- Sys.getenv("PASSWORD")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$MdrSearchScopesGet(data_file = "result.txt")
#' result <- api_instance$MdrSearchScopesGet()
#' dput(result)
#'
#'
#' ####################  MdrSearchScopesScopeGet  ####################
#'
#' library(openapi)
#' var_scope <- DefaultSearchScopes$new() # DefaultSearchScopes | Search Scope
#'
#' api_instance <- SearchesApi$new()
#'
#' # Configure HTTP basic authorization: basicAuth
#' api_instance$api_client$username <- Sys.getenv("USERNAME")
#' api_instance$api_client$password <- Sys.getenv("PASSWORD")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$MdrSearchScopesScopeGet(var_scopedata_file = "result.txt")
#' result <- api_instance$MdrSearchScopesScopeGet(var_scope)
#' dput(result)
#'
#'
#' }
#' @importFrom R6 R6Class
#' @importFrom base64enc base64encode
#' @export
SearchesApi <- R6::R6Class(
  "SearchesApi",
  public = list(
    api_client = NULL,

    #' @description
    #' Initialize a new SearchesApi.
    #'
    #' @param api_client An instance of API client.
    initialize = function(api_client) {
      if (!missing(api_client)) {
        self$api_client <- api_client
      } else {
        self$api_client <- ApiClient$new()
      }
    },

    #' @description
    #' 
    #'
    #' @param q Search Term Expression
    #' @param highlight (optional) Search Highlight Expression
    #' @param start (optional) Initial Search Result Index (default value: 0)
    #' @param page_size (optional) Number of Search Results to Return (default value: 100)
    #' @param class (optional) Limit Search by Setting Scope to Class
    #' @param codelist (optional) Limit Search by Setting Scope to Codelist
    #' @param concept_id (optional) Limit Search by Setting Scope to Concept ID
    #' @param core (optional) Limit Search by Setting Scope to Core
    #' @param data_structure (optional) Limit Search by Setting Scope to Data Structure
    #' @param dataset_structure (optional) Limit Search by Setting Scope to Dataset Structure
    #' @param definition (optional) Limit Search by Setting Scope to Definition
    #' @param description (optional) Limit Search by Setting Scope to Description
    #' @param domain (optional) Limit Search by Setting Scope to Domain
    #' @param effective_date (optional) Limit Search by Setting Scope to Effective Date
    #' @param extensible (optional) Limit Search by Setting Scope to Extensible
    #' @param href (optional) Limit Search by Setting Scope to Href
    #' @param label (optional) Limit Search by Setting Scope to Label
    #' @param measure_type (optional) SLimit Search by Setting Scope to Measure Type
    #' @param name (optional) Limit Search by Setting Scope to Name
    #' @param preferred_term (optional) Limit Search by Setting Scope to Preferred Term
    #' @param product (optional) Limit Search by Setting Scope to Product
    #' @param product_group (optional) Limit Search by Setting Scope to Product Group
    #' @param registration_status (optional) Limit Search by Setting Scope to Registration Status
    #' @param role_description (optional) Limit Search by Setting Scope to Role Description
    #' @param sdtm_target (optional) Limit Search by Setting Scope to SDTM Target
    #' @param simple_datatype (optional) Limit Search by Setting Scope to Simple Datatype
    #' @param submission_value (optional) Limit Search by Setting Scope to Submission Value
    #' @param synonyms (optional) Limit Search by Setting Scope to Synonyms
    #' @param type (optional) Limit Search by Setting Scope to Type
    #' @param ui_href (optional) Limit Search by Setting Scope to UI Href
    #' @param value_domain (optional) Limit Search by Setting Scope to Value Domain
    #' @param variable_set (optional) Limit Search by Setting Scope to Variable Set
    #' @param version (optional) Limit Search by Setting Scope to Version
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return DefaultSearchResponse
    MdrSearchGet = function(q, highlight = NULL, start = 0, page_size = 100, class = NULL, codelist = NULL, concept_id = NULL, core = NULL, data_structure = NULL, dataset_structure = NULL, definition = NULL, description = NULL, domain = NULL, effective_date = NULL, extensible = NULL, href = NULL, label = NULL, measure_type = NULL, name = NULL, preferred_term = NULL, product = NULL, product_group = NULL, registration_status = NULL, role_description = NULL, sdtm_target = NULL, simple_datatype = NULL, submission_value = NULL, synonyms = NULL, type = NULL, ui_href = NULL, value_domain = NULL, variable_set = NULL, version = NULL, data_file = NULL, ...) {
      local_var_response <- self$MdrSearchGetWithHttpInfo(q, highlight, start, page_size, class, codelist, concept_id, core, data_structure, dataset_structure, definition, description, domain, effective_date, extensible, href, label, measure_type, name, preferred_term, product, product_group, registration_status, role_description, sdtm_target, simple_datatype, submission_value, synonyms, type, ui_href, value_domain, variable_set, version, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' 
    #'
    #' @param q Search Term Expression
    #' @param highlight (optional) Search Highlight Expression
    #' @param start (optional) Initial Search Result Index (default value: 0)
    #' @param page_size (optional) Number of Search Results to Return (default value: 100)
    #' @param class (optional) Limit Search by Setting Scope to Class
    #' @param codelist (optional) Limit Search by Setting Scope to Codelist
    #' @param concept_id (optional) Limit Search by Setting Scope to Concept ID
    #' @param core (optional) Limit Search by Setting Scope to Core
    #' @param data_structure (optional) Limit Search by Setting Scope to Data Structure
    #' @param dataset_structure (optional) Limit Search by Setting Scope to Dataset Structure
    #' @param definition (optional) Limit Search by Setting Scope to Definition
    #' @param description (optional) Limit Search by Setting Scope to Description
    #' @param domain (optional) Limit Search by Setting Scope to Domain
    #' @param effective_date (optional) Limit Search by Setting Scope to Effective Date
    #' @param extensible (optional) Limit Search by Setting Scope to Extensible
    #' @param href (optional) Limit Search by Setting Scope to Href
    #' @param label (optional) Limit Search by Setting Scope to Label
    #' @param measure_type (optional) SLimit Search by Setting Scope to Measure Type
    #' @param name (optional) Limit Search by Setting Scope to Name
    #' @param preferred_term (optional) Limit Search by Setting Scope to Preferred Term
    #' @param product (optional) Limit Search by Setting Scope to Product
    #' @param product_group (optional) Limit Search by Setting Scope to Product Group
    #' @param registration_status (optional) Limit Search by Setting Scope to Registration Status
    #' @param role_description (optional) Limit Search by Setting Scope to Role Description
    #' @param sdtm_target (optional) Limit Search by Setting Scope to SDTM Target
    #' @param simple_datatype (optional) Limit Search by Setting Scope to Simple Datatype
    #' @param submission_value (optional) Limit Search by Setting Scope to Submission Value
    #' @param synonyms (optional) Limit Search by Setting Scope to Synonyms
    #' @param type (optional) Limit Search by Setting Scope to Type
    #' @param ui_href (optional) Limit Search by Setting Scope to UI Href
    #' @param value_domain (optional) Limit Search by Setting Scope to Value Domain
    #' @param variable_set (optional) Limit Search by Setting Scope to Variable Set
    #' @param version (optional) Limit Search by Setting Scope to Version
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return API response (DefaultSearchResponse) with additional information such as HTTP status code, headers
    MdrSearchGetWithHttpInfo = function(q, highlight = NULL, start = 0, page_size = 100, class = NULL, codelist = NULL, concept_id = NULL, core = NULL, data_structure = NULL, dataset_structure = NULL, definition = NULL, description = NULL, domain = NULL, effective_date = NULL, extensible = NULL, href = NULL, label = NULL, measure_type = NULL, name = NULL, preferred_term = NULL, product = NULL, product_group = NULL, registration_status = NULL, role_description = NULL, sdtm_target = NULL, simple_datatype = NULL, submission_value = NULL, synonyms = NULL, type = NULL, ui_href = NULL, value_domain = NULL, variable_set = NULL, version = NULL, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`q`)) {
        stop("Missing required parameter `q`.")
      }

      if (!missing(`q`) && is.null(`q`)) {
        stop("Invalid value for `q` when calling SearchesApi$MdrSearchGet, `q` is not nullable")
      }

      if (!missing(`highlight`) && is.null(`highlight`)) {
        stop("Invalid value for `highlight` when calling SearchesApi$MdrSearchGet, `highlight` is not nullable")
      }

      if (!missing(`start`) && is.null(`start`)) {
        stop("Invalid value for `start` when calling SearchesApi$MdrSearchGet, `start` is not nullable")
      }

      if (!missing(`page_size`) && is.null(`page_size`)) {
        stop("Invalid value for `page_size` when calling SearchesApi$MdrSearchGet, `page_size` is not nullable")
      }

      if (!missing(`class`) && is.null(`class`)) {
        stop("Invalid value for `class` when calling SearchesApi$MdrSearchGet, `class` is not nullable")
      }

      if (!missing(`codelist`) && is.null(`codelist`)) {
        stop("Invalid value for `codelist` when calling SearchesApi$MdrSearchGet, `codelist` is not nullable")
      }

      if (!missing(`concept_id`) && is.null(`concept_id`)) {
        stop("Invalid value for `concept_id` when calling SearchesApi$MdrSearchGet, `concept_id` is not nullable")
      }

      if (!missing(`core`) && is.null(`core`)) {
        stop("Invalid value for `core` when calling SearchesApi$MdrSearchGet, `core` is not nullable")
      }

      if (!missing(`data_structure`) && is.null(`data_structure`)) {
        stop("Invalid value for `data_structure` when calling SearchesApi$MdrSearchGet, `data_structure` is not nullable")
      }

      if (!missing(`dataset_structure`) && is.null(`dataset_structure`)) {
        stop("Invalid value for `dataset_structure` when calling SearchesApi$MdrSearchGet, `dataset_structure` is not nullable")
      }

      if (!missing(`definition`) && is.null(`definition`)) {
        stop("Invalid value for `definition` when calling SearchesApi$MdrSearchGet, `definition` is not nullable")
      }

      if (!missing(`description`) && is.null(`description`)) {
        stop("Invalid value for `description` when calling SearchesApi$MdrSearchGet, `description` is not nullable")
      }

      if (!missing(`domain`) && is.null(`domain`)) {
        stop("Invalid value for `domain` when calling SearchesApi$MdrSearchGet, `domain` is not nullable")
      }

      if (!missing(`effective_date`) && is.null(`effective_date`)) {
        stop("Invalid value for `effective_date` when calling SearchesApi$MdrSearchGet, `effective_date` is not nullable")
      }

      if (!missing(`extensible`) && is.null(`extensible`)) {
        stop("Invalid value for `extensible` when calling SearchesApi$MdrSearchGet, `extensible` is not nullable")
      }

      if (!missing(`href`) && is.null(`href`)) {
        stop("Invalid value for `href` when calling SearchesApi$MdrSearchGet, `href` is not nullable")
      }

      if (!missing(`label`) && is.null(`label`)) {
        stop("Invalid value for `label` when calling SearchesApi$MdrSearchGet, `label` is not nullable")
      }

      if (!missing(`measure_type`) && is.null(`measure_type`)) {
        stop("Invalid value for `measure_type` when calling SearchesApi$MdrSearchGet, `measure_type` is not nullable")
      }

      if (!missing(`name`) && is.null(`name`)) {
        stop("Invalid value for `name` when calling SearchesApi$MdrSearchGet, `name` is not nullable")
      }

      if (!missing(`preferred_term`) && is.null(`preferred_term`)) {
        stop("Invalid value for `preferred_term` when calling SearchesApi$MdrSearchGet, `preferred_term` is not nullable")
      }

      if (!missing(`product`) && is.null(`product`)) {
        stop("Invalid value for `product` when calling SearchesApi$MdrSearchGet, `product` is not nullable")
      }

      if (!missing(`product_group`) && is.null(`product_group`)) {
        stop("Invalid value for `product_group` when calling SearchesApi$MdrSearchGet, `product_group` is not nullable")
      }

      if (!missing(`registration_status`) && is.null(`registration_status`)) {
        stop("Invalid value for `registration_status` when calling SearchesApi$MdrSearchGet, `registration_status` is not nullable")
      }

      if (!missing(`role_description`) && is.null(`role_description`)) {
        stop("Invalid value for `role_description` when calling SearchesApi$MdrSearchGet, `role_description` is not nullable")
      }

      if (!missing(`sdtm_target`) && is.null(`sdtm_target`)) {
        stop("Invalid value for `sdtm_target` when calling SearchesApi$MdrSearchGet, `sdtm_target` is not nullable")
      }

      if (!missing(`simple_datatype`) && is.null(`simple_datatype`)) {
        stop("Invalid value for `simple_datatype` when calling SearchesApi$MdrSearchGet, `simple_datatype` is not nullable")
      }

      if (!missing(`submission_value`) && is.null(`submission_value`)) {
        stop("Invalid value for `submission_value` when calling SearchesApi$MdrSearchGet, `submission_value` is not nullable")
      }

      if (!missing(`synonyms`) && is.null(`synonyms`)) {
        stop("Invalid value for `synonyms` when calling SearchesApi$MdrSearchGet, `synonyms` is not nullable")
      }

      if (!missing(`type`) && is.null(`type`)) {
        stop("Invalid value for `type` when calling SearchesApi$MdrSearchGet, `type` is not nullable")
      }

      if (!missing(`ui_href`) && is.null(`ui_href`)) {
        stop("Invalid value for `ui_href` when calling SearchesApi$MdrSearchGet, `ui_href` is not nullable")
      }

      if (!missing(`value_domain`) && is.null(`value_domain`)) {
        stop("Invalid value for `value_domain` when calling SearchesApi$MdrSearchGet, `value_domain` is not nullable")
      }

      if (!missing(`variable_set`) && is.null(`variable_set`)) {
        stop("Invalid value for `variable_set` when calling SearchesApi$MdrSearchGet, `variable_set` is not nullable")
      }

      if (!missing(`version`) && is.null(`version`)) {
        stop("Invalid value for `version` when calling SearchesApi$MdrSearchGet, `version` is not nullable")
      }

      query_params[["q"]] <- `q`

      query_params[["highlight"]] <- `highlight`

      query_params[["start"]] <- `start`

      query_params[["pageSize"]] <- `page_size`

      query_params[["class"]] <- `class`

      query_params[["codelist"]] <- `codelist`

      query_params[["conceptId"]] <- `concept_id`

      query_params[["core"]] <- `core`

      query_params[["dataStructure"]] <- `data_structure`

      query_params[["datasetStructure"]] <- `dataset_structure`

      query_params[["definition"]] <- `definition`

      query_params[["description"]] <- `description`

      query_params[["domain"]] <- `domain`

      query_params[["effectiveDate"]] <- `effective_date`

      query_params[["extensible"]] <- `extensible`

      query_params[["href"]] <- `href`

      query_params[["label"]] <- `label`

      query_params[["measureType"]] <- `measure_type`

      query_params[["name"]] <- `name`

      query_params[["preferredTerm"]] <- `preferred_term`

      query_params[["product"]] <- `product`

      query_params[["productGroup"]] <- `product_group`

      query_params[["registrationStatus"]] <- `registration_status`

      query_params[["roleDescription"]] <- `role_description`

      query_params[["sdtmTarget"]] <- `sdtm_target`

      query_params[["simpleDatatype"]] <- `simple_datatype`

      query_params[["submissionValue"]] <- `submission_value`

      query_params[["synonyms"]] <- `synonyms`

      query_params[["type"]] <- `type`

      query_params[["uiHref"]] <- `ui_href`

      query_params[["valueDomain"]] <- `value_domain`

      query_params[["variableSet"]] <- `variable_set`

      query_params[["version"]] <- `version`

      local_var_url_path <- "/mdr/search"
      # HTTP basic auth
      if (!is.null(self$api_client$username) || !is.null(self$api_client$password)) {
        header_params["Authorization"] <- paste("Basic", base64enc::base64encode(charToRaw(paste(self$api_client$username, self$api_client$password, sep = ":"))))
      }

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "DefaultSearchResponse"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' 
    #'
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return MdrSearchScopesGet200Response
    MdrSearchScopesGet = function(data_file = NULL, ...) {
      local_var_response <- self$MdrSearchScopesGetWithHttpInfo(data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' 
    #'
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return API response (MdrSearchScopesGet200Response) with additional information such as HTTP status code, headers
    MdrSearchScopesGetWithHttpInfo = function(data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      local_var_url_path <- "/mdr/search/scopes"
      # HTTP basic auth
      if (!is.null(self$api_client$username) || !is.null(self$api_client$password)) {
        header_params["Authorization"] <- paste("Basic", base64enc::base64encode(charToRaw(paste(self$api_client$username, self$api_client$password, sep = ":"))))
      }

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "MdrSearchScopesGet200Response"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' 
    #'
    #' @param scope Search Scope
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return ScopeValues
    MdrSearchScopesScopeGet = function(scope, data_file = NULL, ...) {
      local_var_response <- self$MdrSearchScopesScopeGetWithHttpInfo(scope, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' 
    #'
    #' @param scope Search Scope
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return API response (ScopeValues) with additional information such as HTTP status code, headers
    MdrSearchScopesScopeGetWithHttpInfo = function(scope, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`scope`)) {
        stop("Missing required parameter `scope`.")
      }

      if (!missing(`scope`) && is.null(`scope`)) {
        stop("Invalid value for `scope` when calling SearchesApi$MdrSearchScopesScopeGet, `scope` is not nullable")
      }

      local_var_url_path <- "/mdr/search/scopes/{scope}"
      if (!missing(`scope`)) {
        local_var_url_path <- gsub("\\{scope\\}", URLencode(as.character(`scope`), reserved = TRUE), local_var_url_path)
      }

      # HTTP basic auth
      if (!is.null(self$api_client$username) || !is.null(self$api_client$password)) {
        header_params["Authorization"] <- paste("Basic", base64enc::base64encode(charToRaw(paste(self$api_client$username, self$api_client$password, sep = ":"))))
      }

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "ScopeValues"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    }
  )
)
