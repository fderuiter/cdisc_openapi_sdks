#' CDISC Library API
#'
#' REST API Documentation for CDISC Library in production server
#'
#' The version of the OpenAPI document: 1.1.0
#' Contact: cdisc_library_support@cdisc.org
#' Generated by: https://openapi-generator.tech
#'
#' @docType class
#' @title ControlledTerminologyCT operations
#' @description ControlledTerminologyCTApi
#' @format An \code{R6Class} generator object
#' @field api_client Handles the client-server communication.
#'
#' @examples
#' \dontrun{
#' ####################  MdrCtPackagesGet  ####################
#'
#' library(openapi)
#'
#' api_instance <- ControlledTerminologyCTApi$new()
#'
#' # Configure HTTP basic authorization: basicAuth
#' api_instance$api_client$username <- Sys.getenv("USERNAME")
#' api_instance$api_client$password <- Sys.getenv("PASSWORD")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$MdrCtPackagesGet(data_file = "result.txt")
#' result <- api_instance$MdrCtPackagesGet()
#' dput(result)
#'
#'
#' ####################  MdrCtPackagesPackageCodelistsCodelistGet  ####################
#'
#' library(openapi)
#' var_package <- "sdtmct-2019-12-20" # character | CT Package Identifier
#' var_codelist <- "C67154" # character | CT Codelist Identifier
#'
#' api_instance <- ControlledTerminologyCTApi$new()
#'
#' # Configure HTTP basic authorization: basicAuth
#' api_instance$api_client$username <- Sys.getenv("USERNAME")
#' api_instance$api_client$password <- Sys.getenv("PASSWORD")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$MdrCtPackagesPackageCodelistsCodelistGet(var_package, var_codelistdata_file = "result.txt")
#' result <- api_instance$MdrCtPackagesPackageCodelistsCodelistGet(var_package, var_codelist)
#' dput(result)
#'
#'
#' ####################  MdrCtPackagesPackageCodelistsCodelistTermsGet  ####################
#'
#' library(openapi)
#' var_package <- "sdtmct-2019-12-20" # character | CT Package Identifier
#' var_codelist <- "C67154" # character | CT Codelist Identifier
#'
#' api_instance <- ControlledTerminologyCTApi$new()
#'
#' # Configure HTTP basic authorization: basicAuth
#' api_instance$api_client$username <- Sys.getenv("USERNAME")
#' api_instance$api_client$password <- Sys.getenv("PASSWORD")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$MdrCtPackagesPackageCodelistsCodelistTermsGet(var_package, var_codelistdata_file = "result.txt")
#' result <- api_instance$MdrCtPackagesPackageCodelistsCodelistTermsGet(var_package, var_codelist)
#' dput(result)
#'
#'
#' ####################  MdrCtPackagesPackageCodelistsCodelistTermsTermGet  ####################
#'
#' library(openapi)
#' var_package <- "sdtmct-2019-12-20" # character | CT Package Product Identifier
#' var_codelist <- "C67154" # character | CT Codelist Identifier
#' var_term <- "C64796" # character | CT Codelist Term Identifier
#'
#' api_instance <- ControlledTerminologyCTApi$new()
#'
#' # Configure HTTP basic authorization: basicAuth
#' api_instance$api_client$username <- Sys.getenv("USERNAME")
#' api_instance$api_client$password <- Sys.getenv("PASSWORD")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$MdrCtPackagesPackageCodelistsCodelistTermsTermGet(var_package, var_codelist, var_termdata_file = "result.txt")
#' result <- api_instance$MdrCtPackagesPackageCodelistsCodelistTermsTermGet(var_package, var_codelist, var_term)
#' dput(result)
#'
#'
#' ####################  MdrCtPackagesPackageCodelistsGet  ####################
#'
#' library(openapi)
#' var_package <- "sdtmct-2019-12-20" # character | CT Package Identifier
#'
#' api_instance <- ControlledTerminologyCTApi$new()
#'
#' # Configure HTTP basic authorization: basicAuth
#' api_instance$api_client$username <- Sys.getenv("USERNAME")
#' api_instance$api_client$password <- Sys.getenv("PASSWORD")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$MdrCtPackagesPackageCodelistsGet(var_packagedata_file = "result.txt")
#' result <- api_instance$MdrCtPackagesPackageCodelistsGet(var_package)
#' dput(result)
#'
#'
#' ####################  MdrCtPackagesProductGet  ####################
#'
#' library(openapi)
#' var_product <- "sdtmct-2019-12-20" # character | CT Package Product Identifier
#'
#' api_instance <- ControlledTerminologyCTApi$new()
#'
#' # Configure HTTP basic authorization: basicAuth
#' api_instance$api_client$username <- Sys.getenv("USERNAME")
#' api_instance$api_client$password <- Sys.getenv("PASSWORD")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$MdrCtPackagesProductGet(var_productdata_file = "result.txt")
#' result <- api_instance$MdrCtPackagesProductGet(var_product)
#' dput(result)
#'
#'
#' ####################  MdrRootCtProductGroupCodelistsCodelistGet  ####################
#'
#' library(openapi)
#' var_product_group <- "sdtmct" # character | CT Product Group Identifier
#' var_codelist <- "C67154" # character | CT Codelist Identifier
#'
#' api_instance <- ControlledTerminologyCTApi$new()
#'
#' # Configure HTTP basic authorization: basicAuth
#' api_instance$api_client$username <- Sys.getenv("USERNAME")
#' api_instance$api_client$password <- Sys.getenv("PASSWORD")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$MdrRootCtProductGroupCodelistsCodelistGet(var_product_group, var_codelistdata_file = "result.txt")
#' result <- api_instance$MdrRootCtProductGroupCodelistsCodelistGet(var_product_group, var_codelist)
#' dput(result)
#'
#'
#' ####################  MdrRootCtProductGroupCodelistsCodelistTermsTermGet  ####################
#'
#' library(openapi)
#' var_product_group <- "sdtmct" # character | CT Product Group Identifier
#' var_codelist <- "C67154" # character | CT Codelist Identifier
#' var_term <- "C64796" # character | CT Codelist Term Identifier
#'
#' api_instance <- ControlledTerminologyCTApi$new()
#'
#' # Configure HTTP basic authorization: basicAuth
#' api_instance$api_client$username <- Sys.getenv("USERNAME")
#' api_instance$api_client$password <- Sys.getenv("PASSWORD")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$MdrRootCtProductGroupCodelistsCodelistTermsTermGet(var_product_group, var_codelist, var_termdata_file = "result.txt")
#' result <- api_instance$MdrRootCtProductGroupCodelistsCodelistTermsTermGet(var_product_group, var_codelist, var_term)
#' dput(result)
#'
#'
#' }
#' @importFrom R6 R6Class
#' @importFrom base64enc base64encode
#' @export
ControlledTerminologyCTApi <- R6::R6Class(
  "ControlledTerminologyCTApi",
  public = list(
    api_client = NULL,

    #' @description
    #' Initialize a new ControlledTerminologyCTApi.
    #'
    #' @param api_client An instance of API client.
    initialize = function(api_client) {
      if (!missing(api_client)) {
        self$api_client <- api_client
      } else {
        self$api_client <- ApiClient$new()
      }
    },

    #' @description
    #' 
    #'
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return CtPackages
    MdrCtPackagesGet = function(data_file = NULL, ...) {
      local_var_response <- self$MdrCtPackagesGetWithHttpInfo(data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' 
    #'
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return API response (CtPackages) with additional information such as HTTP status code, headers
    MdrCtPackagesGetWithHttpInfo = function(data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      local_var_url_path <- "/mdr/ct/packages"
      # HTTP basic auth
      if (!is.null(self$api_client$username) || !is.null(self$api_client$password)) {
        header_params["Authorization"] <- paste("Basic", base64enc::base64encode(charToRaw(paste(self$api_client$username, self$api_client$password, sep = ":"))))
      }

      # The Accept request HTTP header
      local_var_accepts <- list("application/json", "application/xml")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "CtPackages"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' 
    #'
    #' @param package CT Package Identifier
    #' @param codelist CT Codelist Identifier
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return CtCodelist
    MdrCtPackagesPackageCodelistsCodelistGet = function(package, codelist, data_file = NULL, ...) {
      local_var_response <- self$MdrCtPackagesPackageCodelistsCodelistGetWithHttpInfo(package, codelist, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' 
    #'
    #' @param package CT Package Identifier
    #' @param codelist CT Codelist Identifier
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return API response (CtCodelist) with additional information such as HTTP status code, headers
    MdrCtPackagesPackageCodelistsCodelistGetWithHttpInfo = function(package, codelist, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`package`)) {
        stop("Missing required parameter `package`.")
      }

      if (missing(`codelist`)) {
        stop("Missing required parameter `codelist`.")
      }

      if (!missing(`package`) && is.null(`package`)) {
        stop("Invalid value for `package` when calling ControlledTerminologyCTApi$MdrCtPackagesPackageCodelistsCodelistGet, `package` is not nullable")
      }

      if (!missing(`codelist`) && is.null(`codelist`)) {
        stop("Invalid value for `codelist` when calling ControlledTerminologyCTApi$MdrCtPackagesPackageCodelistsCodelistGet, `codelist` is not nullable")
      }

      local_var_url_path <- "/mdr/ct/packages/{package}/codelists/{codelist}"
      if (!missing(`package`)) {
        local_var_url_path <- gsub("\\{package\\}", URLencode(as.character(`package`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`codelist`)) {
        local_var_url_path <- gsub("\\{codelist\\}", URLencode(as.character(`codelist`), reserved = TRUE), local_var_url_path)
      }

      # HTTP basic auth
      if (!is.null(self$api_client$username) || !is.null(self$api_client$password)) {
        header_params["Authorization"] <- paste("Basic", base64enc::base64encode(charToRaw(paste(self$api_client$username, self$api_client$password, sep = ":"))))
      }

      # The Accept request HTTP header
      local_var_accepts <- list("application/json", "application/xml")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "CtCodelist"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' 
    #'
    #' @param package CT Package Identifier
    #' @param codelist CT Codelist Identifier
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return CtCodelistTerms
    MdrCtPackagesPackageCodelistsCodelistTermsGet = function(package, codelist, data_file = NULL, ...) {
      local_var_response <- self$MdrCtPackagesPackageCodelistsCodelistTermsGetWithHttpInfo(package, codelist, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' 
    #'
    #' @param package CT Package Identifier
    #' @param codelist CT Codelist Identifier
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return API response (CtCodelistTerms) with additional information such as HTTP status code, headers
    MdrCtPackagesPackageCodelistsCodelistTermsGetWithHttpInfo = function(package, codelist, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`package`)) {
        stop("Missing required parameter `package`.")
      }

      if (missing(`codelist`)) {
        stop("Missing required parameter `codelist`.")
      }

      if (!missing(`package`) && is.null(`package`)) {
        stop("Invalid value for `package` when calling ControlledTerminologyCTApi$MdrCtPackagesPackageCodelistsCodelistTermsGet, `package` is not nullable")
      }

      if (!missing(`codelist`) && is.null(`codelist`)) {
        stop("Invalid value for `codelist` when calling ControlledTerminologyCTApi$MdrCtPackagesPackageCodelistsCodelistTermsGet, `codelist` is not nullable")
      }

      local_var_url_path <- "/mdr/ct/packages/{package}/codelists/{codelist}/terms"
      if (!missing(`package`)) {
        local_var_url_path <- gsub("\\{package\\}", URLencode(as.character(`package`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`codelist`)) {
        local_var_url_path <- gsub("\\{codelist\\}", URLencode(as.character(`codelist`), reserved = TRUE), local_var_url_path)
      }

      # HTTP basic auth
      if (!is.null(self$api_client$username) || !is.null(self$api_client$password)) {
        header_params["Authorization"] <- paste("Basic", base64enc::base64encode(charToRaw(paste(self$api_client$username, self$api_client$password, sep = ":"))))
      }

      # The Accept request HTTP header
      local_var_accepts <- list("application/json", "application/xml")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "CtCodelistTerms"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' 
    #'
    #' @param package CT Package Product Identifier
    #' @param codelist CT Codelist Identifier
    #' @param term CT Codelist Term Identifier
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return CtTerm
    MdrCtPackagesPackageCodelistsCodelistTermsTermGet = function(package, codelist, term, data_file = NULL, ...) {
      local_var_response <- self$MdrCtPackagesPackageCodelistsCodelistTermsTermGetWithHttpInfo(package, codelist, term, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' 
    #'
    #' @param package CT Package Product Identifier
    #' @param codelist CT Codelist Identifier
    #' @param term CT Codelist Term Identifier
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return API response (CtTerm) with additional information such as HTTP status code, headers
    MdrCtPackagesPackageCodelistsCodelistTermsTermGetWithHttpInfo = function(package, codelist, term, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`package`)) {
        stop("Missing required parameter `package`.")
      }

      if (missing(`codelist`)) {
        stop("Missing required parameter `codelist`.")
      }

      if (missing(`term`)) {
        stop("Missing required parameter `term`.")
      }

      if (!missing(`package`) && is.null(`package`)) {
        stop("Invalid value for `package` when calling ControlledTerminologyCTApi$MdrCtPackagesPackageCodelistsCodelistTermsTermGet, `package` is not nullable")
      }

      if (!missing(`codelist`) && is.null(`codelist`)) {
        stop("Invalid value for `codelist` when calling ControlledTerminologyCTApi$MdrCtPackagesPackageCodelistsCodelistTermsTermGet, `codelist` is not nullable")
      }

      if (!missing(`term`) && is.null(`term`)) {
        stop("Invalid value for `term` when calling ControlledTerminologyCTApi$MdrCtPackagesPackageCodelistsCodelistTermsTermGet, `term` is not nullable")
      }

      local_var_url_path <- "/mdr/ct/packages/{package}/codelists/{codelist}/terms/{term}"
      if (!missing(`package`)) {
        local_var_url_path <- gsub("\\{package\\}", URLencode(as.character(`package`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`codelist`)) {
        local_var_url_path <- gsub("\\{codelist\\}", URLencode(as.character(`codelist`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`term`)) {
        local_var_url_path <- gsub("\\{term\\}", URLencode(as.character(`term`), reserved = TRUE), local_var_url_path)
      }

      # HTTP basic auth
      if (!is.null(self$api_client$username) || !is.null(self$api_client$password)) {
        header_params["Authorization"] <- paste("Basic", base64enc::base64encode(charToRaw(paste(self$api_client$username, self$api_client$password, sep = ":"))))
      }

      # The Accept request HTTP header
      local_var_accepts <- list("application/json", "application/xml")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "CtTerm"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' 
    #'
    #' @param package CT Package Identifier
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return CtPackageCodelists
    MdrCtPackagesPackageCodelistsGet = function(package, data_file = NULL, ...) {
      local_var_response <- self$MdrCtPackagesPackageCodelistsGetWithHttpInfo(package, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' 
    #'
    #' @param package CT Package Identifier
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return API response (CtPackageCodelists) with additional information such as HTTP status code, headers
    MdrCtPackagesPackageCodelistsGetWithHttpInfo = function(package, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`package`)) {
        stop("Missing required parameter `package`.")
      }

      if (!missing(`package`) && is.null(`package`)) {
        stop("Invalid value for `package` when calling ControlledTerminologyCTApi$MdrCtPackagesPackageCodelistsGet, `package` is not nullable")
      }

      local_var_url_path <- "/mdr/ct/packages/{package}/codelists"
      if (!missing(`package`)) {
        local_var_url_path <- gsub("\\{package\\}", URLencode(as.character(`package`), reserved = TRUE), local_var_url_path)
      }

      # HTTP basic auth
      if (!is.null(self$api_client$username) || !is.null(self$api_client$password)) {
        header_params["Authorization"] <- paste("Basic", base64enc::base64encode(charToRaw(paste(self$api_client$username, self$api_client$password, sep = ":"))))
      }

      # The Accept request HTTP header
      local_var_accepts <- list("application/json", "application/xml")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "CtPackageCodelists"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' 
    #'
    #' @param product CT Package Product Identifier
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return CtPackage
    MdrCtPackagesProductGet = function(product, data_file = NULL, ...) {
      local_var_response <- self$MdrCtPackagesProductGetWithHttpInfo(product, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' 
    #'
    #' @param product CT Package Product Identifier
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return API response (CtPackage) with additional information such as HTTP status code, headers
    MdrCtPackagesProductGetWithHttpInfo = function(product, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`product`)) {
        stop("Missing required parameter `product`.")
      }

      if (!missing(`product`) && is.null(`product`)) {
        stop("Invalid value for `product` when calling ControlledTerminologyCTApi$MdrCtPackagesProductGet, `product` is not nullable")
      }

      local_var_url_path <- "/mdr/ct/packages/{product}"
      if (!missing(`product`)) {
        local_var_url_path <- gsub("\\{product\\}", URLencode(as.character(`product`), reserved = TRUE), local_var_url_path)
      }

      # HTTP basic auth
      if (!is.null(self$api_client$username) || !is.null(self$api_client$password)) {
        header_params["Authorization"] <- paste("Basic", base64enc::base64encode(charToRaw(paste(self$api_client$username, self$api_client$password, sep = ":"))))
      }

      # The Accept request HTTP header
      local_var_accepts <- list("application/json", "application/xml", "text/csv", "application/vnd.ms-excel", "application/vnd.openxmlformats-officedocument.spreadsheetml.sheet")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "CtPackage"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' 
    #'
    #' @param product_group CT Product Group Identifier
    #' @param codelist CT Codelist Identifier
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return RootCtCodelist
    MdrRootCtProductGroupCodelistsCodelistGet = function(product_group, codelist, data_file = NULL, ...) {
      local_var_response <- self$MdrRootCtProductGroupCodelistsCodelistGetWithHttpInfo(product_group, codelist, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' 
    #'
    #' @param product_group CT Product Group Identifier
    #' @param codelist CT Codelist Identifier
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return API response (RootCtCodelist) with additional information such as HTTP status code, headers
    MdrRootCtProductGroupCodelistsCodelistGetWithHttpInfo = function(product_group, codelist, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`product_group`)) {
        stop("Missing required parameter `product_group`.")
      }

      if (missing(`codelist`)) {
        stop("Missing required parameter `codelist`.")
      }

      if (!missing(`product_group`) && is.null(`product_group`)) {
        stop("Invalid value for `product_group` when calling ControlledTerminologyCTApi$MdrRootCtProductGroupCodelistsCodelistGet, `product_group` is not nullable")
      }

      if (!missing(`codelist`) && is.null(`codelist`)) {
        stop("Invalid value for `codelist` when calling ControlledTerminologyCTApi$MdrRootCtProductGroupCodelistsCodelistGet, `codelist` is not nullable")
      }

      local_var_url_path <- "/mdr/root/ct/{productGroup}/codelists/{codelist}"
      if (!missing(`product_group`)) {
        local_var_url_path <- gsub("\\{productGroup\\}", URLencode(as.character(`product_group`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`codelist`)) {
        local_var_url_path <- gsub("\\{codelist\\}", URLencode(as.character(`codelist`), reserved = TRUE), local_var_url_path)
      }

      # HTTP basic auth
      if (!is.null(self$api_client$username) || !is.null(self$api_client$password)) {
        header_params["Authorization"] <- paste("Basic", base64enc::base64encode(charToRaw(paste(self$api_client$username, self$api_client$password, sep = ":"))))
      }

      # The Accept request HTTP header
      local_var_accepts <- list("application/json", "application/xml")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "RootCtCodelist"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' 
    #'
    #' @param product_group CT Product Group Identifier
    #' @param codelist CT Codelist Identifier
    #' @param term CT Codelist Term Identifier
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return RootCtTerm
    MdrRootCtProductGroupCodelistsCodelistTermsTermGet = function(product_group, codelist, term, data_file = NULL, ...) {
      local_var_response <- self$MdrRootCtProductGroupCodelistsCodelistTermsTermGetWithHttpInfo(product_group, codelist, term, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' 
    #'
    #' @param product_group CT Product Group Identifier
    #' @param codelist CT Codelist Identifier
    #' @param term CT Codelist Term Identifier
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return API response (RootCtTerm) with additional information such as HTTP status code, headers
    MdrRootCtProductGroupCodelistsCodelistTermsTermGetWithHttpInfo = function(product_group, codelist, term, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`product_group`)) {
        stop("Missing required parameter `product_group`.")
      }

      if (missing(`codelist`)) {
        stop("Missing required parameter `codelist`.")
      }

      if (missing(`term`)) {
        stop("Missing required parameter `term`.")
      }

      if (!missing(`product_group`) && is.null(`product_group`)) {
        stop("Invalid value for `product_group` when calling ControlledTerminologyCTApi$MdrRootCtProductGroupCodelistsCodelistTermsTermGet, `product_group` is not nullable")
      }

      if (!missing(`codelist`) && is.null(`codelist`)) {
        stop("Invalid value for `codelist` when calling ControlledTerminologyCTApi$MdrRootCtProductGroupCodelistsCodelistTermsTermGet, `codelist` is not nullable")
      }

      if (!missing(`term`) && is.null(`term`)) {
        stop("Invalid value for `term` when calling ControlledTerminologyCTApi$MdrRootCtProductGroupCodelistsCodelistTermsTermGet, `term` is not nullable")
      }

      local_var_url_path <- "/mdr/root/ct/{productGroup}/codelists/{codelist}/terms/{term}"
      if (!missing(`product_group`)) {
        local_var_url_path <- gsub("\\{productGroup\\}", URLencode(as.character(`product_group`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`codelist`)) {
        local_var_url_path <- gsub("\\{codelist\\}", URLencode(as.character(`codelist`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`term`)) {
        local_var_url_path <- gsub("\\{term\\}", URLencode(as.character(`term`), reserved = TRUE), local_var_url_path)
      }

      # HTTP basic auth
      if (!is.null(self$api_client$username) || !is.null(self$api_client$password)) {
        header_params["Authorization"] <- paste("Basic", base64enc::base64encode(charToRaw(paste(self$api_client$username, self$api_client$password, sep = ":"))))
      }

      # The Accept request HTTP header
      local_var_accepts <- list("application/json", "application/xml")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "RootCtTerm"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    }
  )
)
