#' CDISC Library API
#'
#' REST API Documentation for CDISC Library in production server
#'
#' The version of the OpenAPI document: 1.1.0
#' Contact: cdisc_library_support@cdisc.org
#' Generated by: https://openapi-generator.tech
#'
#' @docType class
#' @title AnalysisDataModelAndImplementationGuideADaMAndADaMIG operations
#' @description AnalysisDataModelAndImplementationGuideADaMAndADaMIGApi
#' @format An \code{R6Class} generator object
#' @field api_client Handles the client-server communication.
#'
#' @examples
#' \dontrun{
#' ####################  MdrAdamProductDatastructuresGet  ####################
#'
#' library(openapi)
#' var_product <- "adamig-1-1" # character | CDISC Library Product
#'
#' api_instance <- AnalysisDataModelAndImplementationGuideADaMAndADaMIGApi$new()
#'
#' # Configure HTTP basic authorization: basicAuth
#' api_instance$api_client$username <- Sys.getenv("USERNAME")
#' api_instance$api_client$password <- Sys.getenv("PASSWORD")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$MdrAdamProductDatastructuresGet(var_productdata_file = "result.txt")
#' result <- api_instance$MdrAdamProductDatastructuresGet(var_product)
#' dput(result)
#'
#'
#' ####################  MdrAdamProductDatastructuresStructureGet  ####################
#'
#' library(openapi)
#' var_product <- "adamig-1-1" # character | CDISC Library Product
#' var_structure <- "ADSL" # character | Data structure Identifier
#'
#' api_instance <- AnalysisDataModelAndImplementationGuideADaMAndADaMIGApi$new()
#'
#' # Configure HTTP basic authorization: basicAuth
#' api_instance$api_client$username <- Sys.getenv("USERNAME")
#' api_instance$api_client$password <- Sys.getenv("PASSWORD")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$MdrAdamProductDatastructuresStructureGet(var_product, var_structuredata_file = "result.txt")
#' result <- api_instance$MdrAdamProductDatastructuresStructureGet(var_product, var_structure)
#' dput(result)
#'
#'
#' ####################  MdrAdamProductDatastructuresStructureVariablesGet  ####################
#'
#' library(openapi)
#' var_product <- "adamig-1-1" # character | CDISC Library Product
#' var_structure <- "ADSL" # character | ADaM Data Structure Identifier
#'
#' api_instance <- AnalysisDataModelAndImplementationGuideADaMAndADaMIGApi$new()
#'
#' # Configure HTTP basic authorization: basicAuth
#' api_instance$api_client$username <- Sys.getenv("USERNAME")
#' api_instance$api_client$password <- Sys.getenv("PASSWORD")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$MdrAdamProductDatastructuresStructureVariablesGet(var_product, var_structuredata_file = "result.txt")
#' result <- api_instance$MdrAdamProductDatastructuresStructureVariablesGet(var_product, var_structure)
#' dput(result)
#'
#'
#' ####################  MdrAdamProductDatastructuresStructureVariablesVarGet  ####################
#'
#' library(openapi)
#' var_product <- "adamig-1-1" # character | CDISC Library Product
#' var_structure <- "ADSL" # character | ADaM Data Structure Identifier
#' var_var <- "SITEGRy" # character | ADaM Variable Identifier
#'
#' api_instance <- AnalysisDataModelAndImplementationGuideADaMAndADaMIGApi$new()
#'
#' # Configure HTTP basic authorization: basicAuth
#' api_instance$api_client$username <- Sys.getenv("USERNAME")
#' api_instance$api_client$password <- Sys.getenv("PASSWORD")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$MdrAdamProductDatastructuresStructureVariablesVarGet(var_product, var_structure, var_vardata_file = "result.txt")
#' result <- api_instance$MdrAdamProductDatastructuresStructureVariablesVarGet(var_product, var_structure, var_var)
#' dput(result)
#'
#'
#' ####################  MdrAdamProductDatastructuresStructureVarsetsGet  ####################
#'
#' library(openapi)
#' var_product <- "adamig-1-1" # character | CDISC Library Product
#' var_structure <- "ADSL" # character | ADaM Data Structure Identifier
#'
#' api_instance <- AnalysisDataModelAndImplementationGuideADaMAndADaMIGApi$new()
#'
#' # Configure HTTP basic authorization: basicAuth
#' api_instance$api_client$username <- Sys.getenv("USERNAME")
#' api_instance$api_client$password <- Sys.getenv("PASSWORD")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$MdrAdamProductDatastructuresStructureVarsetsGet(var_product, var_structuredata_file = "result.txt")
#' result <- api_instance$MdrAdamProductDatastructuresStructureVarsetsGet(var_product, var_structure)
#' dput(result)
#'
#'
#' ####################  MdrAdamProductDatastructuresStructureVarsetsVarsetGet  ####################
#'
#' library(openapi)
#' var_product <- "adamig-1-1" # character | CDISC Library Product
#' var_structure <- "ADSL" # character | ADaM Data Structure Identifier
#' var_varset <- "Identifier" # character | ADaM Variable Set Identifier
#'
#' api_instance <- AnalysisDataModelAndImplementationGuideADaMAndADaMIGApi$new()
#'
#' # Configure HTTP basic authorization: basicAuth
#' api_instance$api_client$username <- Sys.getenv("USERNAME")
#' api_instance$api_client$password <- Sys.getenv("PASSWORD")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$MdrAdamProductDatastructuresStructureVarsetsVarsetGet(var_product, var_structure, var_varsetdata_file = "result.txt")
#' result <- api_instance$MdrAdamProductDatastructuresStructureVarsetsVarsetGet(var_product, var_structure, var_varset)
#' dput(result)
#'
#'
#' ####################  MdrAdamProductGet  ####################
#'
#' library(openapi)
#' var_product <- "adamig-1-1" # character | CDISC Library Product
#'
#' api_instance <- AnalysisDataModelAndImplementationGuideADaMAndADaMIGApi$new()
#'
#' # Configure HTTP basic authorization: basicAuth
#' api_instance$api_client$username <- Sys.getenv("USERNAME")
#' api_instance$api_client$password <- Sys.getenv("PASSWORD")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$MdrAdamProductGet(var_productdata_file = "result.txt")
#' result <- api_instance$MdrAdamProductGet(var_product)
#' dput(result)
#'
#'
#' }
#' @importFrom R6 R6Class
#' @importFrom base64enc base64encode
#' @export
AnalysisDataModelAndImplementationGuideADaMAndADaMIGApi <- R6::R6Class(
  "AnalysisDataModelAndImplementationGuideADaMAndADaMIGApi",
  public = list(
    api_client = NULL,

    #' @description
    #' Initialize a new AnalysisDataModelAndImplementationGuideADaMAndADaMIGApi.
    #'
    #' @param api_client An instance of API client.
    initialize = function(api_client) {
      if (!missing(api_client)) {
        self$api_client <- api_client
      } else {
        self$api_client <- ApiClient$new()
      }
    },

    #' @description
    #' 
    #'
    #' @param product CDISC Library Product
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return AdamProductDatastructures
    MdrAdamProductDatastructuresGet = function(product, data_file = NULL, ...) {
      local_var_response <- self$MdrAdamProductDatastructuresGetWithHttpInfo(product, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' 
    #'
    #' @param product CDISC Library Product
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return API response (AdamProductDatastructures) with additional information such as HTTP status code, headers
    MdrAdamProductDatastructuresGetWithHttpInfo = function(product, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`product`)) {
        stop("Missing required parameter `product`.")
      }

      if (!missing(`product`) && is.null(`product`)) {
        stop("Invalid value for `product` when calling AnalysisDataModelAndImplementationGuideADaMAndADaMIGApi$MdrAdamProductDatastructuresGet, `product` is not nullable")
      }

      local_var_url_path <- "/mdr/adam/{product}/datastructures"
      if (!missing(`product`)) {
        local_var_url_path <- gsub("\\{product\\}", URLencode(as.character(`product`), reserved = TRUE), local_var_url_path)
      }

      # HTTP basic auth
      if (!is.null(self$api_client$username) || !is.null(self$api_client$password)) {
        header_params["Authorization"] <- paste("Basic", base64enc::base64encode(charToRaw(paste(self$api_client$username, self$api_client$password, sep = ":"))))
      }

      # The Accept request HTTP header
      local_var_accepts <- list("application/json", "application/xml", "text/csv")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "AdamProductDatastructures"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' 
    #'
    #' @param product CDISC Library Product
    #' @param structure Data structure Identifier
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return AdamDatastructure
    MdrAdamProductDatastructuresStructureGet = function(product, structure, data_file = NULL, ...) {
      local_var_response <- self$MdrAdamProductDatastructuresStructureGetWithHttpInfo(product, structure, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' 
    #'
    #' @param product CDISC Library Product
    #' @param structure Data structure Identifier
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return API response (AdamDatastructure) with additional information such as HTTP status code, headers
    MdrAdamProductDatastructuresStructureGetWithHttpInfo = function(product, structure, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`product`)) {
        stop("Missing required parameter `product`.")
      }

      if (missing(`structure`)) {
        stop("Missing required parameter `structure`.")
      }

      if (!missing(`product`) && is.null(`product`)) {
        stop("Invalid value for `product` when calling AnalysisDataModelAndImplementationGuideADaMAndADaMIGApi$MdrAdamProductDatastructuresStructureGet, `product` is not nullable")
      }

      if (!missing(`structure`) && is.null(`structure`)) {
        stop("Invalid value for `structure` when calling AnalysisDataModelAndImplementationGuideADaMAndADaMIGApi$MdrAdamProductDatastructuresStructureGet, `structure` is not nullable")
      }

      local_var_url_path <- "/mdr/adam/{product}/datastructures/{structure}"
      if (!missing(`product`)) {
        local_var_url_path <- gsub("\\{product\\}", URLencode(as.character(`product`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`structure`)) {
        local_var_url_path <- gsub("\\{structure\\}", URLencode(as.character(`structure`), reserved = TRUE), local_var_url_path)
      }

      # HTTP basic auth
      if (!is.null(self$api_client$username) || !is.null(self$api_client$password)) {
        header_params["Authorization"] <- paste("Basic", base64enc::base64encode(charToRaw(paste(self$api_client$username, self$api_client$password, sep = ":"))))
      }

      # The Accept request HTTP header
      local_var_accepts <- list("application/json", "application/xml")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "AdamDatastructure"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' 
    #'
    #' @param product CDISC Library Product
    #' @param structure ADaM Data Structure Identifier
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return AdamDatastructureVariables
    MdrAdamProductDatastructuresStructureVariablesGet = function(product, structure, data_file = NULL, ...) {
      local_var_response <- self$MdrAdamProductDatastructuresStructureVariablesGetWithHttpInfo(product, structure, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' 
    #'
    #' @param product CDISC Library Product
    #' @param structure ADaM Data Structure Identifier
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return API response (AdamDatastructureVariables) with additional information such as HTTP status code, headers
    MdrAdamProductDatastructuresStructureVariablesGetWithHttpInfo = function(product, structure, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`product`)) {
        stop("Missing required parameter `product`.")
      }

      if (missing(`structure`)) {
        stop("Missing required parameter `structure`.")
      }

      if (!missing(`product`) && is.null(`product`)) {
        stop("Invalid value for `product` when calling AnalysisDataModelAndImplementationGuideADaMAndADaMIGApi$MdrAdamProductDatastructuresStructureVariablesGet, `product` is not nullable")
      }

      if (!missing(`structure`) && is.null(`structure`)) {
        stop("Invalid value for `structure` when calling AnalysisDataModelAndImplementationGuideADaMAndADaMIGApi$MdrAdamProductDatastructuresStructureVariablesGet, `structure` is not nullable")
      }

      local_var_url_path <- "/mdr/adam/{product}/datastructures/{structure}/variables"
      if (!missing(`product`)) {
        local_var_url_path <- gsub("\\{product\\}", URLencode(as.character(`product`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`structure`)) {
        local_var_url_path <- gsub("\\{structure\\}", URLencode(as.character(`structure`), reserved = TRUE), local_var_url_path)
      }

      # HTTP basic auth
      if (!is.null(self$api_client$username) || !is.null(self$api_client$password)) {
        header_params["Authorization"] <- paste("Basic", base64enc::base64encode(charToRaw(paste(self$api_client$username, self$api_client$password, sep = ":"))))
      }

      # The Accept request HTTP header
      local_var_accepts <- list("application/json", "application/xml")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "AdamDatastructureVariables"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' 
    #'
    #' @param product CDISC Library Product
    #' @param structure ADaM Data Structure Identifier
    #' @param var ADaM Variable Identifier
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return AdamVariable
    MdrAdamProductDatastructuresStructureVariablesVarGet = function(product, structure, var, data_file = NULL, ...) {
      local_var_response <- self$MdrAdamProductDatastructuresStructureVariablesVarGetWithHttpInfo(product, structure, var, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' 
    #'
    #' @param product CDISC Library Product
    #' @param structure ADaM Data Structure Identifier
    #' @param var ADaM Variable Identifier
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return API response (AdamVariable) with additional information such as HTTP status code, headers
    MdrAdamProductDatastructuresStructureVariablesVarGetWithHttpInfo = function(product, structure, var, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`product`)) {
        stop("Missing required parameter `product`.")
      }

      if (missing(`structure`)) {
        stop("Missing required parameter `structure`.")
      }

      if (missing(`var`)) {
        stop("Missing required parameter `var`.")
      }

      if (!missing(`product`) && is.null(`product`)) {
        stop("Invalid value for `product` when calling AnalysisDataModelAndImplementationGuideADaMAndADaMIGApi$MdrAdamProductDatastructuresStructureVariablesVarGet, `product` is not nullable")
      }

      if (!missing(`structure`) && is.null(`structure`)) {
        stop("Invalid value for `structure` when calling AnalysisDataModelAndImplementationGuideADaMAndADaMIGApi$MdrAdamProductDatastructuresStructureVariablesVarGet, `structure` is not nullable")
      }

      if (!missing(`var`) && is.null(`var`)) {
        stop("Invalid value for `var` when calling AnalysisDataModelAndImplementationGuideADaMAndADaMIGApi$MdrAdamProductDatastructuresStructureVariablesVarGet, `var` is not nullable")
      }

      local_var_url_path <- "/mdr/adam/{product}/datastructures/{structure}/variables/{var}"
      if (!missing(`product`)) {
        local_var_url_path <- gsub("\\{product\\}", URLencode(as.character(`product`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`structure`)) {
        local_var_url_path <- gsub("\\{structure\\}", URLencode(as.character(`structure`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`var`)) {
        local_var_url_path <- gsub("\\{var\\}", URLencode(as.character(`var`), reserved = TRUE), local_var_url_path)
      }

      # HTTP basic auth
      if (!is.null(self$api_client$username) || !is.null(self$api_client$password)) {
        header_params["Authorization"] <- paste("Basic", base64enc::base64encode(charToRaw(paste(self$api_client$username, self$api_client$password, sep = ":"))))
      }

      # The Accept request HTTP header
      local_var_accepts <- list("application/json", "application/xml")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "AdamVariable"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' 
    #'
    #' @param product CDISC Library Product
    #' @param structure ADaM Data Structure Identifier
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return AdamDatastructureVarsets
    MdrAdamProductDatastructuresStructureVarsetsGet = function(product, structure, data_file = NULL, ...) {
      local_var_response <- self$MdrAdamProductDatastructuresStructureVarsetsGetWithHttpInfo(product, structure, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' 
    #'
    #' @param product CDISC Library Product
    #' @param structure ADaM Data Structure Identifier
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return API response (AdamDatastructureVarsets) with additional information such as HTTP status code, headers
    MdrAdamProductDatastructuresStructureVarsetsGetWithHttpInfo = function(product, structure, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`product`)) {
        stop("Missing required parameter `product`.")
      }

      if (missing(`structure`)) {
        stop("Missing required parameter `structure`.")
      }

      if (!missing(`product`) && is.null(`product`)) {
        stop("Invalid value for `product` when calling AnalysisDataModelAndImplementationGuideADaMAndADaMIGApi$MdrAdamProductDatastructuresStructureVarsetsGet, `product` is not nullable")
      }

      if (!missing(`structure`) && is.null(`structure`)) {
        stop("Invalid value for `structure` when calling AnalysisDataModelAndImplementationGuideADaMAndADaMIGApi$MdrAdamProductDatastructuresStructureVarsetsGet, `structure` is not nullable")
      }

      local_var_url_path <- "/mdr/adam/{product}/datastructures/{structure}/varsets"
      if (!missing(`product`)) {
        local_var_url_path <- gsub("\\{product\\}", URLencode(as.character(`product`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`structure`)) {
        local_var_url_path <- gsub("\\{structure\\}", URLencode(as.character(`structure`), reserved = TRUE), local_var_url_path)
      }

      # HTTP basic auth
      if (!is.null(self$api_client$username) || !is.null(self$api_client$password)) {
        header_params["Authorization"] <- paste("Basic", base64enc::base64encode(charToRaw(paste(self$api_client$username, self$api_client$password, sep = ":"))))
      }

      # The Accept request HTTP header
      local_var_accepts <- list("application/json", "application/xml")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "AdamDatastructureVarsets"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' 
    #'
    #' @param product CDISC Library Product
    #' @param structure ADaM Data Structure Identifier
    #' @param varset ADaM Variable Set Identifier
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return AdamVarset
    MdrAdamProductDatastructuresStructureVarsetsVarsetGet = function(product, structure, varset, data_file = NULL, ...) {
      local_var_response <- self$MdrAdamProductDatastructuresStructureVarsetsVarsetGetWithHttpInfo(product, structure, varset, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' 
    #'
    #' @param product CDISC Library Product
    #' @param structure ADaM Data Structure Identifier
    #' @param varset ADaM Variable Set Identifier
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return API response (AdamVarset) with additional information such as HTTP status code, headers
    MdrAdamProductDatastructuresStructureVarsetsVarsetGetWithHttpInfo = function(product, structure, varset, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`product`)) {
        stop("Missing required parameter `product`.")
      }

      if (missing(`structure`)) {
        stop("Missing required parameter `structure`.")
      }

      if (missing(`varset`)) {
        stop("Missing required parameter `varset`.")
      }

      if (!missing(`product`) && is.null(`product`)) {
        stop("Invalid value for `product` when calling AnalysisDataModelAndImplementationGuideADaMAndADaMIGApi$MdrAdamProductDatastructuresStructureVarsetsVarsetGet, `product` is not nullable")
      }

      if (!missing(`structure`) && is.null(`structure`)) {
        stop("Invalid value for `structure` when calling AnalysisDataModelAndImplementationGuideADaMAndADaMIGApi$MdrAdamProductDatastructuresStructureVarsetsVarsetGet, `structure` is not nullable")
      }

      if (!missing(`varset`) && is.null(`varset`)) {
        stop("Invalid value for `varset` when calling AnalysisDataModelAndImplementationGuideADaMAndADaMIGApi$MdrAdamProductDatastructuresStructureVarsetsVarsetGet, `varset` is not nullable")
      }

      local_var_url_path <- "/mdr/adam/{product}/datastructures/{structure}/varsets/{varset}"
      if (!missing(`product`)) {
        local_var_url_path <- gsub("\\{product\\}", URLencode(as.character(`product`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`structure`)) {
        local_var_url_path <- gsub("\\{structure\\}", URLencode(as.character(`structure`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`varset`)) {
        local_var_url_path <- gsub("\\{varset\\}", URLencode(as.character(`varset`), reserved = TRUE), local_var_url_path)
      }

      # HTTP basic auth
      if (!is.null(self$api_client$username) || !is.null(self$api_client$password)) {
        header_params["Authorization"] <- paste("Basic", base64enc::base64encode(charToRaw(paste(self$api_client$username, self$api_client$password, sep = ":"))))
      }

      # The Accept request HTTP header
      local_var_accepts <- list("application/json", "application/xml")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "AdamVarset"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' 
    #'
    #' @param product CDISC Library Product
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return AdamProduct
    MdrAdamProductGet = function(product, data_file = NULL, ...) {
      local_var_response <- self$MdrAdamProductGetWithHttpInfo(product, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' 
    #'
    #' @param product CDISC Library Product
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return API response (AdamProduct) with additional information such as HTTP status code, headers
    MdrAdamProductGetWithHttpInfo = function(product, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`product`)) {
        stop("Missing required parameter `product`.")
      }

      if (!missing(`product`) && is.null(`product`)) {
        stop("Invalid value for `product` when calling AnalysisDataModelAndImplementationGuideADaMAndADaMIGApi$MdrAdamProductGet, `product` is not nullable")
      }

      local_var_url_path <- "/mdr/adam/{product}"
      if (!missing(`product`)) {
        local_var_url_path <- gsub("\\{product\\}", URLencode(as.character(`product`), reserved = TRUE), local_var_url_path)
      }

      # HTTP basic auth
      if (!is.null(self$api_client$username) || !is.null(self$api_client$password)) {
        header_params["Authorization"] <- paste("Basic", base64enc::base64encode(charToRaw(paste(self$api_client$username, self$api_client$password, sep = ":"))))
      }

      # The Accept request HTTP header
      local_var_accepts <- list("application/json", "application/xml", "text/csv", "application/vnd.ms-excel", "application/vnd.openxmlformats-officedocument.spreadsheetml.sheet")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "AdamProduct"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    }
  )
)
