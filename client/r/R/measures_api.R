#' CDISC Library API
#'
#' REST API Documentation for CDISC Library in production server
#'
#' The version of the OpenAPI document: 1.1.0
#' Contact: cdisc_library_support@cdisc.org
#' Generated by: https://openapi-generator.tech
#'
#' @docType class
#' @title Measures operations
#' @description MeasuresApi
#' @format An \code{R6Class} generator object
#' @field api_client Handles the client-server communication.
#'
#' @examples
#' \dontrun{
#' ####################  MdrQrsMeasureVersionGet  ####################
#'
#' library(openapi)
#' var_measure <- "AIMS1" # character | QRS Measure Identifier
#' var_version <- "1-0" # character | CDISC Library Product Version
#'
#' api_instance <- MeasuresApi$new()
#'
#' # Configure HTTP basic authorization: basicAuth
#' api_instance$api_client$username <- Sys.getenv("USERNAME")
#' api_instance$api_client$password <- Sys.getenv("PASSWORD")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$MdrQrsMeasureVersionGet(var_measure, var_versiondata_file = "result.txt")
#' result <- api_instance$MdrQrsMeasureVersionGet(var_measure, var_version)
#' dput(result)
#'
#'
#' ####################  MdrQrsMeasureVersionItemsGet  ####################
#'
#' library(openapi)
#' var_measure <- "AIMS1" # character | QRS Measure Identifier
#' var_version <- "1-0" # character | CDISC Library Product Version
#'
#' api_instance <- MeasuresApi$new()
#'
#' # Configure HTTP basic authorization: basicAuth
#' api_instance$api_client$username <- Sys.getenv("USERNAME")
#' api_instance$api_client$password <- Sys.getenv("PASSWORD")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$MdrQrsMeasureVersionItemsGet(var_measure, var_versiondata_file = "result.txt")
#' result <- api_instance$MdrQrsMeasureVersionItemsGet(var_measure, var_version)
#' dput(result)
#'
#'
#' ####################  MdrQrsMeasureVersionItemsItemGet  ####################
#'
#' library(openapi)
#' var_measure <- "AIMS1" # character | QRS Measure Identifier
#' var_version <- "1-0" # character | CDISC Library Product Version
#' var_item <- "AIMS01.001" # character | QRS Measure Item Identifier
#'
#' api_instance <- MeasuresApi$new()
#'
#' # Configure HTTP basic authorization: basicAuth
#' api_instance$api_client$username <- Sys.getenv("USERNAME")
#' api_instance$api_client$password <- Sys.getenv("PASSWORD")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$MdrQrsMeasureVersionItemsItemGet(var_measure, var_version, var_itemdata_file = "result.txt")
#' result <- api_instance$MdrQrsMeasureVersionItemsItemGet(var_measure, var_version, var_item)
#' dput(result)
#'
#'
#' ####################  MdrQrsMeasureVersionResponsegroupsGet  ####################
#'
#' library(openapi)
#' var_measure <- "AIMS1" # character | QRS Measure Identifier
#' var_version <- "1-0" # character | CDISC Library Product Version
#'
#' api_instance <- MeasuresApi$new()
#'
#' # Configure HTTP basic authorization: basicAuth
#' api_instance$api_client$username <- Sys.getenv("USERNAME")
#' api_instance$api_client$password <- Sys.getenv("PASSWORD")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$MdrQrsMeasureVersionResponsegroupsGet(var_measure, var_versiondata_file = "result.txt")
#' result <- api_instance$MdrQrsMeasureVersionResponsegroupsGet(var_measure, var_version)
#' dput(result)
#'
#'
#' ####################  MdrQrsMeasureVersionResponsegroupsResponsegroupGet  ####################
#'
#' library(openapi)
#' var_measure <- "AIMS1" # character | QRS Measure Identifier
#' var_version <- "1-0" # character | CDISC Library Product Version
#' var_responsegroup <- "AIMS1.11to12" # character | QRS Measure's Response Group Identifier
#'
#' api_instance <- MeasuresApi$new()
#'
#' # Configure HTTP basic authorization: basicAuth
#' api_instance$api_client$username <- Sys.getenv("USERNAME")
#' api_instance$api_client$password <- Sys.getenv("PASSWORD")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$MdrQrsMeasureVersionResponsegroupsResponsegroupGet(var_measure, var_version, var_responsegroupdata_file = "result.txt")
#' result <- api_instance$MdrQrsMeasureVersionResponsegroupsResponsegroupGet(var_measure, var_version, var_responsegroup)
#' dput(result)
#'
#'
#' }
#' @importFrom R6 R6Class
#' @importFrom base64enc base64encode
#' @export
MeasuresApi <- R6::R6Class(
  "MeasuresApi",
  public = list(
    api_client = NULL,

    #' @description
    #' Initialize a new MeasuresApi.
    #'
    #' @param api_client An instance of API client.
    initialize = function(api_client) {
      if (!missing(api_client)) {
        self$api_client <- api_client
      } else {
        self$api_client <- ApiClient$new()
      }
    },

    #' @description
    #' 
    #'
    #' @param measure QRS Measure Identifier
    #' @param version CDISC Library Product Version
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return QrsProduct
    MdrQrsMeasureVersionGet = function(measure, version, data_file = NULL, ...) {
      local_var_response <- self$MdrQrsMeasureVersionGetWithHttpInfo(measure, version, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' 
    #'
    #' @param measure QRS Measure Identifier
    #' @param version CDISC Library Product Version
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return API response (QrsProduct) with additional information such as HTTP status code, headers
    MdrQrsMeasureVersionGetWithHttpInfo = function(measure, version, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`measure`)) {
        stop("Missing required parameter `measure`.")
      }

      if (missing(`version`)) {
        stop("Missing required parameter `version`.")
      }

      if (!missing(`measure`) && is.null(`measure`)) {
        stop("Invalid value for `measure` when calling MeasuresApi$MdrQrsMeasureVersionGet, `measure` is not nullable")
      }

      if (!missing(`version`) && is.null(`version`)) {
        stop("Invalid value for `version` when calling MeasuresApi$MdrQrsMeasureVersionGet, `version` is not nullable")
      }

      local_var_url_path <- "/mdr/qrs/{measure}/{version}"
      if (!missing(`measure`)) {
        local_var_url_path <- gsub("\\{measure\\}", URLencode(as.character(`measure`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`version`)) {
        local_var_url_path <- gsub("\\{version\\}", URLencode(as.character(`version`), reserved = TRUE), local_var_url_path)
      }

      # HTTP basic auth
      if (!is.null(self$api_client$username) || !is.null(self$api_client$password)) {
        header_params["Authorization"] <- paste("Basic", base64enc::base64encode(charToRaw(paste(self$api_client$username, self$api_client$password, sep = ":"))))
      }

      # The Accept request HTTP header
      local_var_accepts <- list("application/json", "application/xml", "text/csv", "application/vnd.ms-excel")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "QrsProduct"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' 
    #'
    #' @param measure QRS Measure Identifier
    #' @param version CDISC Library Product Version
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return QrsItems
    MdrQrsMeasureVersionItemsGet = function(measure, version, data_file = NULL, ...) {
      local_var_response <- self$MdrQrsMeasureVersionItemsGetWithHttpInfo(measure, version, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' 
    #'
    #' @param measure QRS Measure Identifier
    #' @param version CDISC Library Product Version
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return API response (QrsItems) with additional information such as HTTP status code, headers
    MdrQrsMeasureVersionItemsGetWithHttpInfo = function(measure, version, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`measure`)) {
        stop("Missing required parameter `measure`.")
      }

      if (missing(`version`)) {
        stop("Missing required parameter `version`.")
      }

      if (!missing(`measure`) && is.null(`measure`)) {
        stop("Invalid value for `measure` when calling MeasuresApi$MdrQrsMeasureVersionItemsGet, `measure` is not nullable")
      }

      if (!missing(`version`) && is.null(`version`)) {
        stop("Invalid value for `version` when calling MeasuresApi$MdrQrsMeasureVersionItemsGet, `version` is not nullable")
      }

      local_var_url_path <- "/mdr/qrs/{measure}/{version}/items"
      if (!missing(`measure`)) {
        local_var_url_path <- gsub("\\{measure\\}", URLencode(as.character(`measure`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`version`)) {
        local_var_url_path <- gsub("\\{version\\}", URLencode(as.character(`version`), reserved = TRUE), local_var_url_path)
      }

      # HTTP basic auth
      if (!is.null(self$api_client$username) || !is.null(self$api_client$password)) {
        header_params["Authorization"] <- paste("Basic", base64enc::base64encode(charToRaw(paste(self$api_client$username, self$api_client$password, sep = ":"))))
      }

      # The Accept request HTTP header
      local_var_accepts <- list("application/json", "application/xml")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "QrsItems"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' 
    #'
    #' @param measure QRS Measure Identifier
    #' @param version CDISC Library Product Version
    #' @param item QRS Measure Item Identifier
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return QrsItem
    MdrQrsMeasureVersionItemsItemGet = function(measure, version, item, data_file = NULL, ...) {
      local_var_response <- self$MdrQrsMeasureVersionItemsItemGetWithHttpInfo(measure, version, item, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' 
    #'
    #' @param measure QRS Measure Identifier
    #' @param version CDISC Library Product Version
    #' @param item QRS Measure Item Identifier
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return API response (QrsItem) with additional information such as HTTP status code, headers
    MdrQrsMeasureVersionItemsItemGetWithHttpInfo = function(measure, version, item, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`measure`)) {
        stop("Missing required parameter `measure`.")
      }

      if (missing(`version`)) {
        stop("Missing required parameter `version`.")
      }

      if (missing(`item`)) {
        stop("Missing required parameter `item`.")
      }

      if (!missing(`measure`) && is.null(`measure`)) {
        stop("Invalid value for `measure` when calling MeasuresApi$MdrQrsMeasureVersionItemsItemGet, `measure` is not nullable")
      }

      if (!missing(`version`) && is.null(`version`)) {
        stop("Invalid value for `version` when calling MeasuresApi$MdrQrsMeasureVersionItemsItemGet, `version` is not nullable")
      }

      if (!missing(`item`) && is.null(`item`)) {
        stop("Invalid value for `item` when calling MeasuresApi$MdrQrsMeasureVersionItemsItemGet, `item` is not nullable")
      }

      local_var_url_path <- "/mdr/qrs/{measure}/{version}/items/{item}"
      if (!missing(`measure`)) {
        local_var_url_path <- gsub("\\{measure\\}", URLencode(as.character(`measure`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`version`)) {
        local_var_url_path <- gsub("\\{version\\}", URLencode(as.character(`version`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`item`)) {
        local_var_url_path <- gsub("\\{item\\}", URLencode(as.character(`item`), reserved = TRUE), local_var_url_path)
      }

      # HTTP basic auth
      if (!is.null(self$api_client$username) || !is.null(self$api_client$password)) {
        header_params["Authorization"] <- paste("Basic", base64enc::base64encode(charToRaw(paste(self$api_client$username, self$api_client$password, sep = ":"))))
      }

      # The Accept request HTTP header
      local_var_accepts <- list("application/json", "application/xml")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "QrsItem"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' 
    #'
    #' @param measure QRS Measure Identifier
    #' @param version CDISC Library Product Version
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return QrsResponsegroups
    MdrQrsMeasureVersionResponsegroupsGet = function(measure, version, data_file = NULL, ...) {
      local_var_response <- self$MdrQrsMeasureVersionResponsegroupsGetWithHttpInfo(measure, version, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' 
    #'
    #' @param measure QRS Measure Identifier
    #' @param version CDISC Library Product Version
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return API response (QrsResponsegroups) with additional information such as HTTP status code, headers
    MdrQrsMeasureVersionResponsegroupsGetWithHttpInfo = function(measure, version, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`measure`)) {
        stop("Missing required parameter `measure`.")
      }

      if (missing(`version`)) {
        stop("Missing required parameter `version`.")
      }

      if (!missing(`measure`) && is.null(`measure`)) {
        stop("Invalid value for `measure` when calling MeasuresApi$MdrQrsMeasureVersionResponsegroupsGet, `measure` is not nullable")
      }

      if (!missing(`version`) && is.null(`version`)) {
        stop("Invalid value for `version` when calling MeasuresApi$MdrQrsMeasureVersionResponsegroupsGet, `version` is not nullable")
      }

      local_var_url_path <- "/mdr/qrs/{measure}/{version}/responsegroups"
      if (!missing(`measure`)) {
        local_var_url_path <- gsub("\\{measure\\}", URLencode(as.character(`measure`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`version`)) {
        local_var_url_path <- gsub("\\{version\\}", URLencode(as.character(`version`), reserved = TRUE), local_var_url_path)
      }

      # HTTP basic auth
      if (!is.null(self$api_client$username) || !is.null(self$api_client$password)) {
        header_params["Authorization"] <- paste("Basic", base64enc::base64encode(charToRaw(paste(self$api_client$username, self$api_client$password, sep = ":"))))
      }

      # The Accept request HTTP header
      local_var_accepts <- list("application/json", "application/xml")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "QrsResponsegroups"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' 
    #'
    #' @param measure QRS Measure Identifier
    #' @param version CDISC Library Product Version
    #' @param responsegroup QRS Measure's Response Group Identifier
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return QrsResponsegroup
    MdrQrsMeasureVersionResponsegroupsResponsegroupGet = function(measure, version, responsegroup, data_file = NULL, ...) {
      local_var_response <- self$MdrQrsMeasureVersionResponsegroupsResponsegroupGetWithHttpInfo(measure, version, responsegroup, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' 
    #'
    #' @param measure QRS Measure Identifier
    #' @param version CDISC Library Product Version
    #' @param responsegroup QRS Measure's Response Group Identifier
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return API response (QrsResponsegroup) with additional information such as HTTP status code, headers
    MdrQrsMeasureVersionResponsegroupsResponsegroupGetWithHttpInfo = function(measure, version, responsegroup, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`measure`)) {
        stop("Missing required parameter `measure`.")
      }

      if (missing(`version`)) {
        stop("Missing required parameter `version`.")
      }

      if (missing(`responsegroup`)) {
        stop("Missing required parameter `responsegroup`.")
      }

      if (!missing(`measure`) && is.null(`measure`)) {
        stop("Invalid value for `measure` when calling MeasuresApi$MdrQrsMeasureVersionResponsegroupsResponsegroupGet, `measure` is not nullable")
      }

      if (!missing(`version`) && is.null(`version`)) {
        stop("Invalid value for `version` when calling MeasuresApi$MdrQrsMeasureVersionResponsegroupsResponsegroupGet, `version` is not nullable")
      }

      if (!missing(`responsegroup`) && is.null(`responsegroup`)) {
        stop("Invalid value for `responsegroup` when calling MeasuresApi$MdrQrsMeasureVersionResponsegroupsResponsegroupGet, `responsegroup` is not nullable")
      }

      local_var_url_path <- "/mdr/qrs/{measure}/{version}/responsegroups/{responsegroup}"
      if (!missing(`measure`)) {
        local_var_url_path <- gsub("\\{measure\\}", URLencode(as.character(`measure`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`version`)) {
        local_var_url_path <- gsub("\\{version\\}", URLencode(as.character(`version`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`responsegroup`)) {
        local_var_url_path <- gsub("\\{responsegroup\\}", URLencode(as.character(`responsegroup`), reserved = TRUE), local_var_url_path)
      }

      # HTTP basic auth
      if (!is.null(self$api_client$username) || !is.null(self$api_client$password)) {
        header_params["Authorization"] <- paste("Basic", base64enc::base64encode(charToRaw(paste(self$api_client$username, self$api_client$password, sep = ":"))))
      }

      # The Accept request HTTP header
      local_var_accepts <- list("application/json", "application/xml")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "QrsResponsegroup"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    }
  )
)
